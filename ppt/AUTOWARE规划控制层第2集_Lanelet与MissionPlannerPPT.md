---
marp: true
theme: default
paginate: true
size: 16:9
footer: "Autoware 规划与控制层教程 | 第 2 集"
---

<!--
用途说明：
这是一份第 2 集的 PPT 制作稿，采用 Marp Markdown 格式。
你可以直接按每个 --- 分隔的页面制作 PowerPoint，也可以使用 Marp 导出为 pptx。

建议导出命令：
marp AUTOWARE规划控制层第2集_Lanelet与MissionPlannerPPT.md --pptx

本集重点：
从 Lanelet 地图出发，讲清楚 Mission Planner 如何把连续空间中的起点/目标点，
转换成车道图上的搜索问题，并最终输出 route sections。
-->

# Lanelet 与 Mission Planner

## 从地图车道图到车道级 Route

```text
Goal + Current Pose + Lanelet Map
        ->
Lane-level Route
```

<!--
讲解备注：
本集承接第 1 集。第 1 集讲了 Goal -> Route -> Path -> Trajectory -> Control，
这一集专门放大 Route 这一层，讲它到底是怎么从地图中生成的。

配图建议：
左侧画一张道路高精地图，右侧画被高亮的一串车道 route。
-->

---

# 本集在总链路中的位置

```text
Goal
  -> Route          <- 本集重点
  -> Path
  -> Trajectory
  -> Control Command
```

Mission Planner 解决的是：

```text
应该经过哪些车道？
```

<!--
讲解备注：
先把本集放回总链路。强调：Mission Planner 不输出控制器直接跟踪的轨迹，
它输出的是车道级路线和可行驶走廊。

配图建议：
沿用第 1 集总链路图，只高亮 Route。
-->

---

# 今天要解决的问题

给定：

- 当前车辆位姿
- 目标位姿
- 可选途经点
- Lanelet 高精地图

输出：

```text
一条车道级 Route
```

<!--
讲解备注：
本集的问题很具体：连续世界里的点，如何变成地图拓扑图上的车道序列。

配图建议：
画车辆当前 pose 和目标 pose，地图里高亮一串 lanelet。
-->

---

# 先纠正一个误解

路线规划不是：

```text
画一条能开的曲线
```

而是：

```text
找出车辆应该经过哪些车道，
以及局部规划可以在哪些相邻车道中活动。
```

<!--
讲解备注：
很多新手会把 route 和 path 混在一起。这一页先拆开概念。
Route 是车道级导航结果，不直接处理每个轨迹点的速度和控制。

配图建议：
左边画错误理解：一条细曲线；右边画正确理解：一串被选中的车道区域。
-->

---

# 本集主线

```text
Lanelet 地图
  -> 车道图 G=(V,E)
  -> 起点/目标点匹配到 lanelet
  -> 图搜索得到主路线
  -> 扩展成 route sections
  -> 目标合法性与重规划安全检查
```

<!--
讲解备注：
这一页是本集目录。每讲完一个部分都可以回到这张图。

配图建议：
横向流程图，后面每个大章节高亮对应节点。
-->

---

# Lanelet 地图不是图片

`lanelet::LaneletMap` 是矢量高精地图容器。

它包含：

- Points：真实世界坐标点
- LineStrings：边界线、停止线等
- Lanelets：由左右边界围成的车道单元
- Areas：停车区、人行横道等区域
- RegulatoryElements：限速、红绿灯、优先权等规则

<!--
讲解备注：
强调 Lanelet 地图不是栅格图，也不是背景图片。它是由几何元素和语义关系组成的数据结构。

配图建议：
分层图：点层、线层、lanelet 层、交通规则层。
-->

---

# 一个最小 Lanelet 例子

假设一条直车道：

```text
长度 20 m
宽度 3.5 m
方向沿 +x
```

四个点：

```text
left_start  = (0, 0)
left_end    = (20, 0)
right_start = (0, 3.5)
right_end   = (20, 3.5)
```

<!--
讲解备注：
用最小数据例子帮助新手建立几何直觉。不要一开始就讲复杂 OSM 文件。

配图建议：
画一个矩形车道，标注 x=0 到 x=20，y=0 到 y=3.5。
-->

---

# 从点到边界线

边界线是有序点列：

```text
left_bound  = [left_start, left_end]
right_bound = [right_start, right_end]
```

lanelet 由左右边界构成：

```text
Lanelet 7 = (left_bound, right_bound)
```

这个 lanelet 表示一个有方向的可行驶区域。

<!--
讲解备注：
重点讲“有序”。边界点顺序决定了 lanelet 的方向和几何结构。

配图建议：
把左边界和右边界分别画成两条有箭头的线，再用透明色填充车道区域。
-->

---

# LaneletMap 的层结构

一个简单地图可以包含：

| 元素类型 | 数量 | 作用 |
|---|---:|---|
| Point3d | 4 | 几何坐标点 |
| LineString3d | 2 | 左右边界 |
| Lanelet | 1 | 可行驶车道单元 |

真实地图只是这个结构的大规模扩展。

<!--
讲解备注：
这里让观众理解：复杂地图不是另一种东西，只是点、线、lanelet 和规则的数量更多，关系更复杂。

配图建议：
层级树：LaneletMap -> pointLayer / lineStringLayer / laneletLayer。
-->

---

# 点如何匹配到 Lanelet

车辆当前位置：

```text
p = (10.0, 1.75)
```

在这个例子里：

```text
0 <= x <= 20
0 <= y <= 3.5
```

所以车辆点落在 Lanelet 7 内。

<!--
讲解备注：
这是后面 Pose -> Lanelet 的前置直觉。起点和目标点都是连续坐标，
路线规划必须先知道它们属于哪个车道单元。

配图建议：
在矩形车道中心画一个车辆点，标注坐标。
-->

---

# Lanelet 的数学抽象

第 i 个车道片段可以写成：

$$
\ell_i = (B_i^L, B_i^R, C_i, A_i)
$$

其中：

- $B_i^L$：左边界折线
- $B_i^R$：右边界折线
- $C_i$：中心线折线
- $A_i$：属性集合

<!--
讲解备注：
这页开始把几何例子抽象成公式。公式不要念太快，每个符号都要对应到图上。

配图建议：
一条 lanelet 上标出 left bound、right bound、centerline、属性标签。
-->

---

# 中心线有什么用

中心线通常是一串点：

$$
C_i=\{c_{i,0},c_{i,1},\dots,c_{i,n}\}
$$

它用于计算：

- 车道长度
- 车道方向
- 点在车道中的弧长位置
- 目标高度投影
- 后续路径参考线

<!--
讲解备注：
中心线不是最终轨迹，但它是很多几何计算的基础。

配图建议：
画左右边界和中间中心线，并在中心线上标注 s 坐标。
-->

---

# 车道长度

车道长度通常沿中心线累加：

$$
L(\ell_i)
=
\sum_{k=0}^{n-1}
\|c_{i,k+1}-c_{i,k}\|_2
$$

这比直接量起点到终点距离更适合弯道。

<!--
讲解备注：
弯道里，直线距离不能代表车辆真实沿车道行驶的距离。中心线弧长更符合车辆前进方向。

配图建议：
画一条弯曲中心线，标注每段小线段长度相加。
-->

---

# 从 OSM 到 Lanelet

Lanelet2 OSM 文件常见三层：

```text
node      -> 几何点
way       -> 有序折线
relation  -> lanelet 或交通规则
```

注意：

```text
OSM node 不是路线图里的图节点。
```

路线图里的节点通常是一个完整 lanelet。

<!--
讲解备注：
这是新手很容易混淆的点。OSM node 是地图文件里的点，路线搜索图节点是 lanelet。

配图建议：
画 OSM node -> way -> relation(type=lanelet) 的层级转换。
-->

---

# OSM relation 表示 Lanelet

一个 lanelet relation 通常引用左右边界：

```xml
<relation id='9102'>
  <member type='way' ref='9536' role='left' />
  <member type='way' ref='9462' role='right' />
  <tag k='one_way' v='yes' />
  <tag k='subtype' v='road' />
  <tag k='type' v='lanelet' />
</relation>
```

<!--
讲解备注：
不要把 XML 细节讲太深。这里的目的只是让观众知道：
relation 通过 left/right way 组合成一个 lanelet。

配图建议：
左边放简化 XML，右边画对应的 lanelet 几何。
-->

---

# 经纬度要投影到地图坐标

OSM node 可能保存：

$$
g_k=(lat_k, lon_k, ele_k)
$$

路线规划需要局部平面坐标：

$$
p_k=\Pi(g_k)=(x_k,y_k,z_k)
$$

这样才能做距离、多边形和图搜索计算。

<!--
讲解备注：
经纬度不是平面米制坐标，不能直接做欧氏距离。加载地图时要投影到 map frame。

配图建议：
画地球经纬度点 -> 局部 ENU / map 坐标系。
-->

---

# 左右边界围成车道多边形

若左边界为：

$$
B^L=[l_0,l_1,\dots,l_m]
$$

右边界为：

$$
B^R=[r_0,r_1,\dots,r_n]
$$

车道多边形可以理解为：

$$
\operatorname{poly}(\ell)
=B^L\oplus \operatorname{reverse}(B^R)
$$

<!--
讲解备注：
这个多边形用于判断点是否在车道内，也用于目标车辆轮廓合法性检查。

配图建议：
把左边界和反向右边界首尾拼接，形成闭合多边形。
-->

---

# 中心线可以由边界生成

一个常见近似：

$$
b^L(u),\quad b^R(u),\quad u\in[0,1]
$$

按归一化弧长采样左右边界，然后取中点：

$$
c(u)=\frac{b^L(u)+b^R(u)}{2}
$$

离散后得到中心线：

$$
C=\{c(u_0),c(u_1),\dots,c(u_N)\}
$$

<!--
讲解备注：
不要把它讲成唯一方法。这里用来帮助新手理解中心线和左右边界的关系。

配图建议：
左右边界上取同一比例点，中间连线取中点。
-->

---

# 道路为什么能变成图

把所有可通行 lanelet 作为图节点：

$$
V=\{\ell_i\}
$$

把车道之间的允许连接作为边：

$$
E=\{(\ell_i,\ell_j)\}
$$

于是道路地图变成：

$$
G=(V,E)
$$

<!--
讲解备注：
这页是 Mission Planner 的核心抽象。路线规划不是在像素上搜索，而是在 lanelet 图上搜索。

配图建议：
左边画真实车道，右边抽象成节点和有向边。
-->

---

# 图节点是什么

在路线图里：

```text
一个节点 v 通常对应一个 lanelet
```

不是：

```text
一个 OSM node
```

也不是：

```text
一个轨迹点
```

<!--
讲解备注：
反复强调这一点。OSM node 是几何点，route graph node 是车道片段。

配图建议：
用三列对比：OSM node、lanelet、trajectory point。
-->

---

# 第一类边：前后连接

如果 lanelet i 的末端连接 lanelet j 的起点，并且交通规则允许：

$$
(\ell_i,\ell_j,\text{succeeding})\in E
$$

它表示：

```text
车辆可以从 i 向前进入 j
```

<!--
讲解备注：
强调有方向。即使两个 lanelet 几何上接触，也不代表可以反向开。

配图建议：
两个首尾相接的 lanelet，中间画向前箭头。
-->

---

# 前后连接还要看方向

几何上接近还不够，还要看方向连续：

$$
d(End(\ell_i),Start(\ell_j))<\varepsilon
$$

$$
\left|
\operatorname{norm}
(\psi_i^{end}-\psi_j^{start})
\right|
<\theta_{connect}
$$

同时交通规则必须允许通行。

<!--
讲解备注：
这里解释为什么不是所有贴在一起的 lanelet 都能连边。方向和规则同样重要。

配图建议：
画一个合法顺接和一个方向相反但几何靠近的反例。
-->

---

# 第二类边：可换道关系

如果两个 lanelet 共享边界，并且允许跨越：

$$
(\ell_i,\ell_j,\text{right})\in E
$$

或：

$$
(\ell_i,\ell_j,\text{left})\in E
$$

它表示可以从当前车道换到相邻车道。

<!--
讲解备注：
换道边不是单纯几何相邻，还要看线型和交通规则。

配图建议：
画两条同向车道，中间虚线表示可换道。
-->

---

# 相邻不等于可换道

几何上挨着：

```text
AdjacentLeft / AdjacentRight
```

不一定能直接换道：

```text
Left / Right
```

原因可能是：

- 实线
- 隔离带
- 方向不一致
- 交通规则不允许

<!--
讲解备注：
这一页非常关键。后面 route corridor 扩展时会用到。

配图建议：
一组虚线相邻车道和一组实线相邻车道做对比。
-->

---

# 交通规则元素

RegulatoryElements 可能包括：

- 限速
- 红绿灯
- 停止线
- 让行关系
- 优先权关系
- 道路标记

它们不一定直接改变图拓扑，但会影响规则判断和后续行为规划。

<!--
讲解备注：
Mission Planner 主要依赖静态拓扑和规则。动态交通灯状态通常不是这里直接处理的重点。

配图建议：
在 lanelet 上附加 speed_limit、traffic_light、stop_line 标签。
-->

---

# Mission Planner 总流程

```text
1. 等待地图和定位
2. 把起点/目标点变到 map frame
3. 找起点和目标候选 lanelet
4. 在 lanelet 图上搜索主路线
5. 扩展成 route sections
6. 检查目标合法性和环路
7. 发布 route 和状态
```

<!--
讲解备注：
这是 Mission Planner 的主流程图。后面每一页解释其中一个步骤。

配图建议：
竖向流程图，每个步骤一个方块。
-->

---

# 输入和输出

输入：
- 当前车辆位姿
- 目标位姿
- 可选途经点
- Lanelet 地图
- 车辆尺寸和参数
- 路线状态与旧路线
输出：
- Route
- Route Sections
- Route State
- 诊断和可视化信息

<!--
讲解备注：
这里强调 Mission Planner 不是纯算法函数，而是长期运行的系统模块。

配图建议：
左侧输入列表，中间 Mission Planner，右侧输出列表。
-->

---

# 位姿先统一到地图坐标系

如果位姿在坐标系 $F_a$ 中，需要转到 map frame $F_m$：

$$
p_m=R_{ma}p_a+t_{ma}
$$

$$
q_m=q_{ma}\otimes q_a
$$

否则同一个数值坐标可能表示完全不同的位置。

<!--
讲解备注：
路线规划所有几何判断都必须在同一坐标系下。坐标系没统一，后面的车道匹配和距离计算都没有意义。

配图建议：
画 base_link、odom、map 三个坐标系，用箭头表示变换。
-->

---

# 偏航角也要归一化

从四元数取 yaw：

$$
\psi=\operatorname{atan2}
\left(
2(wz+xy),
1-2(y^2+z^2)
\right)
$$

角度差归一化：

$$
\operatorname{norm}(\theta)=
\operatorname{atan2}(\sin\theta,\cos\theta)
$$

<!--
讲解备注：
接近 pi 和 -pi 的角度如果不归一化，会被误认为差很多。朝向判断会出错。

配图建议：
画圆形角度轴，标注 pi 和 -pi 实际上是相邻方向。
-->

---

# Pose 到 Lanelet

起点和目标点是连续位姿：

```text
(x, y, z, yaw)
```

路线搜索需要图节点：

```text
lanelet id
```

所以必须先做：

$$
\text{Pose}\rightarrow \text{Lanelet}
$$

<!--
讲解备注：
这一步是从连续几何世界到离散图搜索世界的桥。

配图建议：
车辆点落在多个候选 lanelet 附近，然后映射成 lanelet id。
-->

---

# 候选车道怎么找

理想情况下，起点位于某个道路多边形内：

$$
S=
\{\ell\in V
\mid
p_s\in \operatorname{poly}(\ell),
\ell \text{ 是道路车道}
\}
$$

如果没有落在任何车道内，则在附近搜索最近道路 lanelet 作为兜底。

<!--
讲解备注：
定位误差、地图边界误差、车辆停在路肩附近，都可能导致当前位置稍微偏出车道。

配图建议：
一个点在车道内，一个点稍微偏出车道但在搜索半径内。
-->

---

# 不能只看最近中心线

错误做法：

```text
哪个中心线最近，就选哪个 lanelet
```

更稳妥的顺序：

```text
先看点是否在车道多边形内
再看朝向是否合理
再看能否路由到目标
最后再用代价选择
```

<!--
讲解备注：
在宽车道、路口、多车道并行区域，最近中心线不一定是正确车道。

配图建议：
点位于一个宽车道内，但另一条中心线几何距离更近的反例。
-->

---

# 起点候选还要看朝向

车辆朝向：

$$
\psi_s
$$

候选车道方向：

$$
\psi_\ell(p_s)
$$

角度差：

$$
\Delta\psi_s=
\left|
\operatorname{norm}
(\psi_\ell(p_s)-\psi_s)
\right|
$$

通常至少要求：

$$
\Delta\psi_s\le \frac{\pi}{2}
$$

<!--
讲解备注：
如果车辆在双向道路附近，只看距离可能选到反方向车道。朝向过滤能避免这种错误。

配图建议：
车辆夹在两个方向相反的车道之间，画出车头方向和车道方向。
-->

---

# 中心线投影用于弧长

对中心线第 k 段：

$$
a=c_k,\quad b=c_{k+1},\quad d=b-a
$$

投影参数：

$$
u_k=
\operatorname{clip}
\left(
\frac{(p-a)^Td}{\|d\|^2},
0,1
\right)
$$

投影点：

$$
r_k=a+u_kd
$$

<!--
讲解备注：
强调投影发生在“已经选定某个 lanelet 之后”。它用于计算该车道内的纵向位置，不是单独用来地图匹配。

配图建议：
点到中心线某一段的垂线投影，标注 u。
-->

---

# 弧长坐标 s

选定最近投影段 $k^*$ 后：

$$
s(p)=
\sum_{j=0}^{k^*-1}
\|c_{j+1}-c_j\|
+
u_{k^*}
\|c_{k^*+1}-c_{k^*}\|
$$

弧长用于：

- 判断当前位置离车道末端多远
- 判断目标点在终点车道中的位置
- 计算共同路段长度

<!--
讲解备注：
弧长 s 是 route 层很多安全判断的基础。后面重规划也会用到。

配图建议：
中心线上从起点到投影点累计长度，标出 s。
-->

---

# 起点 lanelet 的综合代价

多个候选起点都能到目标时，可以比较：

$$
J_{\text{start}}(\ell_s)
=
L_{\text{route}}(\ell_s,\ell_g)
+
\alpha\Delta\psi_s
$$

含义：

```text
路线不要太长，
起点方向也不要太别扭。
```

<!--
讲解备注：
这个代价帮助在多个候选车道中选更自然的起点。实际系统中朝向惩罚会比较重要。

配图建议：
两个候选起点 lanelet：一个路径短但朝向错，一个路径稍长但方向正确。
-->

---

# 目标 lanelet 也要谨慎选择

目标点处理类似起点，但额外关注：

- 目标位置是否合法
- 目标朝向是否顺着车道
- 车辆轮廓是否放得下
- 重规划时是否优先沿用当前路线附近候选

目标不是“点在车道内就一定合法”。

<!--
讲解备注：
这一页先做预告，后面会展开目标合法性检查。

配图建议：
一个目标点在车道内但车头朝反方向的例子。
-->

---

# 图搜索：从起点车道到目标车道

确定起点车道 $\ell_s$ 和目标车道 $\ell_g$ 后：

$$
\ell_s \leadsto \ell_g
$$

问题变成：

```text
在 lanelet 图上求一条最小代价路径
```

常见方法：

- Dijkstra
- A*

<!--
讲解备注：
这里是连续几何问题转成离散图搜索问题的时刻。

配图建议：
若干 lanelet 节点和有向边，高亮从 start 到 goal 的路径。
-->

---

# 边代价怎么设计

最基本代价可以是车道长度：

$$
w(e)=L(v)
$$

如果是换道边，可以加惩罚：

$$
w(e)=L(v)+\lambda_{lc}
$$

直觉：

```text
距离差不多时，少换道通常更稳定。
```

<!--
讲解备注：
路线规划不一定只追求几何最短。代价表达了工程偏好。

配图建议：
两条路线长度接近，一条频繁换道，一条保持车道。
-->

---

# 最短路形式

最优路线：

$$
P^*
=
\arg\min_{P:\ell_s\leadsto \ell_g}
\sum_{e\in P} w(e)
$$

搜索结束后回溯前驱节点：

$$
P^*=(\ell_0,\ell_1,\dots,\ell_n)
$$

其中：

$$
\ell_0=\ell_s,\quad \ell_n=\ell_g
$$

<!--
讲解备注：
这页只需要让观众知道：搜索结果是一串 lanelet，不是一串轨迹点。

配图建议：
图搜索结果从起点节点到终点节点形成 lanelet 序列。
-->

---

# 途经点：分段最短路

若有检查点序列：

$$
Q=(q_0,q_1,\dots,q_m)
$$

对相邻检查点分别求：

$$
P_i^*=
\operatorname{ShortestPath}(q_{i-1},q_i)
$$

最终拼接：

$$
P^*=P_1^*\oplus P_2^*\oplus\dots\oplus P_m^*
$$

<!--
讲解备注：
途经点是硬约束，所以按顺序分段求解是合理的。注意拼接处需要去掉重复 lanelet。

配图建议：
起点、两个途经点、终点，每一段用不同颜色。
-->

---

# 不可通行车道

如果要避开不可通行车道，可以把代价设为无穷大：

$$
w'(u,v)=
\begin{cases}
+\infty,
& u \text{ 或 } v \text{ 不可通行}\\
w(u,v),
& \text{其他情况}
\end{cases}
$$

搜索会自然绕开这些车道。

<!--
讲解备注：
比直接删除节点更灵活，因为同一张地图可以根据任务切换代价模型。

配图建议：
某条 lanelet 标红为不可通行，路线绕行。
-->

---

# 为什么不能只输出主路线

主路线是：

$$
P^*=(\ell_0,\ell_1,\dots,\ell_n)
$$

但下游还需要知道：

- 相邻车道能不能用
- 换道时的局部空间
- 避障时可借用范围
- 每段路的可行驶走廊

所以输出要组织成 route sections。

<!--
讲解备注：
Mission Planner 不是只给一条“细线”，而是给下游一个车道级走廊。

配图建议：
左边一条主车道线，右边包含相邻车道的宽走廊。
-->

---

# Route Section 是什么

对主路线中的每个首选车道 $\ell_i$：

$$
R_i=
(\ell_i^{pref},
\{\ell_{i,1},\ell_{i,2},\dots,\ell_{i,k}\})
$$

其中：

- `preferred_primitive`：主路径上的 lanelet
- `primitives`：同一横向切片内可用 lanelet 集合

<!--
讲解备注：
preferred 是主干，primitives 是这段路允许下游规划使用的车道集合。

配图建议：
每个纵向切片中高亮 preferred lanelet，同时用浅色标出 primitives。
-->

---

# Route Section 的直觉

Route section 让下游看到的是：

```text
带宽度的可行驶走廊
```

而不是：

```text
没有宽度的一条线
```

这对换道、避障、并线、分流都很重要。

<!--
讲解备注：
这一页把 route section 的意义讲成直觉，不放公式。

配图建议：
用透明蓝色覆盖可行驶走廊，主路线用深蓝箭头。
-->

---

# 从主路线扩展走廊

第一步：

```text
把主路线 lanelet 全部加入走廊集合 R
```

第二步：

```text
把可换道邻居 Left / Right 加入 R
```

第三步：

```text
把仅相邻候选 AdjacentLeft / AdjacentRight 放入候选集合 C
```

<!--
讲解备注：
这里讲集合构造。可换道邻居可以直接加入，仅相邻候选要额外检查。

配图建议：
三种颜色：主路线、可换道邻居、仅相邻候选。
-->

---

# 仅相邻候选为什么要检查

Adjacent 候选不一定能合法进出主路线。

因此需要确认它：

```text
向前能接回路线走廊
向后也能接回路线走廊
```

只有前后都能接回，才加入 route corridor。

<!--
讲解备注：
避免把只是贴在旁边、但无法合法进入或离开主路线的 lanelet 加进来。

配图建议：
一个相邻车道前后都能接回主线，另一个相邻车道中途断开不能接回。
-->

---

# 候选加入条件

定义：

$$
B_{prev}=\text{向前能接回 R}
$$

$$
B_{next}=\text{向后能接回 R}
$$

加入条件：

$$
B_{prev}=\text{true}
\land
B_{next}=\text{true}
\Rightarrow
c\in R
$$

<!--
讲解备注：
这页把前后接回逻辑压缩成一个条件。对新手来说，不需要展开所有递推细节。

配图建议：
一个候选车道两端都有连接箭头回到主走廊。
-->

---

# 空间索引为什么需要

系统经常查询：

- 某位置附近有哪些 route lanelet
- 某个 lanelet 是否属于当前 route
- 目标点附近有哪些候选车道

直接遍历全部车道代价高。

工程上常用 R-tree 等空间索引加速。

<!--
讲解备注：
空间索引不改变路线算法的数学本质，但对实时系统很重要。

配图建议：
地图被分成层级包围盒，一个查询窗口快速找到候选 lanelet。
-->

---

# 目标点不能只看位置

目标点合法性至少要看：

1. 位置是否在允许区域
2. 朝向是否和车道方向一致
3. 车辆轮廓是否落在车道区域内
4. 高度是否贴合地图

点合法，不代表车身合法。

<!--
讲解备注：
这是新手很容易忽略的工程细节。目标 pose 通常代表车辆基准点，不代表整个车身。

配图建议：
目标点在车道内，但车辆矩形轮廓部分压出边界。
-->

---

# 目标朝向检查

目标偏航角：

$$
\psi_g
$$

终点车道方向：

$$
\psi_{\ell_g}(p_g)
$$

要求：

$$
\left|
\operatorname{norm}
(\psi_{\ell_g}(p_g)-\psi_g)
\right|
<\theta_{goal}
$$

<!--
讲解备注：
目标点在车道内但朝反了，也不应该接受为普通道路目标。
停车区可能是特殊情况。

配图建议：
同一个目标点，一个朝向顺车道，一个朝向逆车道。
-->

---

# 车辆轮廓检查

车辆在目标位姿下的轮廓：

$$
f_i^m=R_g f_i+t_g
$$

组成目标车辆多边形：

$$
\mathcal{F}_m
$$

要求它被目标附近车道区域覆盖：

$$
\mathcal{F}_m\subseteq \mathcal{L}_g
$$

<!--
讲解备注：
目标点可能在车道中心，但车头或车尾跨出车道片段边界。实际检查会考虑附近车道区域。

配图建议：
车辆矩形轮廓叠在目标 lanelet 区域上，分别画通过和不通过。
-->

---

# 可选：目标点修正到中心线

用户点击目标点可能不精确。

可以把目标投影到最近中心线：

$$
p_g \rightarrow c^*
$$

朝向使用中心线切线方向：

$$
\psi_g'=\psi_\ell(c^*)
$$

这样目标更贴合车道语义。

<!--
讲解备注：
这是提高易用性的工程处理，不是路线规划的必要数学核心。

配图建议：
用户点击点偏离中心线，系统投影到中心线最近点。
-->

---

# 目标高度也要处理

目标点的 $z$ 值可能不准。

投影到终点车道三维中心线后：

$$
z_g'=z_a+u(z_b-z_a)
$$

最终目标：

$$
p_g'=(x_g,y_g,z_g')
$$

对坡道、桥梁、立交尤其重要。

<!--
讲解备注：
路线规划主要在二维平面上搜索，但输出 pose 是三维的，z 值仍然要合理。

配图建议：
坡道中心线剖面，目标点高度被插值到中心线高度。
-->

---

# 环路为什么会被拒绝

如果路线中出现重复 lanelet：

$$
\exists i\ne j,\quad id(\ell_i)=id(\ell_j)
$$

就可能形成环路或拓扑歧义。

很多下游逻辑默认：

```text
route 是从起点到终点的有序无环走廊
```

<!--
讲解备注：
环路会让“下一个 route lanelet”“距离目标还有多远”等问题变得含糊。

配图建议：
一条绕圈路线中某个 lanelet 被重复经过，标红重复 ID。
-->

---

# 路线请求的两种来源

第一类：

```text
给定起点、途经点、目标点
系统自己搜索 route
```

第二类：

```text
外部已经给出 lanelet route
系统只做记录、检查和发布
```

最终都形成统一抽象：

```text
Lane-level Route
```

<!--
讲解备注：
这页帮助观众理解接口层。Mission Planner 不一定每次都从零图搜索。

配图建议：
两条输入路径汇入同一个 Route 输出。
-->

---

# 路线状态机

常见状态：

| 状态 | 含义 |
|---|---|
| INITIALIZING | 地图或定位未准备 |
| UNSET | 没有路线 |
| ROUTING | 正在首次规划 |
| SET | 已有有效路线 |
| REROUTING | 正在重规划 |
| ARRIVED | 已到达 |

<!--
讲解备注：
Mission Planner 是长期运行的系统，不是调用一次就结束的函数。

配图建议：
状态机图：UNSET -> ROUTING -> SET，SET -> REROUTING -> SET，SET -> ARRIVED。
-->

---

# 状态机的工程意义

状态机保证：

- 没有地图和定位时不规划
- 规划失败时不发布半成品路线
- 已有路线时新目标走重规划流程
- 清除路线后状态回到 UNSET
- 到达目标后状态可被下游感知

<!--
讲解备注：
这页强调工程系统的可靠性。路线规划不只是一个最短路算法。

配图建议：
左边列异常场景，右边列状态机保护动作。
-->

---

# 行驶中重规划为什么危险

静止时换路线比较简单。

行驶中直接换路线可能导致：

- 新路线在车前很近处分叉
- 下游路径规划突然跳变
- 控制器跟踪目标突变
- 车辆没有足够距离完成行为切换

所以重规划需要安全约束。

<!--
讲解备注：
这里为下一页共同路段长度做铺垫。

配图建议：
车辆前方不远处新旧路线分叉，标注“距离太短”。
-->

---

# 新旧路线要有共同路段

重规划通常要求：

```text
新旧路线在车辆前方保持一致足够长
```

共同路段长度：

$$
L_{common}
$$

安全阈值：

$$
L_{safe}
=
\max(vT_{reroute},L_{min})
$$

允许条件：

$$
L_{common}>L_{safe}
$$

<!--
讲解备注：
速度越高，系统需要越长距离来安全切换到新路线。

配图建议：
新旧路线先重合一段，再分叉；标出 common length。
-->

---

# 到达目标不能只看距离

到达通常需要同时满足：

1. 距离足够近
2. 朝向足够接近
3. 车辆保持停止一段时间

距离：

$$
d=
\sqrt{(x-x_g)^2+(y-y_g)^2}
<d_{arrive}
$$

<!--
讲解备注：
只看距离会误判：车辆从目标旁边经过，也可能距离很近。

配图建议：
车辆经过目标附近但未停稳，和车辆停在目标姿态上的对比。
-->

---

# 路线发布与保持

Route 通常不是高频轨迹。

生成后会保持为当前有效路线，直到：

- 新路线替换
- 目标修改触发重规划
- 收到清除请求
- 到达目标
- 发生异常中断

保持语义能让晚启动或重启的下游模块拿到当前路线。

<!--
讲解备注：
这页讲接口语义。Route 不像控制命令那样高频刷新。

配图建议：
Route 发布后被多个下游模块读取，类似持久状态。
-->

---

# 诊断信息也很重要

常见诊断和可视化：

- route corridor 可视化
- preferred lanelet 高亮
- 目标车辆轮廓
- 路线状态
- 规划耗时
- 失败原因

这些信息帮助排查地图、目标点和重规划问题。

<!--
讲解备注：
诊断不是算法核心，但实际调试时非常关键。

配图建议：
RViz 风格图：走廊、目标轮廓和状态面板。
-->

---

# 关键参数直觉

| 参数 | 直观含义 |
|---|---|
| map frame | 所有位姿最终要变到的坐标系 |
| arrival distance | 离目标多近才算到达 |
| arrival angle | 朝向差多小才算到达 |
| goal angle threshold | 目标朝向是否顺着车道 |
| enable correct goal pose | 是否把目标修正到中心线 |
| check footprint inside lanes | 是否检查车身轮廓 |
| reroute time threshold | 重规划预留时间 |
| minimum reroute length | 最小共同路段长度 |

<!--
讲解备注：
参数不要死记。讲调大调小的直觉：更宽松更容易成功，更严格更安全但可能拒绝更多请求。

配图建议：
参数表即可，也可以在右侧放“宽松 <-> 严格”的滑杆。
-->

---

# 一条 Route 是怎样生成的

```text
地图和定位准备好
  -> 起点/目标统一到 map frame
  -> Pose 匹配到候选 lanelet
  -> 起点用朝向和可达性筛选
  -> 在 lanelet 图上求最短路
  -> 主路线扩展为 route sections
  -> 目标合法性检查
  -> 环路检查
  -> 发布 route 并进入 SET
```

<!--
讲解备注：
这一页是完整闭环复盘。建议慢讲，让观众把前面所有点串起来。

配图建议：
长流程图，可以分上下两行展示。
-->

---

# Mission Planner 做什么

它做：

- 把起点、途经点和目标点转成车道图搜索问题
- 在 lanelet 拓扑图上找到车道级路线
- 输出首选车道和相邻可用车道集合
- 检查目标位置、朝向、轮廓和高度
- 管理路线状态、到达判断和安全重规划

<!--
讲解备注：
用正面清单总结 Mission Planner 的职责。

配图建议：
Mission Planner 方块周围发散 5 个职责标签。
-->

---

# Mission Planner 不做什么

它不做：

- 不根据动态障碍物实时绕行
- 不根据交通灯实时状态生成行为
- 不输出控制器直接跟踪的时序轨迹
- 不决定当前方向盘角
- 不保证局部避障和控制级安全

它解决的是：

```text
该走哪些车道？
```

<!--
讲解备注：
职责边界非常重要。不要把下游行为规划、速度规划和控制器的问题都归到 Mission Planner。

配图建议：
左侧 Mission Planner 做的事，右侧下游模块做的事。
-->

---

# 最小数学总结

车道图：

$$
G=(V,E)
$$

起点朝向过滤：

$$
\left|
\operatorname{norm}
(\psi_\ell(p_s)-\psi_s)
\right|
\le \frac{\pi}{2}
$$

最短路：

$$
P^*=
\arg\min_{P:\ell_s\leadsto \ell_g}
\sum_{e\in P}w(e)
$$

<!--
讲解备注：
这是本集最核心的三个数学表达。图、朝向、最短路。

配图建议：
公式左侧配对应小图：图节点、朝向夹角、最短路线。
-->

---

# 再记住三个工程点

第一：

```text
Pose -> Lanelet 不能只看最近中心线
```

第二：

```text
Route 输出的是 route sections，不是一条细线
```

第三：

```text
目标点要检查朝向、轮廓、高度和状态
```

<!--
讲解备注：
这三点是新手从“算法理解”走向“工程理解”的关键。

配图建议：
三张小卡片：地图匹配、route section、目标合法性。
-->

---

# 常见误区

1. OSM node 就是路线图节点
2. lanelet 中心线就是最终轨迹
3. 两个车道挨着就一定能换道
4. 目标点在车道里就一定合法
5. Route 可以直接给控制器跟踪
6. 重规划只是重新跑一次最短路

<!--
讲解备注：
逐条澄清。每条都可以回指前面的对应页面。

配图建议：
做成“误区 / 正确理解”两列表。
-->

---

# 本集总结图

```text
Lanelet Map
  -> Lanelet Graph G=(V,E)
  -> Start / Goal Lanelet
  -> Shortest Lanelet Path
  -> Route Sections
  -> Validated Route
```

Mission Planner 的核心：

```text
把连续位姿导航任务，转成车道图搜索和路线走廊输出。
```

<!--
讲解备注：
这是本集最后的主图。建议和开头主线呼应。

配图建议：
用大流程图展示，每个节点配一个图标。
-->

---

# 课后思考

1. 为什么 OSM node 不是路线搜索图节点？
2. 为什么起点候选不能只看距离？
3. 可换道关系和几何相邻关系有什么区别？
4. 为什么 Mission Planner 要输出 route sections？
5. 为什么行驶中重规划需要共同路段长度？

<!--
讲解备注：
这些问题可以放到视频评论区，也可以作为下一集开头的复习。

配图建议：
纯文字页即可。
-->

---

# 下一集预告

## Hybrid A* Pull Out

下一集会讲：

1. 车辆不在车道中心时怎么办
2. 为什么普通 A* 不够
3. 为什么状态要包含 $(x,y,yaw)$
4. 如何用运动原语搜索可行驶路径
5. 为什么碰撞检查必须考虑车身轮廓

<!--
讲解备注：
自然引到第 3 集：Mission Planner 解决车道级路线，但如果车一开始不在正常车道里，
就需要 Pull Out / Freespace Planning。

配图建议：
车辆停在路边或停车位，规划一条回到主车道的路径。
-->

---

# 下一次给 Codex 的指令

生成下一集时，直接输入：

```text
请根据 AUTOWARE规划控制层视频教程制作指南.md、
AUTOWARE规划控制层第2集_Lanelet与MissionPlannerPPT.md、
concept/hybrid_astar_freespace_pull_out_algorithm_blog.md、
concept/start_planner_module_algorithm_blog.md 和 concept/shift_pull_out_algorithm_blog.md，
生成第 3 集“Hybrid A* Pull Out：车辆如何从自由空间回到车道”的 PPT 制作稿。
要求：
1. 在项目根目录生成 Markdown 格式 PPT 文件；
2. 使用 Marp 风格，以 --- 分隔每一页；
3. 每页包含页面展示内容、讲解备注、配图建议；
4. 面向新手，原理为主，代码为辅；
5. 生成完成后，在文件末尾给出生成第 4 集的指令。
```

<!--
讲解备注：
这一页不一定放进公开视频里，它是给你自己继续制作课程用的。
如果正式录课，可以删除这一页。

配图建议：
不需要配图。
-->

