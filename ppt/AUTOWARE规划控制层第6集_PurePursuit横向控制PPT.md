---
marp: true
theme: default
paginate: true
size: 16:9
footer: "Autoware 规划与控制层教程 | 第 6 集"
---

<!--
用途说明：
这是一份第 6 集的 PPT 制作稿，采用 Marp Markdown 格式。
你可以直接按每个 --- 分隔的页面制作 PowerPoint，也可以使用 Marp 导出为 pptx。

建议导出命令：
marp AUTOWARE规划控制层第6集_PurePursuit横向控制PPT.md --pptx

本集重点：
用几何直觉讲清楚 Pure Pursuit 横向控制：
为什么追前瞻点、圆弧曲率如何推导、曲率如何转成前轮转角，以及工程中如何处理离散轨迹、动态前瞻距离和失败回退。
-->

# Pure Pursuit 横向控制

## 用几何直觉理解路径跟踪

```text
Target Trajectory
  -> Lookahead Point
  -> Circular Arc
  -> Curvature
  -> Steering Angle
```

<!--
讲解备注：
本集承接第 5 集。现在轨迹已经有几何形状和速度，控制器要回答：
车辆此刻应该打多少方向，才能跟踪这条轨迹。

配图建议：
车辆、参考轨迹、前瞻点、连接圆弧和方向盘图标。
-->

---

# 本集在总链路中的位置

```text
Goal
  -> Route
  -> Path
  -> Trajectory
  -> Control Command   <- 本集重点
```

Pure Pursuit 解决的是：

```text
给定当前车辆状态和目标轨迹，
如何计算当前转向？
```

<!--
讲解备注：
第 1 到第 5 集讲的是轨迹如何来、如何平滑、如何加速度。
这集进入控制层，重点是横向控制。

配图建议：
总链路图中高亮 Control Command，并标注 lateral control。
-->

---

# 今天要解决的问题

输入：

- 当前车辆位置
- 当前车辆朝向
- 当前车辆速度
- 一条参考轨迹
- 车辆轴距和转角限制

输出：

```text
目标曲率 κ
或前轮转角 δ
```

<!--
讲解备注：
先把输入输出说清楚。Pure Pursuit 不重新规划路径，也不重新决定速度，它主要输出横向控制量。

配图建议：
左侧输入列表，中间 Pure Pursuit，右侧输出 curvature / steering。
-->

---

# 横向控制在控制什么

自动驾驶控制通常分两类：

- 纵向控制：速度、加速度、刹车
- 横向控制：方向盘或前轮转角

Pure Pursuit 是横向控制算法。

它让车辆：

```text
沿参考轨迹行驶，而不是偏离轨迹。
```

<!--
讲解备注：
和第 5 集做呼应：Velocity Smoother 处理纵向速度，Pure Pursuit 处理横向转向。

配图建议：
同一辆车，纵向箭头表示速度，横向/转向箭头表示方向控制。
-->

---

# Pure Pursuit 的一句话

Pure Pursuit 的核心思想：

```text
不要盯着车辆脚下的最近点，
而是在轨迹前方找一个目标点，
让车辆沿一段圆弧追过去。
```

这个前方目标点叫：

```text
前瞻点 Lookahead Point
```

<!--
讲解备注：
这句话是整集主线。Pure Pursuit 的“Pure”不是复杂优化，而是非常朴素的追点几何。

配图建议：
车辆不看脚下最近点，而看前方一个点。
-->

---

# 为什么不能追最近点

追最近点会有两个问题：

1. 目标点太近，转向非常敏感
2. 最近点可能在车辆旁边甚至身后

结果可能是：

- 低速来回摆
- 高速振荡
- 打出不自然方向

人开车也不是盯着车头下面的白线，而是看前方一段距离。

<!--
讲解备注：
这是 Pure Pursuit 的动机。最近点适合计算误差，但不适合作为直接追踪目标。

配图建议：
最近点在车侧方，前瞻点在轨迹前方；比较两条转向弧线。
-->

---

# 本集主线

```text
车辆坐标系
  -> 追一个前瞻点
  -> 圆弧几何推导曲率
  -> 自行车模型转成前轮转角
  -> 离散轨迹上找前瞻点
  -> 动态前瞻距离
  -> 工程降级和预测轨迹
```

<!--
讲解备注：
先给推导路线。前半段是几何，后半段是工程实现。

配图建议：
横向流程图，每个节点配小图标。
-->

---

# 最小模型

先忽略复杂轨迹。

只考虑：

```text
当前车辆位姿 + 前方一个目标点
```

问题：

```text
如何求一条从车辆当前位置到目标点的圆弧？
```

求出圆弧，就能得到曲率。

<!--
讲解备注：
把问题降到最小。Pure Pursuit 的核心公式只需要一个目标点。

配图建议：
车辆原点、前方目标点、连接圆弧。
-->

---

# 为什么假设圆弧

低速或中等速度下，车辆可以近似为自行车模型。

当前前轮转角固定时：

```text
车辆会沿近似圆弧运动。
```

所以 Pure Pursuit 假设：

```text
当前控制周期内，车辆沿一段圆弧追向前瞻点。
```

<!--
讲解备注：
这不是说真实车辆永远走完这整段圆弧，而是在当前时刻用圆弧近似下一步转向趋势。

配图建议：
自行车模型车辆沿固定转角形成圆弧。
-->

---

# 建立车辆坐标系

约定：

- 车辆当前位置是原点 $(0,0)$
- 车辆当前朝向是 $x$ 轴正方向
- 车辆左侧是 $y$ 轴正方向
- 目标点为 $(x,y)$

如果：

```text
y > 0 目标在左侧
y < 0 目标在右侧
y ≈ 0 目标在正前方
```

<!--
讲解备注：
所有核心公式都在车辆坐标系下推导。先把坐标系画清楚。

配图建议：
车辆在原点，x 轴向前，y 轴向左，目标点在左前方。
-->

---

# 圆心在哪里

车辆当前朝向是 $x$ 轴。

圆弧在车辆当前位置的切线必须与车头方向一致。

圆的半径垂直于切线。

所以圆心一定在 $y$ 轴上：

$$
O=(0,R)
$$

其中 $R$ 是带符号半径。

<!--
讲解备注：
这是推导中最关键的几何直觉。车头方向是圆弧切线，圆心在切线法向上。

配图建议：
车辆点、x 轴切线、y 轴上的圆心 O、半径 R。
-->

---

# 目标点在圆上

设目标点：

$$
P=(x,y)
$$

圆心：

$$
O=(0,R)
$$

目标点在圆上，所以：

$$
(x-0)^2+(y-R)^2=R^2
$$

<!--
讲解备注：
这一步只是圆的定义：圆上点到圆心距离等于半径。

配图建议：
圆心 O 到车辆原点和目标点 P 的两条半径。
-->

---

# 半径推导

展开：

$$
x^2+y^2-2Ry+R^2=R^2
$$

消去 $R^2$：

$$
x^2+y^2=2Ry
$$

得到：

$$
R=\frac{x^2+y^2}{2y}
$$

<!--
讲解备注：
这个公式就是很多人看到的 R=L²/(2y) 的来源，其中 L²=x²+y²。

配图建议：
公式推导旁边保留圆弧几何图，帮助观众不迷失。
-->

---

# 曲率公式

曲率是半径倒数：

$$
\kappa=\frac{1}{R}
$$

因此：

$$
\kappa=
\frac{2y}{x^2+y^2}
$$

这就是 Pure Pursuit 最核心的曲率公式。

<!--
讲解备注：
这是本集第一个必须记住的公式。x,y 都是前瞻点在车辆坐标系下的坐标。

配图建议：
曲率公式卡片，旁边画目标点左/右导致曲率正/负。
-->

---

# 如果用前瞻距离 Ld 表示

前瞻距离：

$$
L_d=\sqrt{x^2+y^2}
$$

所以：

$$
R=\frac{L_d^2}{2y}
$$

$$
\kappa=\frac{2y}{L_d^2}
$$

看得越远，$L_d^2$ 越大，同样横向偏差对应的转向越平缓。

<!--
讲解备注：
这里解释为什么公式里会出现平方。前瞻距离对控制风格影响很强。

配图建议：
同样 y，短 Ld 和长 Ld 对应不同半径。
-->

---

# 公式的直觉

$$
\kappa=\frac{2y}{x^2+y^2}
$$

含义：

- $y>0$：向左转
- $y<0$：向右转
- $y\approx0$：接近直行
- 目标点越远：曲率越小
- 横向偏差越大：曲率越大

<!--
讲解备注：
不要让公式停留在数学层面，要解释每个量如何影响车辆转向。

配图建议：
三个目标点：正前方、左前方、右前方，对应不同曲率。
-->

---

# 从曲率到前轮转角

车辆不能直接执行曲率。

使用简化自行车模型：

$$
\kappa=\frac{\tan\delta}{L}
$$

其中：

- $\kappa$ 是曲率
- $\delta$ 是前轮转角
- $L$ 是轴距

所以：

$$
\delta=\arctan(L\kappa)
$$

<!--
讲解备注：
这是 Pure Pursuit 从几何曲率到车辆控制命令的桥梁。

配图建议：
自行车模型，标出轴距 L、转角 delta、转弯半径 R。
-->

---

# 转角还要限幅

车辆有物理最大转角：

$$
\delta_{\max}
$$

所以输出通常要：

$$
\delta_{\mathrm{cmd}}
=
\operatorname{clamp}
(\arctan(L\kappa),
-\delta_{\max},
\delta_{\max})
$$

几何想转得再急，也不能超过车辆能力。

<!--
讲解备注：
这和规划里的车辆模型一致。控制器输出必须符合执行器能力。

配图建议：
方向盘转角条，有上下限。
-->

---

# 最小版 Pure Pursuit

到这里，最小版算法已经成立：

1. 把目标点转换到车辆坐标系
2. 得到 $(x,y)$
3. 计算：

$$
\kappa=\frac{2y}{x^2+y^2}
$$

4. 转为前轮转角：

$$
\delta=\arctan(L\kappa)
$$

5. 限制最大转角并输出

<!--
讲解备注：
这一页对几何推导做第一次收束。下一步进入目标点从哪里来。

配图建议：
五步流程图。
-->

---

# 关键问题：目标点从哪里来

理论上：

```text
参考路径是一条连续曲线
前瞻圆和路径求交
```

工程里：

```text
参考轨迹是一串离散点
P0, P1, P2, ..., Pn
```

所以要做：

```text
搜索 + 插值
```

<!--
讲解备注：
这是从理论到工程的转折点。前瞻点不是直接给定的，要从离散轨迹中找。

配图建议：
连续曲线和离散点列对比，前瞻圆与曲线相交。
-->

---

# 最近点搜索

第一步：找到车辆当前对应的最近轨迹点。

但不能只看距离。

还要看：

- 距离是否在阈值内
- 轨迹点朝向是否和车辆朝向接近

否则可能匹配到：

```text
空间上近，但方向完全相反的点。
```

<!--
讲解备注：
这和 Mission Planner 起点候选看朝向的思想类似。距离近不代表语义正确。

配图建议：
掉头或相邻车道场景，空间近但方向相反的轨迹点被排除。
-->

---

# 从最近点向前找前瞻点

从最近点开始沿轨迹向前搜索。

对每个轨迹点：

$$
d_i=\|P_i-P_{\mathrm{vehicle}}\|
$$

找到第一个：

$$
d_i>L_d
$$

它说明：

```text
真正的前瞻圆交点在它和前一个点之间。
```

<!--
讲解备注：
直接用第一个超过 Ld 的点会有采样跳变，所以还要插值。

配图建议：
前瞻圆，轨迹点 A 在圆内，B 在圆外，交点在 AB 之间。
-->

---

# 线段与前瞻圆求交

已知：

- 车辆位置 $P$
- 前瞻半径 $L_d$
- 轨迹线段 $A\rightarrow B$

求：

```text
线段 AB 与前瞻圆的交点 T
```

这一步让目标点不受离散采样点跳变影响。

<!--
讲解备注：
这个插值步骤非常重要。它把离散轨迹变成连续前瞻目标点。

配图建议：
圆心 P、半径 Ld、线段 AB、交点 T。
-->

---

# 前瞻点插值几何

线段方向：

$$
\mathbf{a}=B-A
$$

单位方向：

$$
\mathbf{u}=\frac{\mathbf{a}}{\|\mathbf{a}\|}
$$

车辆到线段垂足：

$$
H
$$

横向距离：

$$
h=\operatorname{dist}(P,AB)
$$

<!--
讲解备注：
这页先定义几何量。下一页用直角三角形求交点距离。

配图建议：
P 到 AB 的垂线，垂足 H，交点 T 在线段前方。
-->

---

# 交点距离

如果：

$$
|h|>L_d
$$

圆和线段没有交点。

如果：

$$
|h|\le L_d
$$

由直角三角形：

$$
s^2+h^2=L_d^2
$$

所以：

$$
s=\sqrt{L_d^2-h^2}
$$

目标点：

$$
T=H+s\mathbf{u}
$$

<!--
讲解备注：
沿轨迹前进方向取前方交点，避免取到后方交点。

配图建议：
直角三角形 H-P-T，标出 h、s、Ld。
-->

---

# 横向误差的几何意义

设：

$$
\mathbf{a}=B-A
$$

$$
\mathbf{b}=P-A
$$

二维叉积：

$$
\mathbf{a}\times\mathbf{b}
=
a_xb_y-a_yb_x
$$

横向误差：

$$
h=\frac{\mathbf{a}\times\mathbf{b}}{\|\mathbf{a}\|}
$$

符号表示车辆在线段哪一侧。

<!--
讲解备注：
h 既用于前瞻圆交点判断，也能用于动态前瞻距离中的横向误差项。

配图建议：
车辆在线段左侧和右侧，h 符号不同。
-->

---

# 前瞻距离决定控制风格

如果 $L_d$ 小：

- 看得近
- 响应灵敏
- 低速精度可能更好
- 高速容易振荡

如果 $L_d$ 大：

- 看得远
- 转向平滑
- 高速更稳定
- 弯道可能切弯或响应慢

<!--
讲解备注：
前瞻距离是 Pure Pursuit 最重要的参数。它本质上是稳定性和跟踪精度之间的折中。

配图建议：
短前瞻距离和长前瞻距离对应的圆弧。
-->

---

# 为什么需要动态前瞻距离

固定 $L_d$ 无法同时适应：

- 低速
- 高速
- 直道
- 急弯
- 大横向偏差
- 倒车

工程中常用：

$$
L_d=
\operatorname{clamp}
(L_v+L_c+L_e,\ L_{\min},\ L_{\max})
$$

<!--
讲解备注：
动态前瞻距离是 Pure Pursuit 工程化的核心之一。不是所有场景都看同样远。

配图建议：
前瞻距离随速度和曲率变化的示意滑杆。
-->

---

# 速度项

速度越高，前瞻距离应该越大：

$$
L_v=k_v|v|
$$

原因：

```text
高速看得太近，会频繁修正方向，容易振荡。
```

看得远一点，控制更平顺。

<!--
讲解备注：
用速度绝对值是因为前进和倒车都需要考虑速度大小。

配图建议：
低速短前瞻、高速长前瞻对比。
-->

---

# 曲率项

道路越弯，前瞻距离应适当缩短：

$$
L_c=-k_c|\kappa_{\mathrm{ref}}|
$$

原因：

```text
急弯看得太远，目标点会落到弯道深处，
车辆可能切弯或转向不足。
```

缩短前瞻距离后，目标点更贴近局部弯道。

<!--
讲解备注：
这里的 kappa_ref 是参考轨迹局部曲率，不是 Pure Pursuit 输出曲率。

配图建议：
急弯中长前瞻目标点落得太远，短前瞻更贴局部弯道。
-->

---

# 横向误差项

车辆偏离轨迹较大时，可以增大前瞻距离：

$$
L_e=k_e|e|
$$

通常只在：

$$
|e|\ge e_{th}
$$

时启用。

作用：

```text
避免车辆用过大转角猛切回轨迹，减少过冲和振荡。
```

<!--
讲解备注：
这点有点反直觉。偏差大时看远一点，回归路径更平滑。

配图建议：
偏离大时短前瞻猛切回 vs 长前瞻平滑回归。
-->

---

# 前瞻距离上下限

最终：

$$
L_d=
\operatorname{clamp}
(L_v+L_c+L_e,\ L_{\min},\ L_{\max})
$$

$L_{\min}$ 防止：

```text
看得太近导致抖动
```

$L_{\max}$ 防止：

```text
看得太远导致转向迟钝
```

<!--
讲解备注：
动态公式必须配合上下限。没有上下限，极端速度或误差会导致不合理前瞻距离。

配图建议：
前瞻距离曲线被上下界夹住。
-->

---

# 参考曲率如何估计

常见做法：

```text
在最近轨迹点前后各取一个点
用三个点估计局部圆弧曲率
```

例如：

$$
P_{\mathrm{prev}},\ P_{\mathrm{curr}},\ P_{\mathrm{next}}
$$

它主要用于：

```text
调节前瞻距离
```

最终控制曲率仍来自前瞻目标点。

<!--
讲解备注：
不要把参考曲率和输出曲率混在一起。一个用于前瞻距离，另一个用于控制命令。

配图建议：
三个轨迹点拟合局部圆弧。
-->

---

# 轨迹重采样

规划轨迹点距可能不均匀。

这会影响：

- 最近点搜索
- 曲率估计
- 前瞻点搜索
- 预测轨迹稳定性

所以常按固定弧长间隔重采样：

```text
0, ds, 2ds, 3ds, ..., S
```

<!--
讲解备注：
这和前面 Elastic Band、Velocity Smoother 都呼应。点距稳定，差分和搜索才稳定。

配图建议：
不均匀点列变成均匀点列。
-->

---

# 轨迹平滑

轨迹局部抖动会直接影响控制输出。

常见简单处理：

```text
移动平均
```

对第 $i$ 个点：

```text
p'_i = mean(p_{i-M}, ..., p_i, ..., p_{i+M})
```

平滑不是改变整体路径，而是降低局部噪声。

<!--
讲解备注：
轨迹质量是调控制参数前的第一件事。轨迹很抖时，控制器很难调好。

配图建议：
锯齿轨迹经移动平均变平顺。
-->

---

# 前进和倒车要分开

前进时，目标点应在车辆前方：

$$
x>0
$$

倒车时，目标点应在车辆后方：

$$
x<0
$$

如果不区分方向，倒车时可能追到车辆前方点，导致控制异常。

<!--
讲解备注：
第 3 集 Pull Out 里有倒车子路径，这里说明控制器也要理解前进/倒车语义。

配图建议：
前进时前方前瞻点，倒车时后方前瞻点。
-->

---

# 求解可能失败

Pure Pursuit 不是每一帧都一定成功。

失败原因包括：

- 轨迹点太少
- 找不到最近点
- 找不到前瞻点
- 前瞻圆和轨迹线段无交点
- 车辆偏离轨迹过远

控制器不能因此输出随机方向。

<!--
讲解备注：
工程里失败是正常情况，必须设计降级策略。

配图建议：
前瞻圆和轨迹无交点，求解失败图。
-->

---

# 失败回退策略

常见策略：

1. 当前周期成功：输出新转角并缓存
2. 当前周期失败但有上一帧有效指令：沿用上一帧
3. 刚启动且没有历史指令：输出 0 曲率

目标：

```text
避免方向盘突然跳变。
```

<!--
讲解备注：
这是控制系统的稳健性。不要让单帧几何失败变成执行器突变。

配图建议：
当前失败 -> 使用上一帧方向盘角的流程图。
-->

---

# 转向收敛判断

目标转角：

$$
\delta_{\mathrm{cmd}}
$$

实际转角：

$$
\delta_{\mathrm{actual}}
$$

如果：

$$
|\delta_{\mathrm{cmd}}-\delta_{\mathrm{actual}}|
<
\delta_{\mathrm{threshold}}
$$

则认为转向已经收敛。

<!--
讲解备注：
车辆执行器有延迟，指令发出不代表车轮马上达到目标角度。这个标志可给行为或纵向控制使用。

配图建议：
目标转角和实际转角随时间收敛的曲线。
-->

---

# 为什么要预测轨迹

Pure Pursuit 除了输出当前转角，还常生成预测轨迹。

预测轨迹回答：

```text
如果车辆继续按当前控制逻辑走，
未来一小段大概会怎么走？
```

用途：

- 可视化
- 调试前瞻距离
- 检查短期运动趋势
- 辅助其他模块理解控制行为

<!--
讲解备注：
预测轨迹不是新的规划结果，而是控制器行为的短期外推。

配图建议：
当前车辆前方画一条预测弧线。
-->

---

# 预测轨迹如何生成

滚动外推：

1. 从当前真实位姿开始
2. 运行 Pure Pursuit 得到曲率
3. 转成前轮转角
4. 用运动学模型前进一步
5. 在新位置重复以上过程

每一步都重新找前瞻点。

<!--
讲解备注：
这是一种闭环滚动预测，不是只用当前曲率画一条固定圆弧。

配图建议：
多步外推，每一步都有新的前瞻点和小运动段。
-->

---

# 单步运动学外推

曲率：

$$
\kappa=\frac{\tan\delta}{L}
$$

小段距离：

$$
ds
$$

航向变化：

$$
\Delta\psi=\kappa ds
$$

短距离近似：

$$
x_{\mathrm{next}}=x+ds\cos\psi
$$

$$
y_{\mathrm{next}}=y+ds\sin\psi
$$

$$
\psi_{\mathrm{next}}=\psi+\Delta\psi
$$

<!--
讲解备注：
这和第 3 集 Hybrid A* 的运动原语以及后面 MPC 的自行车模型是同一类车辆运动学思想。

配图建议：
当前位姿沿一小段距离推进到下一位姿。
-->

---

# 速度在 Pure Pursuit 中的作用

速度主要有两个作用：

1. 影响动态前瞻距离
2. 作为预测轨迹点的速度属性

实际控制时：

```text
前瞻距离通常应使用当前车辆实际速度。
```

因为当前速度决定车辆此刻转向响应。

<!--
讲解备注：
速度不是 Pure Pursuit 的输出核心，但会影响前瞻距离。不要把速度规划和横向控制混淆。

配图建议：
速度输入影响 Ld，Ld 影响曲率。
-->

---

# 完整工程流程

```text
接收车辆状态和参考轨迹
  -> 重采样 / 平滑轨迹
  -> 最近点搜索
  -> 计算横向误差和参考曲率
  -> 动态前瞻距离
  -> 搜索并插值前瞻点
  -> 变换到车辆坐标系
  -> 曲率 κ = 2y/(x²+y²)
  -> 转角 δ = atan(Lκ)
  -> 限幅 / 回退 / 预测轨迹
```

<!--
讲解备注：
这是 Pure Pursuit 从理论到工程的完整控制周期。建议慢讲，串回前面所有页面。

配图建议：
长流程图，分成“轨迹预处理、目标点寻找、几何控制、工程保护”四段。
-->

---

# 参数调节顺序

建议不要一上来乱调所有参数。

顺序：

1. 先确认轨迹质量
2. 调基础最小前瞻距离
3. 调速度项
4. 调曲率项
5. 调横向误差项

<!--
讲解备注：
轨迹质量不好，控制参数很难调。先排查输入，再调控制器。

配图建议：
五级调参阶梯。
-->

---

# 典型现象和排查

| 现象 | 优先检查 |
|---|---|
| 低速左右摆 | 最小前瞻距离太小、轨迹噪声 |
| 高速振荡 | 速度项太小、前瞻距离太短 |
| 弯道切弯 | 前瞻距离太长、曲率项不足 |
| 转向迟钝 | 前瞻距离太大 |
| 求解偶发失败 | 最近点/前瞻点搜索、轨迹点质量 |
| 倒车异常 | 前进倒车目标点方向 |

<!--
讲解备注：
这一页很实用。让观众知道问题出现时先查哪里。

配图建议：
表格即可。
-->

---

# Pure Pursuit 的优点

- 几何意义清晰
- 推导简单
- 计算量小
- 实时性好
- 参数相对少
- 对平滑轨迹和中高速道路表现稳定

这也是它长期流行的原因。

<!--
讲解备注：
Pure Pursuit 是很好的入门控制算法，也是工程里很实用的 baseline。

配图建议：
六个优点卡片。
-->

---

# Pure Pursuit 的局限

- 不直接优化控制量
- 不显式考虑动力学和轮胎侧偏
- 对前瞻距离敏感
- 不直接约束目标点处的姿态
- 低速大曲率或狭窄空间可能不足
- 轨迹噪声会直接影响转向

复杂场景可能需要 MPC 等更强方法。

<!--
讲解备注：
这页自然引出下一集 MPC。Pure Pursuit 好用，但不是万能控制器。

配图建议：
能力边界图：简单道路表现好，复杂约束场景需要 MPC。
-->

---

# Pure Pursuit 做什么

它做：

- 从参考轨迹中找前瞻点
- 用圆弧几何计算目标曲率
- 用自行车模型转换成转角
- 用动态前瞻距离适应速度和道路形状
- 通过回退和预测轨迹增强工程稳定性

<!--
讲解备注：
正面总结职责。

配图建议：
Pure Pursuit 中心方块，周围五个职责标签。
-->

---

# Pure Pursuit 不做什么

它不做：

- 不重新规划路径
- 不重新设计速度曲线
- 不优化未来一串控制输入
- 不显式处理转角变化率约束
- 不保证复杂动态系统最优

它解决的是：

```text
当前时刻如何用几何方法算出横向转向。
```

<!--
讲解备注：
职责边界要说清楚。Pure Pursuit 是控制器，不是规划器。

配图建议：
做的事 / 不做的事两列表。
-->

---

# 本集总结图

```text
Reference trajectory
  -> nearest point
  -> dynamic lookahead distance
  -> lookahead point interpolation
  -> target point in vehicle frame (x,y)
  -> curvature κ = 2y/(x²+y²)
  -> steering δ = atan(Lκ)
  -> clamp / fallback / prediction
```

<!--
讲解备注：
这一页把整集主线收束成一张图。它是 Pure Pursuit 的工程闭环。

配图建议：
流程图中嵌入小几何图。
-->

---

# 三个必须记住的公式

Pure Pursuit 曲率：

$$
\kappa=\frac{2y}{x^2+y^2}
$$

自行车模型：

$$
\kappa=\frac{\tan\delta}{L}
$$

转角：

$$
\delta=\arctan(L\kappa)
$$

<!--
讲解备注：
如果只能记三个公式，就记这三个。第一公式来自圆弧几何，后两个来自车辆模型。

配图建议：
三个公式卡片。
-->

---

# 常见误区

1. Pure Pursuit 是追最近点
2. 前瞻距离越小越准
3. 曲率就是方向盘角
4. 轨迹点可以不重采样不平滑
5. 前进和倒车可以用同一套目标点方向
6. 求解失败时可以随便输出 0
7. Pure Pursuit 能替代所有横向控制器

<!--
讲解备注：
逐条澄清。尤其强调前瞻距离不是越小越好，曲率需要经过车辆模型变成转角。

配图建议：
误区 / 正确理解两列表。
-->

---

# 课后思考

1. 为什么 Pure Pursuit 不追最近点？
2. 为什么圆心一定在车辆坐标系的 y 轴上？
3. $\kappa=2y/(x^2+y^2)$ 中，$y$ 和 $L_d$ 分别影响什么？
4. 为什么前瞻距离要随速度和曲率变化？
5. 轨迹点离散时，为什么要做前瞻点插值？

<!--
讲解备注：
这些问题可以放到评论区，引导观众复盘核心几何。

配图建议：
纯文字页即可。
-->

---

# 下一集预告

## 横向 MPC

下一集会讲：

1. 为什么 Pure Pursuit 之后还要学 MPC
2. 阿克曼几何和自行车模型
3. Frenet 横向误差和航向误差
4. 线性化与离散化
5. 如何把未来控制问题写成 QP

<!--
讲解备注：
自然引到第 7 集。Pure Pursuit 是几何控制，MPC 是预测优化控制。

配图建议：
车辆未来预测轨迹、多个候选控制输入和 QP 优化图。
-->

---

# 下一次给 Codex 的指令

生成下一集时，直接输入：

```text
请根据 AUTOWARE规划控制层视频教程制作指南.md、
AUTOWARE规划控制层第6集_PurePursuit横向控制PPT.md、
concept/从阿克曼几何到 QP 求解器输入：自动驾驶横向 MPC 的完整数学链条.md、
concept/KinematicsBicycleModel.md 和 concept/阿克曼转向模型.md，
生成第 7 集“横向 MPC：从阿克曼几何到 QP”的 PPT 制作稿。
要求：
1. 在项目根目录生成 Markdown 格式 PPT 文件；
2. 使用 Marp 风格，以 --- 分隔每一页；
3. 每页包含页面展示内容、讲解备注、配图建议；
4. 面向新手，原理为主，代码为辅；
5. 生成完成后，在文件末尾给出生成第 8 集的指令。
```

<!--
讲解备注：
这一页不一定放进公开视频里，它是给你自己继续制作课程用的。
如果正式录课，可以删除这一页。

配图建议：
不需要配图。
-->

