---
marp: true
theme: default
paginate: true
size: 16:9
footer: "Autoware 规划与控制层教程 | 第 5 集"
---

<!--
用途说明：
这是一份第 5 集的 PPT 制作稿，采用 Marp Markdown 格式。
你可以直接按每个 --- 分隔的页面制作 PowerPoint，也可以使用 Marp 导出为 pptx。

建议导出命令：
marp AUTOWARE规划控制层第5集_VelocitySmoother速度平滑PPT.md --pptx

本集重点：
解释 Velocity Smoother 为什么不能把输入速度硬裁剪为最终速度，
而要在速度、加速度、jerk、曲率、转向角速度和停车约束下生成真正可执行的速度曲线。
-->

# Velocity Smoother

## 速度曲线为什么不能硬裁剪

```text
Geometric Trajectory + Speed Limits
        ->
Physically Feasible Velocity Profile
```

<!--
讲解备注：
本集承接第 4 集。Elastic Band 改善的是路径几何形状，但车真正怎么开，还取决于每个位置的目标速度。
Velocity Smoother 就是把上游给出的速度上限变成车辆能平滑执行的速度曲线。

配图建议：
同一条路径上用颜色表示速度：绿色高速、黄色减速、红色停车。
-->

---

# 本集在总链路中的位置

```text
Goal
  -> Route
  -> Path
  -> Trajectory      <- 本集重点
       -> Velocity Smoother
  -> Control Command
```

Velocity Smoother 解决的是：

```text
同一条几何轨迹，车辆应该以什么速度通过每个点？
```

<!--
讲解备注：
先把本集放回总链路。路径回答“走哪里”，速度平滑回答“怎么快慢变化”。

配图建议：
总链路图中高亮 Trajectory / Velocity Smoother。
-->

---

# 今天要解决的问题

上游已经给出一条轨迹：

- 有位置
- 有朝向
- 有参考速度或速度上限
- 可能有停车点

但这些速度不一定满足：

- 当前车速能否连续接上
- 加速度是否超限
- jerk 是否突兀
- 弯道是否太快
- 停车点能否停住

<!--
讲解备注：
强调输入轨迹里的速度字段不能简单当成最终速度。它更像“这里最多允许这么快”。

配图建议：
输入轨迹速度突然下降，旁边标注“物理上无法瞬间做到”。
-->

---

# 先纠正一个误解

错误理解：

```text
速度平滑 = v_i = min(v_i, speed_limit)
```

正确理解：

```text
速度平滑 = 在速度上限下，
生成满足速度、加速度、jerk 和横向约束的速度曲线。
```

<!--
讲解备注：
这是本集标题的核心。硬裁剪只改数值，不考虑车辆能否从当前运动状态平滑过渡到新速度。

配图建议：
左侧硬裁剪速度曲线有直角突变，右侧平滑速度曲线逐渐下降。
-->

---

# 输入速度是什么

对 Velocity Smoother 来说，输入轨迹速度更像：

```text
速度上限 v_ref(s)
```

而不是：

```text
必须照抄的最终速度
```

输出希望满足：

$$
0\le v(s)\le v_{\mathrm{ref}}(s)
$$

同时满足车辆运动约束。

<!--
讲解备注：
输入速度可能来自地图限速、行为规划、停车点、障碍物规划、弯道限制等。
Velocity Smoother 要在这些上限下面生成可执行速度。

配图建议：
上方画输入速度上限曲线，下方画输出速度曲线始终不超过它。
-->

---

# 本集主线

```text
输入轨迹速度上限
  -> 外部限速 / 停车点 / 弯道 / 转向限制
  -> 局部截取和重采样
  -> 初始速度与初始加速度
  -> 前向 jerk filter
  -> 后向 decel filter
  -> 后处理并发布最终轨迹
```

<!--
讲解备注：
这一页是目录。Velocity Smoother 的步骤很多，但主线很清楚：先得到不能超过的速度上限，再看能不能平滑做到。

配图建议：
横向流程图，速度上限逐步变成最终速度曲线。
-->

---

# 为什么需要速度平滑

如果速度变化突兀，车辆会出现：

- 急加速
- 急刹车
- 点头
- 乘客不适
- 控制器难以跟踪
- 执行器超出能力

速度平滑的目标：

$$
\text{在给定路径上生成满足约束的速度曲线}
$$

<!--
讲解备注：
用乘坐体验和执行器能力来建立动机，不要一上来就讲公式。

配图建议：
一条突变速度曲线对应车辆点头，一条平滑曲线对应平顺驾驶。
-->

---

# 三个纵向运动量

速度：

$$
v(t)=\frac{dx}{dt}
$$

加速度：

$$
a(t)=\frac{dv}{dt}
$$

Jerk：

$$
j(t)=\frac{da}{dt}
$$

Jerk 描述的是：

```text
加速度变化得有多快。
```

<!--
讲解备注：
位置、速度、加速度、jerk 是一层层导数关系。新手需要先把这几个量分清。

配图建议：
四层阶梯：position -> velocity -> acceleration -> jerk。
-->

---

# 为什么 jerk 很重要

只限制加速度还不够。

如果加速度从：

```text
0 瞬间跳到 -2.5 m/s²
```

即使减速度不算极端，乘客也会感觉突兀。

限制 jerk 可以让：

- 力变化更柔和
- 车辆点头减少
- 执行器压力更小
- 自动驾驶更像“老司机”

<!--
讲解备注：
Jerk 的体感非常强。可以用电梯、刹车点头、急加速被推背来类比。

配图建议：
无 jerk 约束的矩形加速度曲线 vs 有 jerk 约束的梯形/S 型加速度曲线。
-->

---

# 基本约束

速度约束：

$$
0\le v\le v_{\max}
$$

加速度约束：

$$
a_{\min}\le a\le a_{\max}
$$

jerk 约束：

$$
j_{\min}\le j\le j_{\max}
$$

速度规划要在这些约束下生成 $v(s)$。

<!--
讲解备注：
这是本集基础约束。a_min 通常是负值，表示最大允许减速度；j_min 通常也是负值。

配图建议：
v/a/j 三条曲线旁分别画上下限带。
-->

---

# 常 jerk 区间公式

在一段 jerk 为常数的区间内：

$$
a(t)=a_0+jt
$$

$$
v(t)=v_0+a_0t+\frac{1}{2}jt^2
$$

$$
x(t)=x_0+v_0t+\frac{1}{2}a_0t^2+\frac{1}{6}jt^3
$$

解析 jerk 减速的所有分段，本质上都来自这三个积分公式。

<!--
讲解备注：
这是第 5 集后半段最重要的基础公式。先让观众知道：后面三种减速曲线都是这个公式分段拼起来的。

配图建议：
jerk 常数 -> acceleration 线性 -> velocity 二次 -> position 三次。
-->

---

# 为什么沿弧长 s 建模

轨迹点天然按空间排列。

速度规划常把速度写成：

$$
v=v(s)
$$

其中 $s$ 是沿轨迹的弧长。

这样可以直接表达：

- 前方多少米有限速
- 前方多少米有停车点
- 弯道在哪个空间位置

<!--
讲解备注：
车辆实际随时间运动，但规划依附于路径点，所以空间域很自然。

配图建议：
路径上标出 s 坐标和每个点的速度。
-->

---

# 速度平方变量 b

加速度：

$$
a=\frac{dv}{dt}
$$

又因为：

$$
\frac{ds}{dt}=v
$$

所以：

$$
a=v\frac{dv}{ds}
$$

令：

$$
b=v^2
$$

则：

$$
\frac{db}{ds}=2a
$$

<!--
讲解备注：
很多 QP 速度优化器喜欢用 b=v^2，是因为它让空间域动力学变成线性关系。
虽然本集主要讲 analytical smoother，但这个关系能帮助理解速度规划的数学骨架。

配图建议：
时间域 v(t) 转为空间域 v(s)，旁边写 b=v²。
-->

---

# 离散空间动力学

相邻轨迹点距离：

$$
\Delta s_i
$$

离散化：

$$
\frac{b_{i+1}-b_i}{\Delta s_i}=2a_i
$$

也就是：

$$
b_{i+1}=b_i+2a_i\Delta s_i
$$

这个关系常用于 QP smoother。

<!--
讲解备注：
这页不用展开 QP，重点是速度、加速度、空间距离之间不是随便填的，它们有物理关系。

配图建议：
两个相邻轨迹点之间标出 Δs、v_i、v_{i+1}、a_i。
-->

---

# Velocity Smoother 的输入输出

输入包括：

- 上游轨迹
- 当前车辆速度
- 当前加速度
- 上一帧输出轨迹
- 外部限速
- operation mode

输出：

```text
最终发布给下游控制器的 trajectory
```

其中速度和加速度已经经过平滑处理。

<!--
讲解备注：
它是“最后速度整形层”。不重新决定几何路线，而是重写速度字段。

配图建议：
左侧多个输入箭头进入 Velocity Smoother，右侧输出 final trajectory。
-->

---

# 主数据流

```text
收到输入轨迹
  -> 取 odom / acceleration / external velocity limit
  -> 删除重叠点
  -> 末点速度设 0
  -> 在上一帧轨迹上投影 ego
  -> 计算外部限速插入位置
  -> 倒车时速度符号处理
  -> calcTrajectoryVelocity()
  -> post resampling
  -> 发布最终轨迹
```

<!--
讲解备注：
这里可以少量点源码名，但不逐行读代码。主线是数据如何进入、加工、发布。

配图建议：
流程图，突出“上一帧轨迹投影”和“外部限速插入”两个关键点。
-->

---

# 局部轨迹截取

速度规划不需要每次处理整条路径。

通常保留：

$$
s\in
[s_0-L_{\mathrm{behind}},\ s_0+L_{\mathrm{ahead}}]
$$

后方一小段用于连续性。

前方较长一段用于：

- 加速
- 减速
- 停车
- 过弯

<!--
讲解备注：
局部截取是实时性的基础。只规划真正相关的未来。

配图建议：
长轨迹中截取自车后方短段和前方长段。
-->

---

# 外部限速不能硬切

外部限速可能突然从：

```text
10 m/s -> 3 m/s
```

错误做法：

```text
从当前位置开始全部 clamp 到 3 m/s
```

问题：

```text
车辆无法瞬间降速，控制器会看到突兀目标。
```

<!--
讲解备注：
这是本集标题最核心的例子。限速是一个数值，但要转化成空间上的限速位置。

配图建议：
速度上限突然阶跃下降，硬切速度曲线出现垂直断崖。
-->

---

# 限速要变成空间位置

正确思路：

```text
计算在 acc / jerk 约束下，
从当前状态降到新限速需要多少距离。
```

当前状态：

$$
v_0,\quad a_0
$$

目标速度：

$$
v_{\mathrm{target}}=v_{\mathrm{limit}}
$$

插入距离：

$$
d_{\mathrm{insert}}
=
d_{\mathrm{decel}}+d_{\mathrm{margin}}
$$

<!--
讲解备注：
从当前位置到 d_insert 之前，速度上限不能粗暴变成新限速；从插入点后才应用新限速。

配图建议：
当前点前方 d_insert 处插入限速点，从该点后速度上限降低。
-->

---

# 当前加速度也重要

如果当前：

$$
a_0>0
$$

车辆即使立刻施加负 jerk，也会继续加速一小段。

未来可能达到峰值速度：

$$
v_{\mathrm{peak}}
=
v_0-\frac{1}{2}\frac{a_0^2}{j_{\min}}
$$

因为：

$$
j_{\min}<0
$$

所以这部分是正的速度增量。

<!--
讲解备注：
这解释了为什么只看当前速度还不够。当前加速度决定接下来速度是否还会继续上升。

配图建议：
当前正在加速，负 jerk 后加速度逐渐降到 0，速度仍先上升到 peak。
-->

---

# 停车接近速度

停车点速度为：

$$
v=0
$$

为了最后几米更稳定，可以在停车点前一段限制接近速度：

$$
s\in
[s_{\mathrm{stop}}-d_{\mathrm{approach}},\ s_{\mathrm{stop}}]
$$

限制：

$$
v(s)\le v_{\mathrm{approach}}
$$

<!--
讲解备注：
车辆不应该高速冲到停车点附近再急刹。低速接近能提高停车位置控制稳定性。

配图建议：
停车点前一段区域标红，速度上限降低到 approach speed。
-->

---

# 曲率限速

过弯时横向加速度：

$$
a_{\mathrm{lat}}
=
v^2|\kappa|
$$

要求：

$$
v^2|\kappa|
\le
a_{\mathrm{lat,max}}
$$

得到：

$$
v
\le
\sqrt{
\frac{a_{\mathrm{lat,max}}}{|\kappa|}
}
$$

弯越急，允许速度越低。

<!--
讲解备注：
这是速度规划和路径几何的连接点。纵向速度规划不能只看纵向，还要看路径曲率。

配图建议：
直线、大半径弯、小半径急弯，对应不同限速。
-->

---

# 曲率限速的工程细节

不能只在曲率最大点限速。

车辆需要：

```text
入弯前提前减速
弯中保持稳定
出弯后再加速
```

因此会使用弯道前后窗口：

$$
[s_i-d_{\mathrm{before}},\ s_i+d_{\mathrm{after}}]
$$

并避免曲率噪声把速度压得过低。

<!--
讲解备注：
速度曲线要符合驾驶习惯，不应该在弯道点上突然下压再立刻升高。

配图建议：
弯道前后高亮一个限速影响区间。
-->

---

# 转向角速度限速

自行车模型：

$$
\delta=\arctan(L\kappa)
$$

转向角速度：

$$
\dot{\delta}
=
\frac{d\delta}{dt}
=
v\frac{d\delta}{ds}
$$

如果：

$$
|\dot{\delta}|\le\dot{\delta}_{\max}
$$

则：

$$
v\le
\frac{\dot{\delta}_{\max}}
{\left|\frac{d\delta}{ds}\right|}
$$

<!--
讲解备注：
横向加速度限制看“弯有多急”，转向角速度限制看“曲率变化有多快”。

配图建议：
S 弯或曲率突变路径，方向盘角需要快速变化。
-->

---

# 两类横向限制互补

横向加速度限制：

$$
a_y=v^2\kappa
$$

关注：

```text
弯道本身有多急
```

转向角速度限制：

$$
\dot{\delta}=v\frac{d\delta}{ds}
$$

关注：

```text
曲率变化有多快
```

<!--
讲解备注：
大半径长弯可能横向加速度主导；S 弯或曲率突变可能转向角速度主导。

配图建议：
左边长弯，右边 S 弯，对应两种限制。
-->

---

# 重采样为什么重要

速度规划里常用差分：

$$
\frac{\Delta \delta}{\Delta s}
$$

$$
\frac{\Delta a}{\Delta s}
$$

如果点距忽大忽小：

```text
差分会很不稳定。
```

所以速度平滑前后都常做重采样。

<!--
讲解备注：
重采样不是改变道路几何意图，而是让计算点距更适合速度规划和下游控制。

配图建议：
点距不均匀路径和重采样后均匀路径对比。
-->

---

# 初始状态从哪里来

理想情况：

```text
从上一帧已发布轨迹在 ego 当前位置处继续
```

即：

$$
v_0=v_{\mathrm{prev}}(s_{\mathrm{ego}})
$$

$$
a_0=a_{\mathrm{prev}}(s_{\mathrm{ego}})
$$

这样相邻规划帧之间速度和加速度更连续。

<!--
讲解备注：
不要每一帧都简单用 odom 速度当初始值，否则传感器噪声可能造成规划速度跳变。

配图建议：
上一帧速度曲线在当前 ego 位置投影，得到本帧初始状态。
-->

---

# 什么时候不用上一帧

应改用当前车辆状态或特殊起步状态：

- 首次规划，没有上一帧
- 实际速度和上一帧期望速度偏差过大
- 刚进入自动驾驶 engage
- 旧轨迹已经不可信
- 停车点就在很近处

目标：

```text
既要跨帧连续，也不能死抱错误历史。
```

<!--
讲解备注：
这是工程鲁棒性。上一帧能帮助连续，但如果旧状态不可信，要从当前车重新规划。

配图建议：
上一帧轨迹和当前 odom 偏差过大，切换到当前状态。
-->

---

# 倒车轨迹

许多速度平滑算法默认：

$$
v\ge0
$$

如果输入轨迹表示倒车：

```text
1. 规划前把负速度取反
2. 使用同一套正向速度平滑逻辑
3. 输出前再把速度符号翻回去
```

这样可以复用算法，同时保留倒车语义。

<!--
讲解备注：
这页补一个工程细节。倒车不是这集重点，但在 Pull Out 场景中很常见。

配图建议：
倒车轨迹速度符号先翻正，再翻回负。
-->

---

# Analytical Smoother 的思想

AnalyticalJerkConstrainedSmoother 不是通用 QP。

它更像：

```text
解析运动学公式 + 前后向滤波
```

核心三步：

1. 前向 jerk filter：尽量跟随速度上限
2. 搜索减速目标：找必须认真降速的位置
3. 后向 decel filter：提前反推从哪里开始刹

<!--
讲解备注：
这页区分 Analytical smoother 和 QP smoother。第 5 集重点讲 analytical 的可解释性。

配图建议：
从当前点向前的滤波箭头，加上从停车点向后的反推箭头。
-->

---

# 前向 jerk filter

从当前状态向前积分：

```text
尽量跟随 v_ref(s)
同时限制 acc 和 jerk
```

上一点状态：

$$
v_{i-1},\quad a_{i-1}
$$

空间距离：

$$
\Delta s_i
$$

估计时间：

$$
\Delta t_i=
\frac{\Delta s_i}{\max(v_{i-1},v_{\min})}
$$

<!--
讲解备注：
前向滤波只看当前和过去，因此它可以平滑加速，但不知道前方是否需要提前减速。

配图建议：
速度曲线从当前点向前逐点生成。
-->

---

# 前向滤波如何限 jerk

先预测速度：

$$
\hat{v}_i=
\max(v_{i-1}+a_{i-1}\Delta t_i,\ 0)
$$

速度误差：

$$
e_i=v_{\mathrm{ref},i}-\hat{v}_i
$$

期望加速度：

$$
a_{\mathrm{fb},i}=k_pe_i
$$

再对加速度和 jerk 限幅。

<!--
讲解备注：
这不是完整控制器，而是用类似反馈的方式让速度尽量贴近上限，同时避免加速度和 jerk 突变。

配图建议：
参考速度和预测速度之间的误差，转成加速度，再被限幅。
-->

---

# 前向滤波的局限

前向滤波的问题：

```text
它不知道前方低速目标需要提前多远减速。
```

例如：

- 停车点
- 弯道最低限速点
- 外部限速生效点
- 路径终点

所以还需要：

```text
后向减速滤波
```

<!--
讲解备注：
这页建立前向和后向的必要配合。只前向会“看得太近”。

配图建议：
前向速度曲线到停车点前才发现需要降速，已经来不及。
-->

---

# 减速目标是什么

减速目标可以理解为速度上限曲线中的“谷底”。

常见来源：

- 停车点：$v=0$
- 弯道最低限速点
- 外部限速开始生效位置
- 轨迹终点
- 局部速度上限突然下降的位置

解析 smoother 会重点处理这些点。

<!--
讲解备注：
不是每个速度点都要后向推一次，而是找关键低速目标。

配图建议：
速度上限曲线中标出几个局部低速谷底。
-->

---

# 后向 decel filter

对某个减速目标：

目标位置：

$$
s_T
$$

目标速度：

$$
v_T
$$

候选减速起点：

$$
s_0
$$

可用距离：

$$
d_{\mathrm{allow}}=s_T-s_0
$$

计算从 $(v_0,a_0)$ 降到 $(v_T,0)$ 需要的距离 $d_{\mathrm{stop}}$。

<!--
讲解备注：
后向滤波的核心问题是：从这里开始刹，能不能在目标点前平滑达到目标速度。

配图建议：
从停车点向后画反推箭头，找减速起点。
-->

---

# 距离是否足够

如果：

$$
0\le d_{\mathrm{stop}}\le d_{\mathrm{allow}}
$$

说明：

```text
从这个起点开始减速来得及。
```

否则：

```text
需要更早开始减速，
或者使用更强的负 jerk / 更大减速度。
```

<!--
讲解备注：
这是后向滤波最核心的判定。它把“能不能刹得住”变成距离比较。

配图建议：
d_stop 小于可用距离和大于可用距离的两种情况。
-->

---

# 舒适性优先级

后向滤波通常优先尝试温和 jerk。

例如从：

$$
j=-0.1
$$

逐步到：

$$
j_{\min}=-1.5
$$

能用温和减速解决，就不用更激烈的减速。

<!--
讲解备注：
这体现了乘坐舒适性优先。只有距离不够时，才逐步增加减速强度。

配图建议：
多条减速曲线，温和曲线优先，激烈曲线作为兜底。
-->

---

# Jerk 受限减速目标

目标：

从：

$$
(v_0,a_0)
$$

到：

$$
(v_T,0)
$$

终点加速度回到 0 的原因：

```text
到达目标速度后，不希望车辆还在继续减速。
```

<!--
讲解备注：
终点速度达到 v_T 还不够，加速度也要回到 0，速度曲线才更自然。

配图建议：
速度降到目标值，同时加速度回到 0。
-->

---

# 三类减速曲线

解析 jerk 减速常分三类：

| 类型 | 加速度形状 | 适用直觉 |
|---|---|---|
| Type 1 | 梯形 | 需要明显减速，达到 $a_{\min}$ |
| Type 2 | 三角形 | 中等减速，不触及 $a_{\min}$ |
| Type 3 | 仅恢复 | 当前已在减速，只需平滑收尾 |

<!--
讲解备注：
这一页先给全局图景，后面逐个讲。

配图建议：
三张加速度曲线：梯形、三角形、单段回零。
-->

---

# Type 1：梯形加速度曲线

三个阶段：

```text
a0 -> a_min
a_min 保持一段时间
a_min -> 0
```

对应 jerk：

```text
负 jerk
0 jerk
正 jerk
```

适合：

```text
需要较大速度下降、距离较长的减速。
```

<!--
讲解备注：
Type 1 是最完整的减速形状。加速度真的到达最小加速度并保持平台。

配图建议：
加速度-时间图：下降、平台、回升。
-->

---

# Type 1 的阶段时间

负 jerk：

$$
j_{\mathrm{dec}}<0
$$

正 jerk：

$$
j_{\mathrm{acc}}>0
$$

阶段 1：

$$
t_1=
\frac{a_{\min}-a_0}{j_{\mathrm{dec}}}
$$

阶段 3：

$$
t_3=
\frac{0-a_{\min}}{j_{\mathrm{acc}}}
$$

平台时间 $t_2$ 由总速度变化决定。

<!--
讲解备注：
这一页只讲阶段时间，不展开全部距离公式。距离最终由常 jerk 积分得到。

配图建议：
在梯形加速度图上标 t1、t2、t3。
-->

---

# Type 2：三角形加速度曲线

如果不需要保持 $a_{\min}$：

```text
a0 -> 负峰值 a1 -> 0
```

没有恒定减速度平台。

适合：

```text
中等减速，降速需求没大到触及最小加速度。
```

<!--
讲解备注：
Type 2 可以理解成 Type 1 的平台时间 t2 <= 0 后的退化形式。

配图建议：
加速度-时间图呈三角形，标出负峰值 a1。
-->

---

# Type 3：仅恢复加速度

如果车辆当前已经在减速：

$$
a_0<0
$$

并且只要把加速度恢复到 0 就足够接近目标速度，则使用：

```text
正 jerk 把 a0 拉回 0
```

时间：

$$
t_1=\frac{0-a_0}{j_{\mathrm{acc}}}
$$

<!--
讲解备注：
这是最温和的一类：不再继续压低加速度，只把已有减速平滑收尾。

配图建议：
加速度从负值平滑回到 0 的单段曲线。
-->

---

# 三类曲线如何选择

先判断 Type 1：

```text
是否需要保持 a_min 平台？
```

若平台时间：

$$
t_2>0
$$

使用 Type 1。

若：

$$
t_2\le0
$$

再判断仅恢复加速度是否足够。

不够则 Type 2，足够则 Type 3。

<!--
讲解备注：
这页讲决策树。不要让观众陷进公式，先记住判断逻辑。

配图建议：
Type 1 / Type 2 / Type 3 的分支决策树。
-->

---

# 连续曲线写回离散轨迹

解析得到的是连续曲线：

$$
x(t),\quad v(t),\quad a(t),\quad j(t)
$$

但输出是离散轨迹点。

做法：

```text
按时间采样连续曲线
再按轨迹点累计距离插值 v 和 a
```

<!--
讲解备注：
速度规划最终必须回到 TrajectoryPoint 上。解析曲线只是中间工具。

配图建议：
连续速度曲线被采样并映射到轨迹点。
-->

---

# 多个减速目标如何组合

真实轨迹可能有多个低速区域：

```text
弯道 -> 外部限速 -> 停车点
```

解析 smoother 通常按目标从近到远处理：

1. 前向滤波
2. 当前目标后向减速检查
3. 写入减速曲线
4. 处理下一个目标

<!--
讲解备注：
Analytical smoother 不是全局最优优化器，而是用局部目标逐步修正速度曲线。

配图建议：
速度上限曲线上三个低速目标，逐段生成速度曲线。
-->

---

# 停车点强制后处理

停车点具有安全优先级。

最终通常强制：

$$
v(s)=0,\quad s\ge s_{\mathrm{stop}}
$$

如果停车点处规划速度仍明显大于 0：

```text
需要记录告警
```

这可能表示当前状态物理上已经来不及停住。

<!--
讲解备注：
强制归零是输出安全保护，但如果到停车点仍无法物理减到 0，应当通过诊断暴露问题。

配图建议：
停车点之后速度被强制压为 0，停车点前标出可能距离不足的警告。
-->

---

# 后方轨迹处理

车辆后方点已经经过，通常不重新优化。

但输出轨迹需要保持完整：

- 首次规划或偏差重规划：使用当前最近点状态
- 正常情况：从上一帧输出轨迹投影获取后方速度/加速度

目的：

```text
保持输出结构完整和跨帧连续。
```

<!--
讲解备注：
后方点不是未来行为核心，但对控制器输入和可视化一致性很重要。

配图建议：
自车后方点用灰色，前方点用彩色规划速度。
-->

---

# Post Resampling

内部速度规划点距不一定适合下游控制器。

因此发布前会做：

```text
post resampling
```

作用：

- 输出点距更适合控制器
- 保留停车点附近分辨率
- 插值得到速度和加速度
- 保持轨迹长度和格式稳定

<!--
讲解备注：
重采样前后都可能发生。前面是为了计算稳定，后面是为了输出格式适合下游。

配图建议：
内部稀疏/不均匀点列转成下游期望点列。
-->

---

# 其他 smoother 的直觉

除了 Analytical smoother，还有：

- JerkFiltered
- L2 Pseudo Jerk
- Linf Pseudo Jerk

直觉：

```text
Analytical：解析公式 + 前后向滤波
QP 类：把速度、加速度、pseudo jerk 写成优化问题
```

本集重点是 Analytical，因为它最适合讲“为什么不能硬裁剪”。

<!--
讲解备注：
这里不要展开太多 QP 细节，给观众一个分类即可。

配图建议：
四类 smoother 简单对比表。
-->

---

# 一个小例子

当前速度：

```text
10 m/s
```

前方限速：

```text
3 m/s
```

错误做法：

```text
当前位置立即把速度目标设成 3 m/s
```

正确做法：

```text
计算减速距离，在前方合适位置插入限速，并生成 jerk 受限减速曲线。
```

<!--
讲解备注：
用这个例子回扣标题。速度上限下降不是瞬间控制目标下降。

配图建议：
两条速度曲线对比：硬裁剪阶跃 vs 平滑下降。
-->

---

# 调参：先看什么

最常关注：

- 最大速度
- 最大加速度
- 最大减速度
- jerk 上下限
- 曲率限速参数
- 转向角速度限制
- 重采样间距
- 停车接近速度

调参不是只追求快，而是在：

```text
效率 / 舒适 / 可控 / 安全
```

之间折中。

<!--
讲解备注：
提醒观众速度规划调参要看现象，不要盲目调大速度或减速度。

配图建议：
四角平衡图：效率、舒适、可控、安全。
-->

---

# 典型现象和排查

| 现象 | 优先检查 |
|---|---|
| 急刹突兀 | jerk / decel 限制、停车点距离 |
| 弯道太快 | 曲率限速、横向加速度上限 |
| 方向盘变化太急 | 转向角速度限制、路径曲率噪声 |
| 速度跨帧跳变 | 初始状态、上一帧投影 |
| 停车点停不准 | approach speed、停车后处理、采样 |
| 计算慢 | 重采样点数、优化范围 |

<!--
讲解备注：
这页非常实用。把故障现象和可能模块连接起来。

配图建议：
问题 -> 排查项的表格即可。
-->

---

# Velocity Smoother 做什么

它做：

- 把输入速度理解为速度上限
- 叠加外部限速、停车点、曲率和转向限制
- 处理当前状态和上一帧连续性
- 生成满足 acc / jerk 的速度曲线
- 做停车点、后方轨迹、最大速度和重采样后处理

<!--
讲解备注：
正面总结职责。

配图建议：
Velocity Smoother 中心方块，周围五个职责标签。
-->

---

# Velocity Smoother 不做什么

它不做：

- 不重新决定 route
- 不重新设计路径几何
- 不负责横向避障
- 不直接输出油门刹车命令
- 不保证距离不足时一定能物理停住

它解决的是：

```text
在已有轨迹上生成更物理可行、更舒适的速度曲线。
```

<!--
讲解备注：
职责边界。速度平滑不是纵向控制器，也不是行为规划器。

配图建议：
做的事 / 不做的事两列表。
-->

---

# 本集总结图

```text
Input trajectory velocity = speed upper bound
  -> external limit / stop / curve / steering filters
  -> local crop + resampling
  -> initial v0, a0
  -> forward jerk filter
  -> decel targets
  -> backward decel filter
  -> stop / max speed / post resampling
  -> final trajectory
```

<!--
讲解备注：
这一页把整集主线收束成一张图。建议慢讲并回到“不能硬裁剪”的主题。

配图建议：
长流程图，速度曲线在每步逐渐变得更可执行。
-->

---

# 三个必须记住的公式

Jerk：

$$
j=\frac{da}{dt}
$$

曲率限速：

$$
v\le
\sqrt{
\frac{a_{\mathrm{lat,max}}}{|\kappa|}
}
$$

常 jerk 积分：

$$
v(t)=v_0+a_0t+\frac{1}{2}jt^2
$$

<!--
讲解备注：
如果只能记三个公式，就记这三个。它们分别对应舒适性、弯道速度、解析减速。

配图建议：
三个公式卡片。
-->

---

# 常见误区

1. 输入轨迹速度就是最终速度
2. 速度平滑就是低通滤波
3. 外部限速可以从当前点立刻硬切
4. 只限制速度和加速度就够了
5. 曲率限速只影响横向控制
6. Analytical smoother 是 QP
7. 停车点速度不为 0 也可以交给控制器自己处理

<!--
讲解备注：
逐条澄清。特别强调输入速度是上限，Analytical smoother 不是 QP。

配图建议：
误区 / 正确理解两列表。
-->

---

# 课后思考

1. 为什么输入速度更适合理解成速度上限？
2. 为什么外部限速不能从当前位置硬切？
3. jerk 和乘坐舒适性有什么关系？
4. 为什么弯道速度和路径曲率有关？
5. 前向 jerk filter 和后向 decel filter 分别解决什么问题？

<!--
讲解备注：
这些问题可以放到评论区，引导观众复盘本集关键概念。

配图建议：
纯文字页即可。
-->

---

# 下一集预告

## Pure Pursuit 横向控制

下一集会讲：

1. 为什么不能追最近点
2. 前瞻点如何决定转向
3. 圆弧几何如何推出曲率
4. 曲率如何通过自行车模型变成前轮转角
5. 前瞻距离如何影响稳定性和跟踪精度

<!--
讲解备注：
自然引到控制层。现在轨迹已经有路径和速度，下一步就是控制器如何跟踪它。

配图建议：
车辆、前瞻圆、前瞻点、圆弧曲率示意图。
-->

---

# 下一次给 Codex 的指令

生成下一集时，直接输入：

```text
请根据 AUTOWARE规划控制层视频教程制作指南.md、
AUTOWARE规划控制层第5集_VelocitySmoother速度平滑PPT.md、
concept/pure_pursuit_blog.md、
concept/pure pursuit 曲率计算.md 和 concept/KinematicsBicycleModel.md，
生成第 6 集“Pure Pursuit：用几何直觉理解横向控制”的 PPT 制作稿。
要求：
1. 在项目根目录生成 Markdown 格式 PPT 文件；
2. 使用 Marp 风格，以 --- 分隔每一页；
3. 每页包含页面展示内容、讲解备注、配图建议；
4. 面向新手，原理为主，代码为辅；
5. 生成完成后，在文件末尾给出生成第 7 集的指令。
```

<!--
讲解备注：
这一页不一定放进公开视频里，它是给你自己继续制作课程用的。
如果正式录课，可以删除这一页。

配图建议：
不需要配图。
-->

