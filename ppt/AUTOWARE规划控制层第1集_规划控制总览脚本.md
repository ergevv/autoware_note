# 第 1 集：Autoware 规划控制总览脚本

本文件是一份可以直接拿去做 PPT 和录制的第 1 集脚本。它对应总指南中的第一集，目标是让新手先建立完整地图，再进入 Mission Planner、Hybrid A*、Velocity Smoother、Pure Pursuit、MPC、MPT 等具体算法。

建议视频标题：

```text
Autoware 规划控制总览：从目标点到方向盘控制命令
```

建议时长：

```text
30 到 40 分钟
```

本集定位：

```text
不讲复杂源码，不推长公式，先帮观众建立“规划控制链路”的整体框架。
```

---

# 1. 本集目标

这一集结束后，观众应该能回答：

1. Autoware 规划控制层为什么要分这么多模块。
2. Route、Path、Trajectory、Control Command 分别是什么。
3. Mission Planner、Behavior Planner、Motion Planner、Velocity Smoother、Controller 大致负责什么。
4. 为什么不能从目标点直接生成方向盘角。
5. 后续课程每一集会解决链路上的哪个问题。

本集不要让观众记住太多包名。第一集最重要的是建立主线：

```text
目标点 -> 路线 -> 路径 -> 轨迹 -> 控制命令
```

---

# 2. 本集一句话

本集最核心的一句话是：

```text
Autoware 的规划控制层，本质上是在把“我要去哪里”一步步变成“车辆此刻应该怎么打方向、怎么加减速”。
```

你可以在开头、中间和结尾重复这句话。

---

# 3. 本集 PPT 总览

建议 28 页左右。

| 页码 | 标题 | 预计时间 |
|---:|---|---:|
| 1 | 标题页 | 1 分钟 |
| 2 | 今天要解决的问题 | 1 分钟 |
| 3 | 一个最简单的自动驾驶任务 | 2 分钟 |
| 4 | 为什么不能直接从目标点控制车辆 | 2 分钟 |
| 5 | 规划控制总链路 | 2 分钟 |
| 6 | Route、Path、Trajectory、Control 的区别 | 3 分钟 |
| 7 | Route 是什么 | 2 分钟 |
| 8 | Path 是什么 | 2 分钟 |
| 9 | Trajectory 是什么 | 2 分钟 |
| 10 | Control Command 是什么 | 2 分钟 |
| 11 | Mission Planner 在做什么 | 3 分钟 |
| 12 | Behavior / Start Planning 在做什么 | 3 分钟 |
| 13 | Motion Planning 和路径优化在做什么 | 3 分钟 |
| 14 | Velocity Smoother 在做什么 | 3 分钟 |
| 15 | Controller 在做什么 | 3 分钟 |
| 16 | Pure Pursuit 和 MPC 的区别 | 3 分钟 |
| 17 | 为什么地图很重要 | 2 分钟 |
| 18 | 为什么车辆模型很重要 | 2 分钟 |
| 19 | 为什么优化问题会反复出现 | 3 分钟 |
| 20 | 一条轨迹如何被层层加工 | 3 分钟 |
| 21 | 新手看源码为什么容易迷路 | 2 分钟 |
| 22 | 正确学习顺序 | 2 分钟 |
| 23 | 后续课程路线 | 3 分钟 |
| 24 | 本集总结图 | 2 分钟 |
| 25 | 三句话总结 | 1 分钟 |
| 26 | 下一集预告 | 1 分钟 |
| 27 | 观众课后问题 | 1 分钟 |
| 28 | 结尾页 | 30 秒 |

---

# 4. 开场口播

你可以这样开场：

```text
大家好，这一集我们先不急着读 Autoware 源码，也不急着推 MPC 公式。

很多人学习 Autoware 规划控制的时候，第一步就是打开源码目录，然后很快就会发现包很多、消息很多、参数很多，Mission Planner、Behavior Planner、Motion Planner、Velocity Smoother、Controller 全都混在一起。

如果没有一条主线，源码越看越多，反而越看越乱。

所以这节课我们先回答一个最核心的问题：
给自动驾驶车一个目标点，它最后是怎么一步步变成方向盘、油门和刹车控制命令的？

只要这条链路建立起来，后面再看 Mission Planner、Hybrid A*、Elastic Band、Velocity Smoother、Pure Pursuit、MPC 和 MPT，就不会觉得它们是孤立算法。
```

---

# 5. 第 1 页：标题页

## 页面内容

```text
Autoware 规划控制总览
从目标点到方向盘控制命令
```

副标题：

```text
Route -> Path -> Trajectory -> Control
```

## 讲解备注

这一页不要讲太多，只要告诉观众：

```text
这一集是整套课的地图。
```

## 配图建议

画一辆车，前方有一个目标点，中间用箭头连到方向盘图标。

---

# 6. 第 2 页：今天要解决的问题

## 页面内容

```text
今天只解决一个问题：

给车辆一个目标点，Autoware 如何一步步生成可执行控制命令？
```

下面放四个词：

```text
Route
Path
Trajectory
Control
```

## 讲解备注

口播：

```text
这一集不追求把每个模块细节讲完。
我们只建立一个框架：目标点进入系统后，先变成车道级路线，再变成局部路径，再变成带速度的轨迹，最后才进入控制器。
```

---

# 7. 第 3 页：一个最简单的自动驾驶任务

## 页面内容

```text
任务：
车辆当前在 A 点，目标在 B 点。

问题：
车辆应该怎么开过去？
```

## 配图建议

画一个简单路口：

```text
A 点车辆 -> 道路网络 -> B 点目标
```

道路中间可以有：

- 车道线。
- 路口。
- 禁行区域。
- 弯道。

## 讲解备注

口播：

```text
从人类直觉看，这个任务好像很简单：车在 A 点，目标在 B 点，开过去就行。

但对自动驾驶系统来说，这句话里面隐藏了很多问题。

它不能直接画一条从 A 到 B 的直线，因为直线可能穿过隔离带，可能逆行，可能压到不可通行区域，也可能完全不符合车辆转弯能力。
```

---

# 8. 第 4 页：为什么不能直接从目标点控制车辆

## 页面内容

```text
目标点本身不够用

它没有告诉车辆：
1. 经过哪些车道
2. 在车道内走哪条线
3. 每一段开多快
4. 当前方向盘应该转多少
```

## 讲解备注

口播：

```text
目标点只告诉系统最终想去哪里，但没有告诉车辆中间应该怎么走。

自动驾驶不是瞬移，也不是点机器人。车辆有长度、有宽度、有转弯半径、有速度、有加速度限制，还要遵守道路拓扑和交通规则。

所以系统必须把一个很高层的目标，逐步翻译成越来越具体、越来越接近底盘执行的命令。
```

## 这一页结论

```text
目标点不能直接控制车辆，必须经过分层规划和控制。
```

---

# 9. 第 5 页：规划控制总链路

## 页面内容

```text
目标点
  -> Mission Planner
  -> Behavior / Motion Planning
  -> Path / Trajectory Optimization
  -> Velocity Smoother
  -> Controller
  -> Vehicle
```

也可以画成：

```text
Goal
  -> Route
  -> Path
  -> Trajectory
  -> Control Command
```

## 讲解备注

口播：

```text
这张图是整套课程最重要的一张图。

后面我们学的每一个算法，都可以放到这条链路里。

Mission Planner 负责生成路线；
行为和运动规划负责在路线约束下生成具体路径；
轨迹优化让路径更平滑、更可跟踪；
Velocity Smoother 给轨迹加上合理速度；
Controller 根据当前车和目标轨迹输出控制命令。
```

## 动画建议

逐步点亮每个模块，不要一次全出现。

---

# 10. 第 6 页：Route、Path、Trajectory、Control 的区别

## 页面内容

| 名称 | 回答的问题 | 例子 |
|---|---|---|
| Route | 经过哪些车道 | 从 1 号车道到 8 号车道 |
| Path | 在车道里走哪条线 | 一串几何路径点 |
| Trajectory | 每个点以多快速度通过 | 带速度的路径点 |
| Control | 当前怎么操作车辆 | 转向角、加速度 |

## 讲解备注

口播：

```text
新手最容易混淆这四个词。

Route 是导航层结果，它关心道路拓扑。
Path 是几何结果，它关心车辆在道路空间里的具体走线。
Trajectory 在 path 的基础上加了速度和时间意义。
Control Command 是此时此刻发给车辆的执行命令。
```

## 生活类比

```text
Route 像导航路线。
Path 像你在车道里具体压哪条线。
Trajectory 像你还知道每一段开多快。
Control 像你当前手和脚怎么操作。
```

---

# 11. 第 7 页：Route 是什么

## 页面内容

```text
Route 回答：
从当前位置到目标点，应该经过哪些车道？
```

输入：

```text
当前位姿 + 目标位姿 + Lanelet 地图
```

输出：

```text
lanelet 序列 + route sections + 可行驶车道集合
```

## 配图建议

画一个道路网络，把被选中的 lanelet 用高亮颜色标出来。

## 讲解备注

口播：

```text
Route 不是一条可以直接跟踪的曲线。

它更像一个车道级导航结果，告诉后续模块：接下来应该沿哪些车道走，哪些相邻车道可以作为规划空间。
```

## 对应后续课程

```text
后面 Mission Planner 那一集会详细讲。
```

---

# 12. 第 8 页：Path 是什么

## 页面内容

```text
Path 回答：
在 route 给定的道路空间中，车辆具体沿哪条几何线行驶？
```

特点：

```text
有位置和朝向
通常没有完整时间信息
需要考虑车道边界、障碍物、平滑性
```

## 讲解备注

口播：

```text
同一个 route 里，可以有很多可能的 path。

比如车辆可以沿车道中心线行驶，也可以稍微向左偏一点绕开障碍物，还可以在允许换道时生成换道路径。

所以 route 是高层约束，path 是更具体的几何选择。
```

---

# 13. 第 9 页：Trajectory 是什么

## 页面内容

```text
Trajectory = Path + Velocity
```

也可以写成：

```text
(x, y, yaw, v, a, t)
```

## 讲解备注

口播：

```text
只有 path 还不够。

同一条路径，低速开和高速开完全不同。弯道要降速，停车点前要减速，限速变化不能瞬间满足。

所以轨迹不仅要告诉车辆走哪里，还要告诉车辆每一段以什么速度走。
```

## 配图建议

同一条几何线，上面用颜色表示速度：

- 红色表示慢。
- 绿色表示快。
- 停车点速度为 0。

---

# 14. 第 10 页：Control Command 是什么

## 页面内容

```text
Control Command 回答：
车辆此刻应该怎么执行？
```

常见输出：

```text
steering
acceleration
brake
gear
```

## 讲解备注

口播：

```text
控制器不负责重新决定路线，也不负责重新规划完整路径。

它的任务是看当前车辆状态和目标轨迹之间的误差，然后输出当前时刻的转向、加速度或制动命令。
```

## 关键区分

```text
规划看未来一段怎么走。
控制看当前一刻怎么执行。
```

---

# 15. 第 11 页：Mission Planner 在做什么

## 页面内容

```text
Mission Planner:
目标点 -> 车道级 Route
```

关键步骤：

```text
1. 加载 Lanelet 地图
2. 找起点所在车道
3. 找目标点所在车道
4. 在车道图上搜索
5. 输出 route sections
```

## 讲解备注

口播：

```text
Mission Planner 的核心不是避障，也不是生成控制器要跟踪的曲线。

它更像车道级导航。它把道路抽象成图，图里的节点是 lanelet，边表示车道之间可以通行或换道。

然后它在这个图上找到从起点到终点的一串车道。
```

## 对应素材

```text
concept/mission_planner_route_planning_blog.md
```

---

# 16. 第 12 页：Behavior / Start Planning 在做什么

## 页面内容

```text
Behavior / Start Planning:
在 route 约束下决定具体行为或起步方式
```

例子：

```text
跟车
换道
停车
靠边
从路边起步
从自由空间回到车道
```

## 讲解备注

口播：

```text
Mission Planner 只告诉你大方向，但真实车辆经常会遇到更局部的问题。

比如车停在路边，当前并不在车道中心线上。这个时候，直接沿 route 中心线走是不现实的，因为车辆可能需要先打一把方向，甚至倒一下，再回到车道。

这就是 Start Planner 和 Pull Out 类问题要解决的事情。
```

---

# 17. 第 13 页：Motion Planning 和路径优化在做什么

## 页面内容

```text
Motion Planning / Path Optimization:
生成更安全、更平滑、更可跟踪的局部路径或轨迹
```

相关算法：

```text
Hybrid A*
Elastic Band
MPT
```

## 讲解备注

口播：

```text
有了 route 和行为决策之后，系统还需要生成局部可执行路径。

这条路径不能碰撞，不能太抖，不能超出可行驶边界，还要尽量符合车辆运动学。

所以我们会看到 Hybrid A* 这种搜索算法，也会看到 Elastic Band、MPT 这种优化算法。
```

## 重点句

```text
局部规划不只是找一条线，而是找一条车辆能安全、平滑、稳定跟踪的线。
```

---

# 18. 第 14 页：Velocity Smoother 在做什么

## 页面内容

```text
Velocity Smoother:
给几何轨迹生成满足约束的速度曲线
```

约束：

```text
速度
加速度
jerk
曲率限速
停车点
外部限速
```

## 讲解备注

口播：

```text
很多新手会以为，只要路径有了，车就能开。

但路径只回答走哪里，没有回答开多快。

如果前方有限速，不能瞬间把速度从 10 米每秒变成 3 米每秒。
如果前方有停车点，也必须提前根据加速度和 jerk 约束减速。

Velocity Smoother 的核心，就是在已有轨迹上生成一条物理可行、乘坐舒适、适合控制器的速度曲线。
```

## 对应素材

```text
concept/velocity_smoother_blog.md
```

---

# 19. 第 15 页：Controller 在做什么

## 页面内容

```text
Controller:
当前车辆状态 + 目标轨迹 -> 控制命令
```

控制器关心：

```text
横向误差
航向误差
速度误差
转角限制
加速度限制
执行器延迟
```

## 讲解备注

口播：

```text
控制器站在整条链路的最后。

它看到的是当前车辆状态和未来一段目标轨迹。它要做的是让车辆尽量贴着目标轨迹走。

横向控制主要输出转向，纵向控制主要输出加速度或制动。
```

---

# 20. 第 16 页：Pure Pursuit 和 MPC 的区别

## 页面内容

| 控制方法 | 核心思想 | 特点 |
|---|---|---|
| Pure Pursuit | 追踪前方目标点 | 几何直观、简单 |
| MPC | 优化未来一段控制输入 | 能处理约束、模型更强 |

## 讲解备注

口播：

```text
Pure Pursuit 可以理解为几何控制。
它在轨迹前方找一个前瞻点，然后计算一段圆弧去追这个点。

MPC 则是优化控制。
它会预测未来一段时间车辆怎么运动，然后选择一串控制输入，让未来误差和控制代价尽量小，同时满足转角和转角变化率约束。
```

## 过渡句

```text
所以 Pure Pursuit 适合先学，因为它帮助我们建立横向控制直觉。MPC 更强，但需要车辆模型、误差模型和 QP 优化基础。
```

---

# 21. 第 17 页：为什么地图很重要

## 页面内容

```text
地图提供：
1. 车道边界
2. 中心线
3. 行驶方向
4. 车道连接关系
5. 交通规则属性
```

## 讲解备注

口播：

```text
在 Autoware 里，高精地图不是单纯用来显示的背景图。

它直接参与路线规划、车道查询、目标点合法性判断、可行驶区域生成。

没有地图拓扑，Mission Planner 就不知道哪些车道相连，也不知道车辆能不能从一个 lanelet 开到另一个 lanelet。
```

---

# 22. 第 18 页：为什么车辆模型很重要

## 页面内容

```text
车辆不是点
车辆不能横着移动
车辆有最小转弯半径
车辆有执行器延迟和限制
```

关键公式：

$$
\dot{x}=v\cos\psi
$$

$$
\dot{y}=v\sin\psi
$$

$$
\dot{\psi}=\frac{v}{L}\tan\delta
$$

## 讲解备注

口播：

```text
这组自行车模型公式后面会反复出现。

它告诉我们，车辆不是一个可以随意在平面上移动的点。车辆的朝向、速度和转角决定了它下一步能去哪里。

Hybrid A*、Pure Pursuit、MPC 都离不开这个基础模型。
```

## 注意

这一页只做预告，不要展开推导。后面会有专门课程。

---

# 23. 第 19 页：为什么优化问题会反复出现

## 页面内容

```text
很多模块都可以写成：

最小化某个代价
同时满足一些约束
```

例子：

```text
Elastic Band: 路径尽量平滑，但不能偏离太多
Velocity Smoother: 速度尽量高，但加速度和 jerk 不能超限
MPC: 误差尽量小，但转角和转角变化不能超限
MPT: 轨迹尽量好跟踪，但不能越界或碰撞
```

## 讲解备注

口播：

```text
自动驾驶规划控制里，很多问题都不是单目标问题。

我们既希望车开得准，又希望平滑；既希望效率高，又希望舒适；既希望靠近参考线，又不能越过边界。

这就自然形成了优化问题。
```

## 重点句

```text
目标函数表达偏好，约束表达底线。
```

---

# 24. 第 20 页：一条轨迹如何被层层加工

## 页面内容

```text
Lanelet route
  -> 初始局部路径
  -> 平滑路径
  -> 优化轨迹
  -> 速度平滑轨迹
  -> 控制器跟踪目标
```

## 配图建议

同一张道路图上叠加不同颜色：

- 灰色：车道中心线。
- 蓝色：route。
- 黄色：初始路径。
- 绿色：平滑后路径。
- 红色点：带速度轨迹点。

## 讲解备注

口播：

```text
这里要特别注意，Autoware 里的轨迹不是凭空出现的。

它通常会经过很多层处理。每一层不是推翻上一层，而是在上一层结果基础上增加约束和信息。

从 route 到 path，是从车道级导航变成几何线。
从 path 到 trajectory，是加上速度。
从 trajectory 到 control，是变成当前执行命令。
```

---

# 25. 第 21 页：新手看源码为什么容易迷路

## 页面内容

```text
直接读源码容易遇到：
1. 包太多
2. 消息类型太多
3. 参数太多
4. 工程保护逻辑太多
5. 不知道主流程在哪里
```

## 讲解备注

口播：

```text
源码里不只有核心算法，还有大量工程代码。

比如坐标变换、异常处理、参数读取、调试输出、状态机、消息转换、fallback 逻辑。

这些都很重要，但新手如果一开始就全部看，很容易分不清主干和枝叶。
```

## 重点句

```text
先看算法主线，再看工程细节。
```

---

# 26. 第 22 页：正确学习顺序

## 页面内容

推荐顺序：

```text
1. 总览
2. Lanelet 和 Route
3. 局部路径和 Hybrid A*
4. 路径平滑和轨迹优化
5. 速度平滑
6. Pure Pursuit
7. MPC
8. 端到端复盘
```

## 讲解备注

口播：

```text
我不建议一上来就学 MPC。

MPC 很重要，但它依赖车辆模型、参考轨迹、误差定义、离散化、优化问题。如果前面的 route、path、trajectory 都没分清，MPC 公式会很容易变成纯数学。

所以我们先建立链路，再学模块，最后回到优化控制。
```

---

# 27. 第 23 页：后续课程路线

## 页面内容

```text
后续课程：
1. Lanelet 与 Mission Planner
2. Hybrid A* Pull Out
3. Elastic Band 路径平滑
4. MPT 轨迹优化
5. Velocity Smoother
6. Pure Pursuit
7. 横向 MPC
8. 总复盘
```

## 讲解备注

口播：

```text
后面的每一集都会放回今天这张总链路里。

我们不会孤立地讲某一个公式，而是始终问：
这个模块的输入是什么？输出是什么？它解决链路上的哪个问题？
```

---

# 28. 第 24 页：本集总结图

## 页面内容

```text
Goal
  -> Route
  -> Path
  -> Trajectory
  -> Control Command
```

每个下面写一句：

```text
Route: 经过哪些车道
Path: 车道里走哪条线
Trajectory: 每一段开多快
Control: 当前怎么执行
```

## 讲解备注

口播：

```text
如果这一集只记住一张图，就是这张。

目标点不是直接变成控制命令，而是经过 route、path、trajectory、control 这几层逐步翻译。
```

---

# 29. 第 25 页：三句话总结

## 页面内容

```text
第一，规划控制是一条链，不是一个单独算法。

第二，Route、Path、Trajectory、Control 分别处在不同抽象层。

第三，学习源码前，先搞清楚每个模块的输入、输出和物理意义。
```

## 讲解备注

口播：

```text
这三句话就是本集最重要的结论。

后面我们每讲一个模块，都回到这三句话。
```

---

# 30. 第 26 页：下一集预告

## 页面内容

```text
下一集：
Lanelet 与 Mission Planner

我们会讲：
1. 道路如何变成车道图
2. 起点和目标点如何匹配到 lanelet
3. 最短路线如何生成
4. 为什么输出是 route sections
```

## 讲解备注

口播：

```text
下一集我们正式进入第一个模块：Mission Planner。

它回答的是：给定当前位置、目标点和高精地图，Autoware 怎么生成车道级 route。
```

---

# 31. 第 27 页：观众课后问题

## 页面内容

```text
课后想一想：

1. Route 和 Path 最大区别是什么？
2. 为什么速度规划不能只做 min(v, speed_limit)？
3. 为什么控制器不应该重新决定路线？
4. 为什么车辆模型会影响路径规划？
```

## 讲解备注

这页可以用于视频结尾互动，也可以放在评论区引导讨论。

---

# 32. 第 28 页：结尾页

## 页面内容

```text
下一集见：
Lanelet 与 Mission Planner
```

## 讲解备注

自然结束即可。

---

# 33. 本集完整口播串联稿

下面是一版更连贯的口播稿，你可以直接拿去试讲。正式录制时不需要逐字照念，可以当作提示。

```text
大家好，这一集我们先不急着打开 Autoware 源码，也不急着推 MPC 公式。

很多人学习 Autoware 规划控制时，第一步就是去翻源码。然后很快就会发现，包很多，消息很多，参数很多，Mission Planner、Behavior Planner、Motion Planner、Velocity Smoother、Controller 全都混在一起。

如果没有主线，源码越看越多，反而越看越乱。

所以这一集我们先回答一个最核心的问题：给自动驾驶车一个目标点，它最后是怎么一步步变成方向盘、油门和刹车控制命令的？

我们从一个最简单的任务开始。车辆当前在 A 点，目标在 B 点。人类看这个问题，可能会觉得很简单，开过去就行。但自动驾驶系统不能直接从 A 点画一条直线到 B 点。因为这条直线可能穿过隔离带，可能逆行，可能压过不可通行区域，也可能完全不符合车辆的转弯能力。

目标点本身只告诉系统最终想去哪里，但没有告诉车辆中间经过哪些车道，在车道里走哪条线，每一段开多快，也没有告诉当前方向盘应该转多少。

所以自动驾驶系统需要把一个很高层的目标，一层一层翻译成越来越具体的结果。

最重要的链路就是：Goal 到 Route，到 Path，到 Trajectory，到 Control Command。

Route 回答的是经过哪些车道。它更像车道级导航结果。

Path 回答的是在这些车道里具体走哪条几何线。

Trajectory 在 Path 基础上增加速度和时间意义，告诉车辆每一段应该以多快速度通过。

Control Command 则是当前真正发给车辆的命令，比如转向、加速度、制动。

这个区别非常关键。很多新手把 Route、Path、Trajectory 混成一个东西，后面看代码就会非常痛苦。

Mission Planner 负责的是 Route。它根据当前车辆位姿、目标位姿和 Lanelet 地图，找到一条车道级路线。它的核心不是避障，也不是生成控制器要跟踪的曲线，而是在车道图上找到从起点到终点的一串 lanelet，并组织成 route sections。

有了 Route 之后，还需要行为和运动规划。因为真实车辆不只是沿着 route 走就行。比如车辆可能停在路边，当前位置不在车道中心线上，甚至朝向也和道路方向不一致。这时就需要 Start Planner 或 Pull Out 类算法，把车辆从当前自由空间状态带回到正常车道行驶空间。

局部运动规划还要考虑路径是否平滑、是否可跟踪、是否满足车身约束和可行驶边界。这里会出现 Hybrid A*、Elastic Band、MPT 这些算法。

Hybrid A* 更像搜索，它考虑车辆的位置、朝向、前进倒车和碰撞检查。

Elastic Band 更像路径平滑，它把离散路径点当作可以横向微调的变量，让路径不要有明显折角。

MPT 则更像轨迹优化，它会考虑车辆模型、道路边界、软约束和上一帧结果，让轨迹更适合控制器跟踪。

但即使几何轨迹已经很好，也还不能直接开。因为车辆还需要速度。

同一条路径，低速开和高速开完全不同。弯道速度太高会产生过大的横向加速度；停车点前不能最后一刻才刹车；外部限速降低时，车辆也不能瞬间降到新速度。

这就是 Velocity Smoother 的作用。它在已有轨迹上生成满足速度、加速度、jerk、曲率限速和停车约束的速度曲线。

最后，轨迹会交给控制器。

控制器根据当前车辆状态和目标轨迹，计算当前时刻应该怎么操作。横向控制主要输出转向，纵向控制主要输出加速度或制动。

后面我们会讲两个典型横向控制方法。Pure Pursuit 是几何控制，它在轨迹前方找一个前瞻点，然后算一段圆弧去追这个点。MPC 是优化控制，它预测未来一段时间车辆怎么运动，然后选择一串控制输入，让未来误差尽量小，同时满足转角和转角变化率限制。

这里也能看出，车辆模型非常重要。车辆不是一个点，不能横着移动，有最小转弯半径，也有执行器限制。自行车模型里的 x、y、yaw、速度和转角关系，会在 Hybrid A*、Pure Pursuit、MPC 中反复出现。

另外你还会发现，优化问题在 Autoware 里出现得非常频繁。Elastic Band 是在平滑和偏离原路径之间做权衡；Velocity Smoother 是在效率、舒适性和约束之间做权衡；MPC 是在跟踪误差、控制输入和平滑性之间做权衡；MPT 是在参考轨迹、道路边界和车辆约束之间做权衡。

所以我们可以记住一句话：目标函数表达偏好，约束表达底线。

这一集最后，我们回到最开始的图。

给车辆一个目标点，它不会直接变成方向盘角。它会先变成 Route，回答经过哪些车道；再变成 Path，回答车道里走哪条线；再变成 Trajectory，回答每一段开多快；最后进入 Controller，输出当前控制命令。

所以本集只需要记住三句话。

第一，规划控制是一条链，不是一个单独算法。

第二，Route、Path、Trajectory、Control 分别处在不同抽象层。

第三，学习源码前，先搞清楚每个模块的输入、输出和物理意义。

下一集我们正式进入 Mission Planner，讲道路如何被建模成 Lanelet 图，以及 Autoware 如何从起点和目标点生成车道级 Route。
```

---

# 34. 本集配图清单

建议至少准备这些图：

1. 车辆从 A 点到 B 点的道路场景图。
2. 目标点不能直接控制车辆的示意图。
3. 总链路图：Goal -> Route -> Path -> Trajectory -> Control。
4. Route、Path、Trajectory 的对比图。
5. Mission Planner 在 Lanelet 图上搜索的示意图。
6. Pull Out 从路边回到车道的示意图。
7. 路径平滑前后对比图。
8. 速度曲线叠加在轨迹上的示意图。
9. Pure Pursuit 前瞻点示意图。
10. MPC 预测时域示意图。

这些图可以先画得很简单。第一集不是展示细节，而是建立地图。

---

# 35. 本集录制提醒

## 35.1 语速

新手向课程不要太快。每个新术语第一次出现时停一下。

特别是这几个词：

- Route。
- Path。
- Trajectory。
- Control Command。
- Lanelet。
- Velocity Smoother。
- MPC。

## 35.2 不要展开太深

第一集不要深入讲：

- Lanelet2 文件格式细节。
- Hybrid A* 代价函数。
- Elastic Band QP 推导。
- Velocity Smoother jerk 三段式减速。
- MPC 矩阵推导。

这些都留到后续单集。

## 35.3 要反复回到主线

每讲完一个模块，都回到这句话：

```text
它的输入是什么，输出是什么，它在 Goal -> Route -> Path -> Trajectory -> Control 中处于哪里？
```

---

# 36. 本集完成后的下一步

录完这一集后，下一步建议制作：

```text
第 2 集：Lanelet 与 Mission Planner
```

你可以继续让我生成：

```text
请根据 AUTOWARE规划控制层视频教程制作指南.md 和 concept/mission_planner_route_planning_blog.md，
生成第 2 集“Lanelet 与 Mission Planner”的详细视频脚本，
要求包含 PPT 页大纲、口播稿、配图建议和新手易错点。
```

