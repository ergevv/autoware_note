---
marp: true
theme: default
paginate: true
size: 16:9
footer: "Autoware 规划与控制层教程 | 第 4 集 | 新手友好详解版"
---

# 对应源码文件: 
autoware_devel/src/universe/autoware_universe/planning/autoware_path_smoother/src/elastic_band_smoother.cpp

---


<!--
用途说明：
这是一份第 4 集的 PPT 制作稿，采用 Marp Markdown 格式。
本文件是“新手友好详解版”：保留原 PPT 主线和关键知识点，并增加术语解释、
公式读法、模块输入输出、过渡页和常见疑问页，适合作为录课讲义和自学材料。
你可以直接按每个 --- 分隔的页面制作 PowerPoint，也可以使用 Marp 导出为 pptx。

建议导出命令：
marp AUTOWARE规划控制层第4集_ElasticBand路径平滑PPT.md --pptx

本集重点：
从“路径为什么需要平滑”讲起，逐步推到 Autoware Elastic Band 的核心 QP：
优化每个路径点的横向偏移，在 box 约束下最小化离散二阶差分和横向偏移正则项。
-->

# Elastic Band 路径平滑

## 从离散点到 QP

### 新手友好详解版

```text
Raw Path
  -> Lateral Offset Variables
  -> Smooth QP
  -> Smoothed Path
```

<!--
讲解备注：
本集承接第 3 集。Hybrid A* 或其他规划器可能生成可行路径，但可行不代表足够平滑。
Elastic Band 的作用是做轻量几何平滑，让后续 Frenet、MPT、控制器看到更稳定的参考路径。

配图建议：
左侧画一条锯齿路径，右侧画平滑后的路径，中间放 QP 优化器图标。
-->

---

# 新手友好版怎么使用

本版不是精简版，而是：

- 保留原有知识点
- 把术语第一次出现时讲清楚
- 把公式拆成“意义、符号、工程作用”
- 在章节之间增加过渡页
- 在矩阵推导前后补直觉解释

```text
目标：让第一次接触 QP 路径平滑的人，
也能知道每一步为什么存在。
```

<!--
讲解备注：
开场先降低心理门槛：这一集公式多，但主线很单纯。
从点列的折角出发，最后只是求每个点横向挪多少。

配图建议：
画一条从“锯齿点列”到“横向小挪动”再到“QP 求解”的学习路线。
-->

---

# 本集在总链路中的位置

```text
Goal
  -> Route
  -> Path
       -> Path Smoothing   <- 本集重点
  -> Trajectory
  -> Control Command
```

Elastic Band 解决的是：

```text
怎样在不大幅改变原路径的前提下，
让离散路径更光顺？
```

<!--
讲解备注：
先把本集放回总链路。它不决定 route，也不决定速度，更不直接输出控制命令。
它是路径进入后续轨迹优化或控制前的几何预处理。

配图建议：
总链路图中高亮 Path Smoothing 子模块。
-->

---

# 今天要解决的问题

上游已经给出一条路径：

- 来自车道中心线
- 来自路径拼接
- 来自避障绕行
- 来自 Hybrid A* 搜索
- 来自历史路径复用

但它可能：

```text
有锯齿、有折角、有航向跳变
```

<!--
讲解备注：
从问题出发。上游路径“能走”不代表“适合控制器跟踪”。

配图建议：
一条路径局部有小折角，旁边画方向箭头突然变化。
-->

---

# 为什么小折角也严重

几何不连续会放大到后续模块：

- 曲率估计抖动
- Frenet 投影不稳定
- 法向量和切向量跳变
- 横向误差和航向误差抖动
- 控制器转角输出抖动

路径看起来只是小锯齿，控制上可能变成方向盘抖动。

<!--
讲解备注：
这页是本集动机。要让观众理解：平滑不是为了图好看，而是为了让下游计算稳定。

配图建议：
路径小折角 -> 曲率尖峰 -> 转向角抖动的三段因果图。
-->

---

# Elastic Band 的一句话

Autoware Elastic Band 可以记成：

```text
带横向位移约束的离散路径平滑 QP
```

它做的是：

```text
局部、小幅、受约束的几何修正
```

它不做的是：

```text
重新规划拓扑、动态避障、速度规划、控制求解
```

<!--
讲解备注：
这一页先定边界。Elastic Band 不是完整规划器，它只是轻量路径平滑器。

配图建议：
在原路径附近画一个窄走廊，平滑路径只能在窄走廊内调整。
-->

---

# 它和传统 Elastic Band 不一样

传统机器人 Elastic Band 常像：

```text
弹簧内力 + 障碍物斥力
```

Autoware 版本更像：

```text
路径点横向偏移变量 + 二次规划求解
```

所以不要把它理解成：

```text
障碍物斥力把路径推开
```

<!--
讲解备注：
名字容易误导。Autoware Elastic Band 更偏 QP 几何平滑，不是力迭代避障算法。

配图建议：
左边传统弹簧/斥力示意，右边 QP 优化器输出横向偏移。
-->

---

# 本集主线

```text
离散路径点
  -> 什么叫平滑
  -> 二阶差分
  -> 矩阵二次型
  -> 横向偏移变量
  -> QP 目标函数
  -> box 约束
  -> 在线工程处理
```

<!--
讲解备注：
这一页是目录。第 4 集公式会多一些，所以需要先告诉观众推导路线。

配图建议：
横向流程图，每个节点后面配一个小图标：点列、差分、矩阵、QP。
-->

---

# 本集学习地图

这一集可以分成四层：

```text
第一层：问题直觉
路径点有折角，后续曲率和控制会抖

第二层：平滑怎么度量
三点二阶差分衡量局部折角

第三层：变量怎么选
不直接优化 x/y，只优化横向偏移 l_i

第四层：怎么交给求解器
目标函数 + box 约束 -> 标准 QP
```

<!--
讲解备注：
这是防迷路页。第 4 集公式密集，但每个公式都能放进这四层。
讲完一个大段后可以回到这张图，告诉观众现在所在的位置。

配图建议：
四层阶梯图：问题、度量、变量、求解器。
-->

---

# 先记住五个词

`离散点列`：

```text
工程里路径不是连续曲线，而是一串采样点
```

`二阶差分`：

```text
用三个相邻点衡量局部折角或方向变化
```

`横向偏移`：

```text
每个点只沿法向小幅移动的距离 l_i
```

`QP`：

```text
二次目标 + 线性约束的优化问题
```

`Box Constraint`：

```text
每个变量都有自己的上下界
```

<!--
讲解备注：
这五个词是本集反复出现的核心概念。
先给口语化定义，后面再逐步精确化。

配图建议：
五张概念卡片：点列、三点折角、横向小箭头、QP 三要素、上下界框。
-->

---

# 模块卡片：Elastic Band

输入是什么：

```text
原始路径点列 + 每个点的局部坐标系 + 可移动范围 + 权重参数
```

输出是什么：

```text
平滑后的路径点列
```

为什么需要它：

```text
把上游“可行但可能锯齿”的路径，
变成更适合后续轨迹优化和控制的参考路径。
```

它不解决什么：

```text
不重新规划拓扑，不做完整碰撞避障，不规划速度，不输出控制命令。
```

<!--
讲解备注：
每个重要模块都用输入、输出、为什么需要和职责边界固定住。
这页可以避免观众把 Elastic Band 误解成完整 path planner。

配图建议：
中间 Elastic Band 方块，左侧输入路径和参数，右侧输出平滑路径，下方写职责边界。
-->

---

# 路径是离散点列

连续路径：

$$
\mathbf{p}(s)=
\begin{bmatrix}
x(s)\\
y(s)
\end{bmatrix}
$$

工程里通常采样成：

$$
\mathbf{r}_0,\mathbf{r}_1,\dots,\mathbf{r}_{N-1}
$$

第 $i$ 个参考点：

$$
\mathbf{r}_i=
\begin{bmatrix}
x_i\\
y_i
\end{bmatrix}
$$

<!--
讲解备注：
先从路径离散化开始。Elastic Band 处理的是一串点，而不是连续函数。

配图建议：
一条连续曲线被采样成多个离散点。
-->

---

# 公式慢读：离散路径点

连续路径：

$$
\mathbf{p}(s)=
\begin{bmatrix}
x(s)\\
y(s)
\end{bmatrix}
$$

它想表达：

```text
路径本来可以看成随弧长 s 变化的连续曲线。
```

工程里变成：

$$
\mathbf{r}_0,\mathbf{r}_1,\dots,\mathbf{r}_{N-1}
$$

它想表达：

```text
程序实际处理的是一串采样点。
```

工程作用：

```text
Elastic Band 的所有平滑、约束和求解，
都发生在这串离散点上。
```

<!--
讲解备注：
这页帮助新手从“数学曲线”切换到“工程点列”。
后面的二阶差分就是在点列上定义的。

配图建议：
一条连续曲线被采样成 r0 到 rN 的点列。
-->

---

# 每个点还有局部坐标系

由航向角 $\theta_i$ 构造：

$$
\mathbf{t}_i=
\begin{bmatrix}
\cos\theta_i\\
\sin\theta_i
\end{bmatrix}
$$

$$
\mathbf{n}_i=
\begin{bmatrix}
-\sin\theta_i\\
\cos\theta_i
\end{bmatrix}
$$

其中：

```text
t_i 是切向
n_i 是左法向
```

<!--
讲解备注：
法向量后面会成为横向偏移方向。讲清楚 t 和 n 的几何意义。

配图建议：
路径点处画切向箭头和法向箭头。
-->

---

# 公式慢读：切向和法向

切向量：

$$
\mathbf{t}_i=
\begin{bmatrix}
\cos\theta_i\\
\sin\theta_i
\end{bmatrix}
$$

它表示：

```text
路径在第 i 个点的前进方向。
```

法向量：

$$
\mathbf{n}_i=
\begin{bmatrix}
-\sin\theta_i\\
\cos\theta_i
\end{bmatrix}
$$

它表示：

```text
垂直于路径前进方向的横向方向。
```

工程作用：

```text
后面每个路径点只能沿 n_i 方向小幅移动。
```

<!--
讲解备注：
这里一定要把 t 和 n 画出来。
切向是“沿路走”，法向是“横向挪”，Elastic Band 只允许后者。

配图建议：
路径点上画一根向前箭头 t_i 和一根垂直横向箭头 n_i。
-->

---

# 直觉：只允许横向移动

Autoware Elastic Band 的关键限制：

```text
每个点不沿前后方向乱滑，
只沿局部法向移动一小段。
```

优化后的点：

$$
\mathbf{p}_i
=
\mathbf{r}_i+l_i\mathbf{n}_i
$$

其中 $l_i$ 是第 $i$ 个点的横向偏移。

<!--
讲解备注：
这是本集核心变量。直接优化 x,y 是 2N 个变量，优化 l_i 是 N 个变量，并且不破坏路径点顺序。

配图建议：
每个路径点沿法向短线段上下移动。
-->

---

# 公式慢读：横向偏移变量

公式：

$$
\mathbf{p}_i
=
\mathbf{r}_i+l_i\mathbf{n}_i
$$

它想表达：

```text
新路径点 = 原路径点 + 沿法向移动的一小段距离。
```

符号对应：

- $\mathbf{r}_i$：原始参考点
- $\mathbf{n}_i$：该点的横向法向方向
- $l_i$：这个点要横向移动多少
- $\mathbf{p}_i$：优化后的新点

工程作用：

```text
把二维点优化，变成只求每个点的一个横向偏移量。
```

<!--
讲解备注：
这是本集最关键的变量定义。
强调 l_i 正负表示向法向两侧移动，大小表示移动距离。

配图建议：
原点 r_i、法向箭头 n_i、偏移距离 l_i、新点 p_i。
-->

---

# 为什么不用 x、y 直接优化

直接优化：

```text
(x_0,y_0,\dots,x_{N-1},y_{N-1})
```

问题：

- 变量是 $2N$
- 点可能沿纵向乱滑
- 采样顺序可能被破坏
- 约束更难写

改成横向偏移：

```text
(l_0,l_1,\dots,l_{N-1})
```

更小、更稳、更贴合路径语义。

<!--
讲解备注：
这页解释变量选择的工程智慧。降维不是为了好看，而是为了稳定和可控。

配图建议：
左边点在二维平面乱动，右边点只在短横向线段上动。
-->

---

# 常见疑问：只横向移动会不会太保守

是的，它是刻意保守。

这样做的好处是：

- 不容易改变路径前后顺序
- 不容易破坏上游规划意图
- 变量从 $2N$ 降到 $N$
- 约束能写成简单上下界
- 在线求解更稳定

代价是：

```text
如果原路径拓扑本身错了，
Elastic Band 不能靠横向小挪动救回来。
```

<!--
讲解备注：
这页提前讲能力边界。横向偏移是保守设计，不是万能重规划。

配图建议：
窄走廊内的小幅平滑成功；原路径绕错拓扑时标出无法修复。
-->

---

# 什么叫路径更平滑

看相邻两段方向：

$$
\mathbf{p}_k-\mathbf{p}_{k-1}
$$

$$
\mathbf{p}_{k+1}-\mathbf{p}_k
$$

如果路径平顺，这两段方向应该接近。

如果差得很大，局部就有折角。

<!--
讲解备注：
先讲几何直觉，不要直接上二阶差分。平滑就是相邻线段方向变化小。

配图建议：
一组平顺三点和一组折角三点对比。
-->

---

# 二阶差分

相邻线段变化：

$$
(\mathbf{p}_{k+1}-\mathbf{p}_k)
-
(\mathbf{p}_k-\mathbf{p}_{k-1})
$$

整理：

$$
\mathbf{p}_{k+1}
-2\mathbf{p}_k
+\mathbf{p}_{k-1}
$$

这就是离散二阶差分。

<!--
讲解备注：
二阶差分对应“方向变化”。它是 Elastic Band 平滑代价的核心。

配图建议：
三点形成折线，画出前后两段向量和它们的差。
-->

---

# 公式慢读：二阶差分

公式：

$$
\mathbf{p}_{k+1}
-2\mathbf{p}_k
+\mathbf{p}_{k-1}
$$

它想表达：

```text
后一段方向和前一段方向差了多少。
```

换一种看法：

```text
一阶差分看“方向”；
二阶差分看“方向变化”。
```

工程作用：

```text
把“局部是否有折角”变成一个可计算、可平方惩罚的向量。
```

<!--
讲解备注：
这页建议用三点图讲：p_k-p_{k-1} 是进入当前点的方向，p_{k+1}-p_k 是离开当前点的方向。
两者差越大，折角越明显。

配图建议：
三点折线，画出两个一阶差分向量，再画它们的差向量。
-->

---

# 二阶差分的物理直觉

如果局部接近直线：

$$
\mathbf{p}_{k+1}
-2\mathbf{p}_k
+\mathbf{p}_{k-1}
\approx 0
$$

如果有尖角：

$$
\left\|
\mathbf{p}_{k+1}
-2\mathbf{p}_k
+\mathbf{p}_{k-1}
\right\|
\text{ 变大}
$$

所以它可以惩罚折角和锯齿。

<!--
讲解备注：
这一页只讲直觉。让观众知道公式为什么合理。

配图建议：
直线路径二阶差分为 0，折线路径二阶差分较大。
-->

---

# 平滑目标函数

平滑代价：

$$
J_{\text{smooth}}
=
\sum_{k=1}^{N-2}
\left\|
\mathbf{p}_{k+1}
-2\mathbf{p}_k
+\mathbf{p}_{k-1}
\right\|^2
$$

目标：

```text
让所有局部折角尽量小。
```

<!--
讲解备注：
这是本集第一个必须记住的公式。它把“更平滑”写成了一个可优化的数量。

配图建议：
在路径上每三个相邻点画一个局部二阶差分小箭头。
-->

---

# 公式慢读：平滑目标

公式：

$$
J_{\text{smooth}}
=
\sum_{k=1}^{N-2}
\left\|
\mathbf{p}_{k+1}
-2\mathbf{p}_k
+\mathbf{p}_{k-1}
\right\|^2
$$

它想表达：

```text
把每组三点的折角程度平方后加起来。
```

为什么要平方：

```text
不管折角朝哪边，都算作不平滑；
大的折角会被更重地惩罚。
```

工程作用：

```text
给 QP 提供最核心的平滑目标。
```

<!--
讲解备注：
这页是二阶差分从局部量变成全局代价。
强调 sum 是整条路径所有局部折角的总惩罚。

配图建议：
路径上多个三点窗口，每个窗口产生一个小代价，最后汇总成 J_smooth。
-->

---

# 只最小化平滑不够

如果只最小化二阶差分：

```text
路径会倾向于被拉成尽可能直的线
```

但原路径可能已经包含：

- 避障意图
- 车道结构
- 停车点
- 目标点
- 行为规划结果

所以不能为了平滑破坏上游意图。

<!--
讲解备注：
平滑不是越强越好。Elastic Band 的核心是“在允许范围内平滑”，不是重新规划一条最直路径。

配图建议：
原路径绕开障碍，过度平滑后切穿障碍的反例。
-->

---

# 常见疑问：越平滑越好吗

不是。

只追求平滑，可能会：

- 把避障绕行拉直
- 把路径拉出车道
- 破坏停车点附近形状
- 让路径偏离上游行为意图

所以 Elastic Band 必须同时考虑：

```text
平滑性
```

和：

```text
不要离原路径太远
```

<!--
讲解备注：
这页把“平滑”和“忠于原路径”这两个目标讲清楚。
优化问题通常不是单目标越大越好，而是在多个目标间折中。

配图建议：
一条过度拉直穿出走廊的路径打叉，一条适度平滑路径打勾。
-->

---

# 需要两个目标

Elastic Band 同时追求：

```text
1. 二阶差分小
2. 不要偏离原始路径太远
```

对应代价：

$$
J
=
J_{\text{smooth}}
+
J_{\text{lat}}
$$

其中 $J_{\text{lat}}$ 惩罚横向偏移本身。

<!--
讲解备注：
这页建立“平滑”和“保形”的权衡。后面 smooth_weight 和 lat_error_weight 都来自这里。

配图建议：
一条过度平滑偏离很大的路径 vs 一条适度平滑路径。
-->

---

# 过渡：从三点公式到矩阵

现在我们已经有了：

```text
每组三点的二阶差分
```

但一条路径有很多点。

如果逐组三点手写公式，会很乱。

矩阵的作用只是：

```text
把所有三点二阶差分一次性批量算出来。
```

<!--
讲解备注：
这页专门降低矩阵恐惧。
矩阵 D 不是新概念，它只是把前面的三点公式复制很多行。

配图建议：
左边多个三点窗口，右边对应矩阵 D 的多行。
-->

---

# 一维二阶差分矩阵

对一维点列：

$$
\mathbf{x}=
\begin{bmatrix}
x_0&x_1&\cdots&x_{N-1}
\end{bmatrix}^T
$$

定义：

$$
D=
\begin{bmatrix}
1&-2&1&0&\cdots&0\\
0&1&-2&1&\cdots&0\\
\vdots&&&&\ddots&\vdots\\
0&\cdots&0&1&-2&1
\end{bmatrix}
$$

则 $D\mathbf{x}$ 就是所有二阶差分。

<!--
讲解备注：
这是从几何公式进入矩阵形式的关键。矩阵 D 的每一行只看相邻三点。

配图建议：
矩阵 D 的一行高亮，对应路径中的三个点。
-->

---

# 公式慢读：二阶差分矩阵 D

矩阵一行：

$$
\begin{bmatrix}
1&-2&1
\end{bmatrix}
$$

它想表达：

```text
取前一点，减两倍当前点，再加后一点。
```

矩阵很多行：

```text
第 1 行看 x0, x1, x2
第 2 行看 x1, x2, x3
第 3 行看 x2, x3, x4
...
```

工程作用：

```text
D x 一次性得到所有局部二阶差分。
```

<!--
讲解备注：
让观众把 D 的每一行看成一个“三点探测器”。
这样矩阵就不抽象了。

配图建议：
矩阵某一行高亮，同时路径上对应三个点高亮。
-->

---

# 平方和变成二次型

一维二阶差分平方和：

$$
\|D\mathbf{x}\|^2
=
(D\mathbf{x})^T(D\mathbf{x})
$$

整理：

$$
\|D\mathbf{x}\|^2
=
\mathbf{x}^TD^TD\mathbf{x}
$$

令：

$$
P_D=D^TD
$$

这就是 QP 里 Hessian 的来源之一。

<!--
讲解备注：
让观众看到：平方和天然会变成二次型。QP 不是硬套出来的，而是由平滑目标自然产生。

配图建议：
D x -> 平方和 -> x^T D^T D x 的转换图。
-->

---

# 公式慢读：为什么会出现 Hessian

公式：

$$
\|D\mathbf{x}\|^2
=
\mathbf{x}^TD^TD\mathbf{x}
$$

它想表达：

```text
所有二阶差分的平方和，
可以写成标准二次型。
```

符号对应：

- $D\mathbf{x}$：所有二阶差分
- $(D\mathbf{x})^T(D\mathbf{x})$：把它们平方并求和
- $D^TD$：二次型中的矩阵

工程作用：

```text
QP 求解器需要 Hessian 矩阵，
D^TD 就是平滑项对应的 Hessian 来源。
```

<!--
讲解备注：
Hessian 这个词可以简单解释成“二次项前面的矩阵”。
这里的重点是：QP 不是硬套，二阶差分平方自然产生二次型。

配图建议：
三步箭头：差分向量 -> 平方和 -> Hessian。
-->

---

# 二维路径的平滑矩阵

把 $x$ 和 $y$ 坐标堆叠：

$$
\mathbf{z}=
\begin{bmatrix}
x_0\\
\vdots\\
x_{N-1}\\
y_0\\
\vdots\\
y_{N-1}
\end{bmatrix}
\in\mathbb{R}^{2N}
$$

二维平滑矩阵：

$$
P_0=
\begin{bmatrix}
D^TD&0\\
0&D^TD
\end{bmatrix}
$$

于是：

$$
J_{\text{smooth}}=\mathbf{z}^TP_0\mathbf{z}
$$

<!--
讲解备注：
x 和 y 分别做同样的二阶差分，所以矩阵是块对角结构。

配图建议：
两个相同 D^T D 方块分别作用于 x 和 y。
-->

---

# 稀疏带状结构

二阶差分只连接相邻三点。

所以：

```text
D 很稀疏
D^T D 是五对角带状矩阵
```

工程优势：

- 存储少
- 计算快
- 求解器容易利用稀疏性
- 适合在线规划

<!--
讲解备注：
这页解释为什么这种平滑问题适合实时系统。它不是大而杂的优化，而是结构非常规整。

配图建议：
矩阵热力图，只有主对角线附近有非零元素。
-->

---

# 常见疑问：矩阵这么大为什么还能在线跑

因为它很稀疏。

二阶差分只关心：

```text
前一点、当前点、后一点
```

所以矩阵里大部分位置都是 0。

这带来三个好处：

- 存储量小
- 乘法快
- QP 求解器能利用结构

```text
大矩阵不可怕，杂乱无结构的大矩阵才可怕。
```

<!--
讲解备注：
这页解释在线实时性的来源。
新手看到矩阵会觉得计算很重，这里要强调带状稀疏结构。

配图建议：
矩阵热力图：只有主对角线附近有颜色，其余为空白。
-->

---

# 回到 Autoware 的变量

优化变量不是 $\mathbf{z}$，而是：

$$
\mathbf{l}=
\begin{bmatrix}
l_0&l_1&\cdots&l_{N-1}
\end{bmatrix}^T
$$

每个点：

$$
\mathbf{p}_i(l_i)
=
\mathbf{r}_i+l_i\mathbf{n}_i
$$

坐标展开：

$$
x_i'=x_i-\sin\theta_i\,l_i
$$

$$
y_i'=y_i+\cos\theta_i\,l_i
$$

<!--
讲解备注：
这页把前面的全局坐标平滑代价和真正变量连接起来。

配图建议：
一个点沿法向偏移，标出 x、y 坐标变化。
-->

---

# 公式慢读：横向偏移展开到 x/y

公式：

$$
x_i'=x_i-\sin\theta_i\,l_i
$$

$$
y_i'=y_i+\cos\theta_i\,l_i
$$

它想表达：

```text
沿法向移动 l_i 后，
全局 x 和 y 都会发生相应变化。
```

符号对应：

- $\theta_i$：参考路径在该点的航向角
- $l_i$：横向偏移
- $(x_i',y_i')$：移动后的全局坐标

工程作用：

```text
把求解器输出的 l_i 转回真实地图坐标。
```

<!--
讲解备注：
这页把局部横向偏移和全局坐标变化连接起来。
不要只说矩阵 T，先让观众看到单点公式。

配图建议：
局部法向偏移箭头分解到全局 x/y 方向。
-->

---

# 法向映射矩阵 T

把全局坐标和横向偏移联系起来。

第 $i$ 行只有两个非零元素：

$$
T_{i,i}=-\sin\theta_i
$$

$$
T_{i,i+N}=\cos\theta_i
$$

直觉：

```text
T 把全局位移投影到局部法向。
```

<!--
讲解备注：
不需要让新手记住矩阵全部形状，只要知道 T 是“全局 xy”和“横向 l”之间的桥。

配图建议：
全局 dx,dy 向量投影到法向 n_i 上。
-->

---

# 公式慢读：法向映射矩阵 T

可以把 $T$ 理解成一张表：

```text
每一行记录一个点的法向方向。
```

它的作用是：

```text
把 N 个横向偏移 l_i，
转换成 2N 个全局 x/y 坐标增量。
```

单点关系：

$$
l_i\mathbf{n}_i
=
l_i
\begin{bmatrix}
-\sin\theta_i\\
\cos\theta_i
\end{bmatrix}
$$

工程作用：

```text
T 是“横向变量 l”和“全局路径点 z”之间的桥。
```

<!--
讲解备注：
不要强迫观众记 T 的维度和每个元素。
只要知道 T 保存每个点的法向方向，能把 l 映射回全局坐标。

配图建议：
l 向量进入 T^T，输出每个点的 dx/dy 箭头。
-->

---

# 从横向偏移回到全局点

所有优化后的点可以写成：

$$
\mathbf{z}(\mathbf{l})
=
\mathbf{x}+T^T\mathbf{l}
$$

其中：

- $\mathbf{x}$ 是原始路径坐标堆叠
- $T^T\mathbf{l}$ 是每个点的全局坐标增量

这一步把全局平滑代价变成关于 $\mathbf{l}$ 的函数。

<!--
讲解备注：
这是推导的核心桥梁。原始平滑定义在 z 上，但 QP 变量是 l。

配图建议：
l 向量经过 T^T 映射成每个点的 dx,dy。
-->

---

# 公式慢读：z(l)

公式：

$$
\mathbf{z}(\mathbf{l})
=
\mathbf{x}+T^T\mathbf{l}
$$

它想表达：

```text
优化后的全局路径点 =
原始路径点 +
由横向偏移带来的全局坐标变化。
```

符号对应：

- $\mathbf{x}$：原始路径的 x/y 坐标堆叠
- $\mathbf{l}$：所有点的横向偏移
- $T^T\mathbf{l}$：由横向偏移产生的全局坐标增量
- $\mathbf{z}(\mathbf{l})$：优化后的全局路径坐标

工程作用：

```text
让原来写在全局坐标 z 上的平滑代价，
变成关于求解变量 l 的代价。
```

<!--
讲解备注：
这是从几何问题转成 QP 变量问题的关键桥。
建议慢讲：求解器不直接求 z，而是求 l。

配图建议：
原始路径 x，加上横向偏移增量 T^T l，得到新路径 z(l)。
-->

---

# 平滑项代入变量

带权重的平滑矩阵：

$$
R=w_sP_0
$$

平滑代价：

$$
J_{\text{smooth}}(\mathbf{l})
=
\frac{1}{2}
(\mathbf{x}+T^T\mathbf{l})^T
R
(\mathbf{x}+T^T\mathbf{l})
$$

展开后会得到：

```text
关于 l 的二次项 + 一次项 + 常数项
```

<!--
讲解备注：
这里先不急着展开所有项。告诉观众：因为 z(l) 对 l 是线性的，所以平方后一定是二次函数。

配图建议：
z(l) 进入平滑代价，输出 QP 的 P 和 q。
-->

---

# 公式慢读：代入后为什么还是二次

关键事实：

```text
z(l) = x + T^T l
```

这是关于 $\mathbf{l}$ 的线性表达。

把线性表达放进平方项：

```text
线性表达的平方
  -> 关于 l 的二次函数
```

所以会得到：

```text
二次项 + 一次项 + 常数项
```

工程作用：

```text
说明 Elastic Band 可以交给标准 QP 求解器，
不需要非线性优化器。
```

<!--
讲解备注：
这页是从几何推导到 QP 的心理过渡。
观众不一定马上会展开矩阵，但要理解“线性 + 平方 = 二次”。

配图建议：
线性盒子进入平方盒子，输出二次函数。
-->

---

# 平滑项的 QP 贡献

展开并去掉常数项后：

$$
J_{\text{smooth}}(\mathbf{l})
=
\frac{1}{2}
\mathbf{l}^T
TRT^T
\mathbf{l}
+
(TR\mathbf{x})^T\mathbf{l}
$$

因此：

$$
P_{\text{smooth}}=TRT^T
$$

$$
\mathbf{q}_{\text{smooth}}=TR\mathbf{x}
$$

<!--
讲解备注：
一次项来自原始路径形状。如果原路径有折角，一次项会推动某些 l_i 非零，从而减小二阶差分。

配图建议：
原始折角路径产生“推动偏移”的箭头。
-->

---

# 公式慢读：平滑项的 P 和 q

二次项：

$$
P_{\text{smooth}}=TRT^T
$$

它控制：

```text
不同横向偏移 l_i 之间如何协同影响平滑性。
```

一次项：

$$
\mathbf{q}_{\text{smooth}}=TR\mathbf{x}
$$

它表达：

```text
原始路径本身的折角，会推动某些点往哪个方向移动。
```

工程作用：

```text
把“原路径哪里不平滑”翻译成求解器能理解的 P 和 q。
```

<!--
讲解备注：
这里不要把 P 和 q 讲成纯代数符号。
P 决定变量之间的耦合，q 来自原始路径形状。

配图建议：
原始折线路径产生 q 方向箭头，P 表示邻近点之间的耦合线。
-->

---

# 横向偏移正则项

为了不偏离原路径太远：

$$
J_{\text{lat}}(\mathbf{l})
=
\frac{1}{2}w_l\|\mathbf{l}\|^2
$$

写成矩阵：

$$
J_{\text{lat}}(\mathbf{l})
=
\frac{1}{2}
\mathbf{l}^T(w_lI)\mathbf{l}
$$

作用：

- 抑制过大偏移
- 保留上游路径意图
- 改善数值稳定性

<!--
讲解备注：
w_l 越大，路径越贴近原始路径；w_l 越小，路径越容易被平滑项拉动。

配图建议：
同一原路径下，w_l 小和 w_l 大的平滑结果对比。
-->

---

# 公式慢读：横向偏移正则

公式：

$$
J_{\text{lat}}(\mathbf{l})
=
\frac{1}{2}w_l\|\mathbf{l}\|^2
$$

它想表达：

```text
每个点横向挪得越多，代价越大。
```

符号对应：

- $\mathbf{l}$：所有点的横向偏移
- $\|\mathbf{l}\|^2$：所有偏移量平方和
- $w_l$：贴近原路径的权重

工程作用：

```text
防止为了平滑而过度偏离上游路径。
```

<!--
讲解备注：
这页和“越平滑越好吗”呼应。
lat 正则项就是让路径不要忘记原路径意图。

配图建议：
小偏移和大偏移的代价条对比。
-->

---

# 最终目标函数

把平滑项和横向正则项相加：

$$
J(\mathbf{l})
=
\frac{1}{2}
\mathbf{l}^T
\left(
w_sTP_0T^T+w_lI
\right)
\mathbf{l}
+
\left(
w_sTP_0\mathbf{x}
\right)^T
\mathbf{l}
$$

这就是 Elastic Band 的核心 QP 目标。

<!--
讲解备注：
这是本集第二个核心公式。所有符号都已经前面铺过，观众此时应该能看懂结构。

配图建议：
目标函数分成两块：平滑项、贴近原路径项。
-->

---

# 公式慢读：最终目标函数

最终目标可以用一句话读：

```text
既要让路径更平滑，
也要让每个点别离原路径太远。
```

矩阵形式：

$$
J(\mathbf{l})
=
\frac{1}{2}\mathbf{l}^TP\mathbf{l}
+\mathbf{q}^T\mathbf{l}
$$

其中：

- $P$：二次项，主要来自平滑和横向正则
- $\mathbf{q}$：一次项，来自原始路径形状
- $\mathbf{l}$：真正要求解的横向偏移

工程作用：

```text
求出一组最合适的 l_i，让路径在小范围内变顺。
```

<!--
讲解备注：
这页把复杂目标函数压回人话。
观众只要知道 P、q、l 分别是什么，就不会被矩阵吓住。

配图建议：
目标函数上方放两块积木：smooth 和 lateral regularization。
-->

---

# 标准 QP 形式

令：

$$
P=w_sTP_0T^T+w_lI
$$

$$
\mathbf{q}=w_sTP_0\mathbf{x}
$$

则：

$$
\min_{\mathbf{l}}
\quad
\frac{1}{2}\mathbf{l}^TP\mathbf{l}
+
\mathbf{q}^T\mathbf{l}
$$

这就是标准二次目标函数。

<!--
讲解备注：
QP 的 Q 来自 Quadratic，也就是二次。目标函数里有 l^T P l，所以自然是二次规划。

配图建议：
左边完整目标函数，右边简化成标准 QP。
-->

---

# 模块卡片：QP 求解器

输入是什么：

```text
变量 l 的二次目标 P、q
变量上下界 lb、ub
```

输出是什么：

```text
每个路径点的最优横向偏移 l_i
```

为什么需要它：

```text
目标函数和约束互相拉扯，
需要求解器找到整体代价最小且满足约束的偏移量。
```

它不解决什么：

```text
不理解道路语义，不理解障碍物动态，只求这个数学问题的最优解。
```

<!--
讲解备注：
把 QP 求解器从“黑盒数学”讲成“输入矩阵，输出偏移”的模块。
这能帮助新手理解求解器在工程链路里的位置。

配图建议：
QP 求解器方块，左侧输入 P/q/lb/ub，右侧输出 l*。
-->

---

# QP 的基本结构

标准形式：

$$
\min_{\mathbf{v}}
\quad
\frac{1}{2}\mathbf{v}^TH\mathbf{v}
+
\mathbf{f}^T\mathbf{v}
$$

subject to：

$$
\mathbf{b}_{lower}
\le
C\mathbf{v}
\le
\mathbf{b}_{upper}
$$

Elastic Band 中：

```text
v = l
H = P
f = q
```

<!--
讲解备注：
因为 concept/QP求解.md 当前是空文件，这里用标准 QP 形式补齐新手理解。
关键是：变量、目标、约束三件事要对应上。

配图建议：
QP 三要素图：变量、目标、约束。
-->

---

# 公式慢读：QP 三要素

一个 QP 可以先不用看求解器名字。

先看三件事：

```text
变量：要求什么？
目标：什么叫更好？
约束：什么结果不允许？
```

Elastic Band 中：

```text
变量：l_i，每个点横向挪多少
目标：二阶差分小，同时偏移别太大
约束：每个 l_i 只能在 [-c_i, c_i] 内
```

<!--
讲解备注：
这是 QP 入门页。很多新手怕 QP，是因为一上来看到 H、f、C、b。
先让他们用变量、目标、约束这三句话理解。

配图建议：
QP 三角形：变量、目标、约束，中心写“最优解”。
-->

---

# 为什么它一定是 QP

因为：

1. 变量 $\mathbf{l}$ 是连续向量
2. $\mathbf{p}_i=\mathbf{r}_i+l_i\mathbf{n}_i$ 对 $\mathbf{l}$ 是线性的
3. 二阶差分对 $\mathbf{l}$ 仍然是线性的
4. 线性表达的平方和是二次目标
5. 横向移动范围是线性不等式

所以它不是非线性规划，而是标准 QP。

<!--
讲解备注：
这页帮助新手建立“为什么是 QP”的逻辑，不是因为用了某个求解器才叫 QP，而是问题结构决定的。

配图建议：
线性表达 -> 平方 -> 二次目标；线性边界 -> QP 约束。
-->

---

# 横向约束

每个点只能在法向短线段内移动：

$$
\mathbf{p}_i(l_i)
=
\mathbf{r}_i+l_i\mathbf{n}_i
$$

并满足：

$$
-c_i\le l_i\le c_i
$$

其中 $c_i$ 是第 $i$ 个点允许的最大横向偏移。

<!--
讲解备注：
约束是 Elastic Band 保守性的来源。它保证点只在局部小范围内移动。

配图建议：
每个点旁边画一条短横向线段，端点表示上下界。
-->

---

# 公式慢读：横向约束

公式：

$$
-c_i\le l_i\le c_i
$$

它想表达：

```text
第 i 个点最多只能沿法向左右移动 c_i。
```

符号对应：

- $l_i$：第 i 个点的横向偏移
- $c_i$：第 i 个点允许的最大移动距离
- $-c_i$ 和 $c_i$：左右两个边界

工程作用：

```text
保证 Elastic Band 只是小幅修正，而不是重新规划整条路。
```

<!--
讲解备注：
这页要把约束讲成“每个点脚下的一小段滑轨”。
优化器只能在滑轨内选位置。

配图建议：
每个路径点画一根短横向滑轨，两端标 -c_i 和 c_i。
-->

---

# Box Constraint

把所有点写成向量：

$$
\begin{bmatrix}
-c_0\\
-c_1\\
\vdots\\
-c_{N-1}
\end{bmatrix}
\le
\begin{bmatrix}
l_0\\
l_1\\
\vdots\\
l_{N-1}
\end{bmatrix}
\le
\begin{bmatrix}
c_0\\
c_1\\
\vdots\\
c_{N-1}
\end{bmatrix}
$$

这里：

$$
A=I
$$

所以约束是最简单的 box constraint。

<!--
讲解备注：
box 约束就是每个变量都有自己的上下界。求解器处理这种结构通常很高效。

配图建议：
每个 l_i 都有一个小区间条，变量必须落在区间内。
-->

---

# 常见疑问：Box Constraint 为什么简单

因为每个变量只管自己：

```text
l_0 在自己的区间里
l_1 在自己的区间里
l_2 在自己的区间里
...
```

它不像复杂约束那样：

```text
l_0 + 2l_1 - l_3 <= b
```

所以 box constraint 可以理解成：

```text
每个点都有一条独立的横向滑轨。
```

<!--
讲解备注：
这页补 QP 约束直觉。
box constraint 是最容易理解也最容易求解的一类约束。

配图建议：
多个互不相连的小滑轨，每个 l_i 在自己的滑轨上移动。
-->

---

# 固定点也是约束

如果某个点不能移动：

$$
c_i=0
$$

则：

$$
0\le l_i\le 0
$$

也就是：

$$
l_i=0
$$

固定点可以看成上下界相同的 box constraint。

<!--
讲解备注：
这解释了如何用同一套约束表示“可动点”和“固定点”。

配图建议：
一个点的横向线段长度为 0，表示完全固定。
-->

---

# 三类移动范围

Autoware 会给不同位置不同自由度：

$$
c_i=
\begin{cases}
c_{\text{fix}}, & \text{fix 区域}\\
c_{\text{joint}}, & \text{joint 区域}\\
c_{\text{smooth}}, & \text{smooth 区域}
\end{cases}
$$

直觉：

```text
前端和关键点更固定，
中间平滑段更自由。
```

<!--
讲解备注：
不是所有点都应该同样自由。自车附近轨迹若突然横移，会造成控制抖动。

配图建议：
一条路径分成 fix、joint、smooth 三段，用不同颜色表示可动范围。
-->

---

# Fix 区域

fix 区域用于强约束关键点：

- 路径起点附近
- 局部路径包含目标点时的终点附近
- 需要与上一周期保持连续的位置

常见情况：

$$
c_{\text{fix}}=0
$$

则：

$$
l_i=0
$$

<!--
讲解备注：
起点固定能避免自车附近轨迹突然跳动。终点固定能避免改变目标语义。

配图建议：
路径前端和末端几个点用锁图标标记。
-->

---

# Joint 区域

joint 区域是固定段和平滑段之间的过渡。

如果直接从：

```text
完全固定 -> 大范围可动
```

可能在边界产生新折角。

所以 joint 区域使用较小可动范围：

$$
0\le c_{\text{fix}}\le c_{\text{joint}}\le c_{\text{smooth}}
$$

<!--
讲解备注：
joint 区域让自由度逐渐释放，减少固定区域和自由区域的连接突变。

配图建议：
横向可动线段从短逐渐变长。
-->

---

# 公式慢读：三类可动范围

公式：

$$
0\le c_{\text{fix}}\le c_{\text{joint}}\le c_{\text{smooth}}
$$

它想表达：

```text
从固定区域到平滑区域，
路径点的移动自由度逐渐变大。
```

符号对应：

- $c_{\text{fix}}$：固定区域的可动范围，通常接近 0
- $c_{\text{joint}}$：过渡区域的可动范围
- $c_{\text{smooth}}$：主要平滑区域的可动范围

工程作用：

```text
避免从“完全不能动”突然跳到“大范围可动”，
减少新的连接折角。
```

<!--
讲解备注：
这页把 fix / joint / smooth 三段讲成自由度逐渐释放。
这样观众能理解 joint 区域不是多余的。

配图建议：
路径点横向滑轨长度从 0 逐渐变长。
-->

---

# Smooth 区域

smooth 区域是主要优化段：

$$
|l_i|\le c_{\text{smooth}}
$$

如果 $c_{\text{smooth}}$ 太小：

```text
优化器几乎没有空间消除折角
```

如果太大：

```text
路径可能偏离上游规划意图
```

<!--
讲解备注：
这里解释 clearance 参数的核心直觉。

配图建议：
小 clearance 平滑有限，大 clearance 偏离明显的对比图。
-->

---

# 完整问题的一句话

$$
\mathbf{p}_i=\mathbf{r}_i+l_i\mathbf{n}_i
$$

$$
\min_{\mathbf{l}}
\quad
w_s
\sum_{k=1}^{N-2}
\left\|
\mathbf{p}_{k+1}-2\mathbf{p}_k+\mathbf{p}_{k-1}
\right\|^2
+
w_l
\sum_{i=0}^{N-1}
l_i^2
$$

subject to：

$$
-c_i\le l_i\le c_i
$$

<!--
讲解备注：
这是最适合放在视频中间复盘的总公式。它比矩阵形式更直观。

配图建议：
公式旁边配一条路径，每个点有横向可动线段。
-->

---

# 公式慢读：完整问题一句话

这组公式可以这样读：

```text
每个点只能沿法向移动 l_i；
我们希望所有三点折角尽量小；
同时希望所有 l_i 不要太大；
并且每个 l_i 必须在允许范围内。
```

对应关系：

- $\mathbf{p}_i=\mathbf{r}_i+l_i\mathbf{n}_i$：变量如何生成路径点
- 二阶差分平方和：平滑目标
- $\sum l_i^2$：不要偏离原路径
- $-c_i\le l_i\le c_i$：横向可动范围

<!--
讲解备注：
这是把完整数学问题翻译成人话的复盘页。
建议在录制时停下来，让观众确认四个组成部分都懂了。

配图建议：
把完整公式分成四个彩色框，每个框连接到对应图示。
-->

---

# 矩阵形式再看一次

$$
\min_{\mathbf{l}}
\quad
\frac{1}{2}\mathbf{l}^TP\mathbf{l}
+
\mathbf{q}^T\mathbf{l}
$$

subject to：

$$
\mathbf{l}_{min}\le \mathbf{l}\le \mathbf{l}_{max}
$$

其中：

$$
P=w_sTP_0T^T+w_lI
$$

$$
\mathbf{q}=w_sTP_0\mathbf{x}
$$

<!--
讲解备注：
这是给求解器的形式。前一页是人类容易理解的形式，这一页是求解器容易吃的形式。

配图建议：
“人类几何形式”箭头指向“求解器矩阵形式”。
-->

---

# 过渡：数学问题已经完成，接下来是在线工程

到这里，核心 QP 已经讲完：

```text
变量：l
目标：平滑 + 横向正则
约束：box constraint
```

但真实在线模块还要考虑：

- 每次优化多长一段路径
- 点距是否均匀
- 点数是否固定
- 自车附近是否稳定
- 求解失败如何回退

<!--
讲解备注：
这一页把“算法推导”和“工程运行”隔开。
新手容易以为 QP 公式写完就结束，实际上在线系统还要处理很多稳定性细节。

配图建议：
左侧 QP 公式，右侧在线运行齿轮：裁剪、重采样、固定点、回退。
-->

---

# 在线运行为什么还要裁剪

平滑器通常只处理自车附近一段路径：

$$
L_{\text{forward}}
=
N\Delta s
$$

原因：

- 没必要优化整条全局路径
- 计算量更可控
- 只关注近期会被下游使用的部分
- 后方保留少量路径保证连续

<!--
讲解备注：
在线规划不等于每次处理全局路线。局部裁剪是实时性的基础。

配图建议：
长路径中截取自车前方一段优化窗口。
-->

---

# 公式慢读：优化窗口长度

公式：

$$
L_{\text{forward}}
=
N\Delta s
$$

它想表达：

```text
优化点数乘以点间距，
大致就是本次向前优化的路径长度。
```

符号对应：

- $N$：优化点数量
- $\Delta s$：相邻点的弧长间隔
- $L_{\text{forward}}$：前向优化窗口长度

工程作用：

```text
控制每一帧优化的范围和计算量。
```

<!--
讲解备注：
这页把 num_points 和 delta_arc_length 连接起来。
更长窗口看得更远，但计算更多；更短窗口响应快，但可能不够稳定。

配图建议：
自车前方截取一段窗口，标出 N 个点和 delta_s。
-->

---

# 为什么要等弧长重采样

二阶差分默认点间距比较均匀。

如果点距忽大忽小：

```text
同样的二阶差分数值，
不再代表同样的几何弯折。
```

所以通常使用：

$$
\Delta s=\text{constant}
$$

让平滑代价更稳定。

<!--
讲解备注：
这页解释重采样不是为了好看，而是为了让优化模型的物理含义稳定。

配图建议：
点距不均匀和点距均匀的路径对比。
-->

---

# 常见疑问：为什么点距不均会影响平滑

二阶差分默认每个点之间距离差不多。

如果点距不均匀：

```text
密集区域的小折角会被过度关注；
稀疏区域的大形状变化可能被低估。
```

所以等弧长重采样是在保证：

```text
每个二阶差分项有相近的几何尺度。
```

<!--
讲解备注：
这页解释重采样的物理意义。
不是为了点看起来整齐，而是为了二阶差分代价公平。

配图建议：
同一曲线用不均匀点和均匀点采样，标出三点窗口大小不同。
-->

---

# 为什么要补齐点数

在线优化器喜欢固定维度。

如果局部路径点数不足 $N$：

```text
用末尾点补齐
```

好处：

- 变量维度固定
- Hessian 和约束矩阵维度稳定
- 便于 warm start
- 接近目标时求解流程不突变

<!--
讲解备注：
补齐点不代表真实路径变长，最终输出时仍使用真实路径对应部分。

配图建议：
短路径末尾重复点补齐到固定长度。
-->

---

# 为什么插入固定点

在线规划每一帧都重新运行。

如果前端每次都重新优化：

```text
自车附近轨迹可能轻微跳动
```

固定前端点相当于告诉优化器：

```text
车前很近的轨迹不要随意改变。
```

这对方向盘稳定性很重要。

<!--
讲解备注：
这页把 fix 区域和在线稳定联系起来。

配图建议：
相邻两帧轨迹前端重合，后方平滑段可以变化。
-->

---

# 求解后如何回到路径

QP 求出：

$$
\mathbf{l}^*
=
\begin{bmatrix}
l_0^*&l_1^*&\cdots&l_{N-1}^*
\end{bmatrix}^T
$$

每个点：

$$
\mathbf{p}_i^*
=
\mathbf{r}_i+l_i^*\mathbf{n}_i
$$

坐标：

$$
x_i^*=x_i-\sin\theta_i\,l_i^*
$$

$$
y_i^*=y_i+\cos\theta_i\,l_i^*
$$

<!--
讲解备注：
求解器输出的不是路径点，而是一串横向偏移。最后还要映射回 x,y 点列。

配图建议：
l_i 输出箭头指向新的路径点位置。
-->

---

# 航向角需要重新计算

点的位置变了，旧航向角未必还正确。

因此输出路径通常需要：

```text
根据平滑后的点列重新计算 yaw
```

Elastic Band 改的是：

```text
路径几何形状
```

不是：

```text
速度曲线
```

<!--
讲解备注：
路径平滑后，切向方向也会变化。速度不是 Elastic Band 的优化对象，后面会交给 Velocity Smoother。

配图建议：
平滑前后同一点附近的切向箭头变化。
-->

---

# 失败验证和回退

在线系统不能只看求解器是否返回结果。

常见验证：

$$
\max_i |l_i^*|
\le
l_{\text{valid}}
$$

如果：

- 求解失败
- 输出异常数值
- 横向偏移过大

就回退到上一轮成功结果或原始路径。

<!--
讲解备注：
安全系统里，稳定回退比发布突兀的新解更重要。

配图建议：
异常平滑结果被打叉，系统回退到上一条稳定路径。
-->

---

# 常见疑问：为什么求解成功还要验证

因为求解器成功只说明：

```text
数学问题找到了一个解
```

但工程上还要确认：

- 数值没有异常
- 偏移没有过大
- 路径没有突然跳变
- 输出仍然适合下游模块

所以在线模块通常会：

```text
验证 -> 通过才发布 -> 失败则回退
```

<!--
讲解备注：
这页强调自动驾驶软件不能盲目信任单个求解结果。
稳定回退是在线系统的一部分。

配图建议：
QP 输出经过 validator，失败时回退到 previous path。
-->

---

# 参数：离散化

`num_points`：

```text
优化点数量 N
```

`delta_arc_length`：

```text
点间弧长间隔 Δs
```

前向优化长度近似：

$$
L_{\text{forward}}
=
N\Delta s
$$

<!--
讲解备注：
N 越大范围越长但计算量更大；delta_s 越小越细但变量更多。

配图建议：
同一路径不同采样间隔和优化窗口长度对比。
-->

---

# 公式慢读：离散化参数

公式：

$$
L_{\text{forward}}
=
N\Delta s
$$

它想表达：

```text
优化窗口长度由点数和点间距共同决定。
```

调参直觉：

- $N$ 增大：看得更远，但变量更多
- $\Delta s$ 变小：路径更细，但点数压力更大
- $L_{\text{forward}}$ 过短：可能只修局部，不够稳定
- $L_{\text{forward}}$ 过长：计算量变大

<!--
讲解备注：
这页把参数和实际优化范围联系起来。
不要单独讲 num_points，要和 delta_arc_length 一起讲。

配图建议：
同一条路径，展示小 N、大 N、小 delta_s 的窗口变化。
-->

---

# 参数：权重

`smooth_weight` 对应：

$$
w_s
$$

越大：

```text
越追求降低折角和锯齿
```

`lat_error_weight` 对应：

$$
w_l
$$

越大：

```text
越贴近原始路径
```

<!--
讲解备注：
两个权重是平滑和保守之间的跷跷板。

配图建议：
跷跷板：左边 smooth_weight，右边 lat_error_weight。
-->

---

# 公式慢读：权重跷跷板

平滑权重：

$$
w_s
$$

贴近原路径权重：

$$
w_l
$$

它们不是单独起作用，而是形成比例：

```text
w_s 相对更大 -> 更愿意变平滑
w_l 相对更大 -> 更愿意贴住原路径
```

工程作用：

```text
调权重就是在“光顺”和“保守”之间找平衡。
```

<!--
讲解备注：
这页强调看比例。新手调参容易只盯单个数值，其实两个权重的相对大小更关键。

配图建议：
跷跷板两端分别是 smooth 和 keep original。
-->

---

# 权重比例的直觉

如果：

$$
\frac{w_s}{w_l}
\text{ 较大}
$$

结果更平滑，但可能偏离更多。

如果：

$$
\frac{w_l}{w_s}
\text{ 较大}
$$

结果更贴原路径，但平滑效果有限。

<!--
讲解备注：
调参时看比例比单独看某一个权重更有意义。

配图建议：
三条结果：原路径、偏平滑、偏保守。
-->

---

# 参数：clearance

`clearance_for_fix`：

$$
c_{\text{fix}}
$$

`clearance_for_joint`：

$$
c_{\text{joint}}
$$

`clearance_for_smooth`：

$$
c_{\text{smooth}}
$$

它们本质上定义：

$$
-c_i\le l_i\le c_i
$$

<!--
讲解备注：
clearance 不是障碍物距离约束，而是每个点的横向可动范围。

配图建议：
fix、joint、smooth 三段横向可动线段长度不同。
-->

---

# 公式慢读：clearance 参数

公式：

$$
-c_i\le l_i\le c_i
$$

在参数里表现为：

```text
clearance_for_fix
clearance_for_joint
clearance_for_smooth
```

它想表达：

```text
不同区域给不同长度的横向滑轨。
```

工程作用：

```text
既允许中间区域平滑，
又避免自车附近或关键点附近突然变化。
```

<!--
讲解备注：
再次提醒 clearance 不是离障碍物多远，而是点可以横向移动多远。

配图建议：
fix/joint/smooth 三段点的横向滑轨逐渐变长。
-->

---

# 参数：求解器和在线稳定

求解器参数：

- `max_iteration`
- `eps_abs`
- `eps_rel`

在线稳定参数：

- `enable_warm_start`
- `enable_optimization_validation`
- `validation.max_error`

直觉：

```text
精度越严格，可能更准，但计算更慢。
warm start 能让相邻周期更稳定。
```

<!--
讲解备注：
这里不讲具体求解器实现，只讲参数影响。

配图建议：
实时性和精度的权衡图。
-->

---

# 它为什么帮助 Frenet / MPT

Frenet 坐标系依赖参考路径：

$$
\mathbf{t}_i=
\begin{bmatrix}
\cos\theta_i\\
\sin\theta_i
\end{bmatrix},
\quad
\mathbf{n}_i=
\begin{bmatrix}
-\sin\theta_i\\
\cos\theta_i
\end{bmatrix}
$$

如果路径锯齿：

```text
theta、t、n、曲率、横向误差都会抖
```

平滑路径能让后续误差模型更稳定。

<!--
讲解备注：
这页把 Elastic Band 和后续 MPT/MPC 联系起来。它改善的是后续模块的输入条件。

配图建议：
锯齿参考线导致 Frenet 法向量跳变；平滑后法向量稳定。
-->

---

# 过渡：回到整套链路

Elastic Band 本身只做几何平滑。

但它的价值体现在下游：

```text
更稳定的切向 / 法向
更稳定的曲率估计
更稳定的横向误差
更稳定的轨迹优化输入
更稳定的控制参考
```

所以它不是“让图看起来更顺”，而是：

```text
让后面的数学模型少被参考线噪声干扰。
```

<!--
讲解备注：
这页把本集从 QP 推导拉回工程意义。
观众需要知道为什么花这么多时间讲路径几何质量。

配图建议：
Elastic Band 输出箭头指向 Frenet、MPT、Controller，旁边标稳定输入。
-->

---

# 它不做碰撞检查

Elastic Band 的约束只有：

$$
-c_i\le l_i\le c_i
$$

这里没有：

- 障碍物距离约束
- 车辆 footprint 约束
- 道路边界完整覆盖约束
- 动态对象预测约束

所以它不保证 collision-free。

<!--
讲解备注：
这是最重要的职责边界之一。Elastic Band 输出更平滑路径，但不等价于安全避障路径。

配图建议：
平滑路径可能更靠近障碍物，用警告标识说明后续仍需检查。
-->

---

# 常见疑问：为什么不把碰撞也加进 QP

可以做，但那会变成更复杂的问题。

Elastic Band 选择保持轻量：

```text
只做局部几何平滑
只使用简单 box constraint
不引入复杂障碍物约束
```

好处：

- 求解快
- 结构稳定
- 容易回退
- 不改变模块职责边界

代价：

```text
平滑结果仍需要后续模块验证安全性。
```

<!--
讲解备注：
这页解释设计取舍，不是说碰撞约束不重要，而是它通常交给更适合的后续模块。

配图建议：
左边轻量 EB，右边复杂带障碍物约束优化器，标出速度和职责差异。
-->

---

# 职责边界

Elastic Band 负责：

```text
轻量几何平滑
```

后续模块负责：

- 可行驶区域检查
- 轨迹优化
- 碰撞相关约束
- 速度规划
- 控制跟踪

职责拆开后，Elastic Band 才能保持简单、稳定、快速。

<!--
讲解备注：
不要把所有安全要求塞进 Elastic Band。不同模块分工才能在线稳定运行。

配图建议：
模块分工图：Elastic Band -> MPT -> Velocity Smoother -> Controller。
-->

---

# 优点

Elastic Band 的优点：

- 目标是二次函数，结构清晰
- 约束是简单 box constraint
- 变量只有 $N$ 个横向偏移
- Hessian 稀疏带状，适合在线求解
- 小幅调整，不容易破坏路径拓扑
- 失败时容易回退

<!--
讲解备注：
这些优点来自它的保守设计。简单不是缺点，而是为了在线可靠。

配图建议：
六个优点卡片。
-->

---

# 局限

Elastic Band 的局限：

- 不负责避障
- 不保证 collision-free
- 不能处理大尺度拓扑变化
- 依赖上游参考路径质量
- 横向可动范围太小时效果有限
- 不是车辆动力学优化

<!--
讲解备注：
这页和优点配套。告诉观众什么时候不该指望 Elastic Band 解决问题。

配图建议：
能力边界框，框内是几何平滑，框外是避障/动力学/速度。
-->

---

# 从零理解的七步

1. 路径是离散点列
2. 平滑是减少相邻线段方向变化
3. 二阶差分衡量局部折角
4. 二阶差分平方和变成矩阵二次型
5. Autoware 优化横向偏移 $l_i$
6. 平滑项和横向正则项组成 QP 目标
7. 每个 $l_i$ 有上下界 box constraint

<!--
讲解备注：
这是学习路线复盘。适合视频后半段给新手整理思路。

配图建议：
七级阶梯图。
-->

---

# 学到这里应该形成的心智模型

不要把 Elastic Band 记成“一个神秘 QP”。

把它记成六个问题：

```text
输入是什么？       一串离散路径点 r_i
平滑怎么量化？     三点二阶差分
变量是什么？       每个点横向偏移 l_i
为什么不乱跑？     box constraint 限制 l_i
目标是什么？       平滑 + 不偏离原路径
输出是什么？       p_i = r_i + l_i n_i
```

<!--
讲解备注：
这是总结前的学习检查页。
观众如果能复述这六个问题，就已经掌握了本集主干。

配图建议：
六个问题围成一圈，中间写 Elastic Band。
-->

---

# 本集总结图

```text
Raw discrete path r_i
  -> local normal n_i
  -> variable l_i
  -> p_i = r_i + l_i n_i
  -> minimize second difference + lateral offset
  -> -c_i <= l_i <= c_i
  -> smoothed path p_i*
```

<!--
讲解备注：
这一页把整集内容压缩成一条链。建议慢讲。

配图建议：
流程图上每一步配一个小几何示意。
-->

---

# 三个必须记住的公式

平滑目标：

$$
\sum_{k=1}^{N-2}
\left\|
\mathbf{p}_{k+1}-2\mathbf{p}_k+\mathbf{p}_{k-1}
\right\|^2
$$

横向偏移：

$$
\mathbf{p}_i=\mathbf{r}_i+l_i\mathbf{n}_i
$$

QP：

$$
\min_{\mathbf{l}}
\frac{1}{2}\mathbf{l}^TP\mathbf{l}+\mathbf{q}^T\mathbf{l}
\quad
\text{s.t. }
-c_i\le l_i\le c_i
$$

<!--
讲解备注：
如果观众只记三个公式，就记这三个。它们分别对应平滑、变量和优化形式。

配图建议：
三个公式卡片。
-->

---

# 常见误区

1. Elastic Band 是完整避障规划器
2. 平滑越强越好
3. 可以随意移动路径点
4. 直接优化 x,y 比优化横向偏移更好
5. 求解器输出的就是最终路径点
6. 平滑后不需要再做可行性检查

<!--
讲解备注：
逐条澄清。尤其强调“平滑不等于安全”“横向偏移变量是保守设计”。

配图建议：
误区 / 正确理解两列表。
-->

---

# 课后思考

1. 为什么二阶差分能表示路径折角？
2. 为什么只最小化二阶差分会过度拉直路径？
3. 为什么 Autoware 选择优化横向偏移 $l_i$？
4. box constraint 在这个问题里限制了什么？
5. 为什么 Elastic Band 不保证 collision-free？

<!--
讲解备注：
这些问题可以放到视频评论区，引导观众复盘核心概念。

配图建议：
纯文字页即可。
-->

---

# 下一集预告

## Velocity Smoother

下一集会讲：

1. 为什么路径有了还不能直接开
2. 速度、加速度、jerk 分别约束什么
3. 曲率限速为什么来自横向加速度
4. 为什么速度上限不能硬裁剪
5. 如何生成更平滑、更可执行的速度曲线

<!--
讲解备注：
自然引到第 5 集。Elastic Band 改的是几何形状，下一步还要决定每个点开多快。

配图建议：
同一条平滑路径上用颜色表示速度，弯道和停车点前速度降低。
-->

---

# 下一次给 Codex 的指令

继续优化课程时，直接输入：

```text
请执行优化任务 6：
在不覆盖原文件的前提下，
根据 AUTOWARE规划控制层课程总索引.md、
AUTOWARE规划控制层第5集_VelocitySmoother速度平滑PPT.md、
concept/velocity_smoother_blog.md、
concept/从速度上限到 Jerk 解析减速：Autoware Velocity Smoother 的完整数学链条.md 和 concept/jerk.md，
生成 AUTOWARE规划控制层第5集_VelocitySmoother速度平滑PPT_新手友好详解版.md。

要求：
1. 不精简内容，不删除关键知识点；
2. 在原 PPT 主线基础上增强新手可理解性；
3. 每个核心概念第一次出现时补充通俗解释；
4. 每个公式都补充：物理意义、符号解释、工程作用；
5. 每个重要模块都说明：输入、输出、为什么需要它；
6. 增加必要的过渡页、类比页、常见疑问页；
7. 每页仍包含页面展示内容、讲解备注、配图建议；
8. 输出到项目根目录，不覆盖原文件；
9. 任务完成后，给出下一次任务的指令。
```

<!--
讲解备注：
这一页不一定放进公开视频里，它是给你自己继续制作课程用的。
如果正式录课，可以删除这一页。

配图建议：
不需要配图。
-->
