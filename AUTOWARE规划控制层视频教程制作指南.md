# Autoware 规划与控制层视频教程制作总指南

这份文档的目标是帮你把已经写好的 `concept/` 文章，整理成一套可以真正录制的视频教程。你现在不是缺内容，而是缺一条清晰的讲课主线、每一集的脚本结构、画图思路、录制顺序和后续打磨方法。

本教程建议定位为：

```text
从目标点到方向盘：用原理讲透 Autoware 规划与控制层
```

核心风格是：

```text
场景问题 -> 数学模型 -> Autoware 工程实现 -> 参数直觉 -> 常见误区
```

不要把视频做成“逐行读代码”。对新手来说，逐行读代码很容易迷路。更好的方式是先讲清楚模块要解决什么问题，再讲数学模型为什么长这样，最后用代码证明 Autoware 的实现确实是在做这件事。

---

# 1. 你现在手里的素材怎么用

你已经写好的文章可以分成 7 组：

| 已有文档 | 建议视频主题 | 在课程里的作用 |
|---|---|---|
| `concept/mission_planner_route_planning_blog.md` | 车道级路线规划 | 讲清楚 route 是怎么从目标点和 Lanelet 地图中生成的 |
| `concept/hybrid_astar_freespace_pull_out_algorithm_blog.md` | 自由空间脱困与 Hybrid A* | 讲车辆不在车道中心时，如何搜索一条能开回车道的路径 |
| `concept/autoware_elastic_band_smoothing_blog.md` | Elastic Band 路径平滑 | 讲离散路径如何通过 QP 变得更光顺 |
| `concept/velocity_smoother_blog.md` | Velocity Smoother | 讲速度、加速度、jerk 约束如何形成可执行速度曲线 |
| `concept/pure_pursuit_blog.md` | Pure Pursuit 横向控制 | 讲最直观的几何路径跟踪控制 |
| `concept/从阿克曼几何到 QP 求解器输入：自动驾驶横向 MPC 的完整数学链条.md` | 横向 MPC 数学链条 | 讲阿克曼、自行车模型、Frenet、线性化、离散化、QP |
| `concept/autoware_mpt_blog.md` | MPT 轨迹优化 | 讲控制前的轨迹优化、约束、避障、warm start |

建议你不要按“我写了哪篇文章就录哪集”的方式来录。更好的课程顺序是按自动驾驶数据流来排：

```text
目标点
  -> 地图车道图
  -> Route
  -> 可行驶走廊
  -> 局部路径
  -> 平滑路径
  -> 带速度轨迹
  -> 横向/纵向控制
  -> 车辆执行器
```

这样观众会一直知道“我现在学的这个模块，在整辆车里处于什么位置”。

---

# 2. 这套教程应该讲给谁听

建议你假设观众是：

- 会一点 C++ 或 Python，但不一定读过 Autoware 源码。
- 知道自动驾驶大概分感知、定位、规划、控制，但不知道每层具体怎么接。
- 学过一点线性代数、微积分、运动学，但公式不熟。
- 对 ROS2、Lanelet2、QP、MPC 这些词有印象，但没有建立系统理解。
- 想知道 Autoware 代码背后的原理，而不是只会改参数。

所以讲课时要遵守一个原则：

```text
先给直觉，再给公式；先给输入输出，再给内部细节；先讲为什么，再讲怎么做。
```

每一集开头都要回答三个问题：

1. 这一集解决什么问题？
2. 如果没有这个模块，车会出现什么现象？
3. 这个模块的输入和输出分别是什么？

只要这三个问题讲清楚，新手就不容易掉队。

---

# 3. 课程总标题和分集建议

建议整套课程分成 15 集左右。你也可以先做一个 8 集精简版，后面再扩展。

## 3.1 精简版 8 集

适合先快速做出一套完整课程。

| 集数 | 标题 | 重点 |
|---|---|---|
| 第 1 集 | Autoware 规划控制总览：从目标点到控制指令 | 建立全局地图 |
| 第 2 集 | Lanelet 与 Mission Planner：路线是怎么生成的 | Route、Lanelet 图搜索 |
| 第 3 集 | Hybrid A*：车辆如何从自由空间回到车道 | 状态搜索、碰撞、代价 |
| 第 4 集 | Elastic Band：为什么路径需要平滑 | 二阶差分、横向偏移、QP |
| 第 5 集 | Velocity Smoother：速度曲线为什么不能硬裁剪 | 速度、加速度、jerk |
| 第 6 集 | Pure Pursuit：用几何直觉理解横向控制 | 前瞻点、圆弧、曲率 |
| 第 7 集 | 横向 MPC：从阿克曼几何到 QP | Frenet、线性化、预测模型 |
| 第 8 集 | MPT 与整套链路复盘：轨迹优化如何连接规划和控制 | 轨迹优化、边界、参数 |

## 3.2 完整版 15 集

适合更系统、更适合长期更新。

| 集数 | 标题 | 建议时长 | 对应素材 |
|---|---|---:|---|
| 第 0 集 | 课程导学：应该如何学习 Autoware 规划控制 | 15 分钟 | 本文档 |
| 第 1 集 | 规划控制总览：Route、Path、Trajectory、Control 的区别 | 30 分钟 | 多篇综合 |
| 第 2 集 | Lanelet 地图基础：道路为什么要建模成车道图 | 35 分钟 | `lanelet.md`、路线规划文档 |
| 第 3 集 | Mission Planner 原理：从起点终点到车道级路线 | 45 分钟 | `mission_planner_route_planning_blog.md` |
| 第 4 集 | Route Section 与可行驶走廊：为什么输出不是一条线 | 35 分钟 | `mission_planner_route_planning_blog.md` |
| 第 5 集 | 行驶中重规划：为什么不能随便换一条新路线 | 30 分钟 | `mission_planner_route_planning_blog.md` |
| 第 6 集 | Start Planner 与 Pull Out 问题：车不在车道中心怎么办 | 35 分钟 | Hybrid A* 文档 |
| 第 7 集 | Hybrid A* 搜索原理：状态、运动原语、代价函数 | 50 分钟 | Hybrid A* 文档 |
| 第 8 集 | Elastic Band 路径平滑：从离散点到 QP | 45 分钟 | Elastic Band 文档 |
| 第 9 集 | MPT 轨迹优化上：局部参考线、车辆模型、预测模型 | 50 分钟 | MPT 文档 |
| 第 10 集 | MPT 轨迹优化下：边界、软约束、warm start | 50 分钟 | MPT 文档 |
| 第 11 集 | Velocity Smoother 上：速度上限、曲率限速、重采样 | 45 分钟 | Velocity Smoother 文档 |
| 第 12 集 | Velocity Smoother 下：jerk 受限减速曲线 | 50 分钟 | Velocity Smoother 文档 |
| 第 13 集 | Pure Pursuit：从前瞻点到方向盘转角 | 40 分钟 | Pure Pursuit 文档 |
| 第 14 集 | 横向 MPC 上：阿克曼、自行车模型、Frenet 误差 | 50 分钟 | MPC 数学链条 |
| 第 15 集 | 横向 MPC 下：线性化、离散化、QP 求解器输入 | 60 分钟 | MPC 数学链条 |
| 第 16 集 | 总复盘：一条轨迹如何变成可执行控制命令 | 35 分钟 | 全部素材 |

如果你是第一次录课，建议先做精简版 8 集。完成之后，再把每集拆成完整版。

---

# 4. 整套课的主线图

你的视频应该反复出现这张主线图：

```text
用户目标点 / 任务请求
        |
        v
Mission Planner
  输入: 当前位姿、目标位姿、Lanelet 地图
  输出: Route、Route Sections、可行驶车道集合
        |
        v
Behavior / Start / Pull Out Planning
  输入: Route、当前状态、场景条件、障碍物或占据栅格
  输出: 行为决策、局部路径候选
        |
        v
Path / Trajectory Optimization
  输入: 局部路径、可行驶区域、边界、障碍物约束
  输出: 更平滑、更可跟踪的路径或轨迹
        |
        v
Velocity Smoother
  输入: 几何轨迹、速度上限、停车点、车辆当前速度加速度
  输出: 满足速度/加速度/jerk 的时空轨迹
        |
        v
Controller
  输入: 当前车辆状态、目标轨迹
  输出: 转向、加速度、制动等控制命令
        |
        v
Vehicle Interface
  输入: 控制命令
  输出: 底盘执行
```

每讲一个模块，都要把它放回这张图里。新手最怕的是学了很多孤立算法，却不知道它们之间怎么接。

---

# 5. 每一集都可以套用的脚本模板

以后你写每一集视频脚本，可以直接套这个结构：

```text
1. 开场问题
   今天我们解决一个具体问题：xxx。

2. 直觉场景
   如果没有这个模块，车辆会发生什么？例如抖动、急刹、转向发散、路线不合法。

3. 输入输出
   输入有哪些？输出是什么？上下游是谁？

4. 最小数学模型
   用最小的公式解释核心原理。

5. 工程增强
   Autoware 为什么还要加这些处理？例如重采样、限幅、坐标变换、状态机、fallback。

6. 参数直觉
   哪些参数最关键？调大调小会怎样？

7. 常见误区
   新手最容易误解哪里？

8. 小结
   用一张图或三句话总结本集。
```

每集不要一上来就放代码。推荐顺序是：

```text
问题画面 -> 抽象模型 -> 公式推导 -> 流程图 -> 代码位置 -> 参数解释
```

代码只负责验证你讲的原理，不要让代码抢走主线。

---

# 6. 第 0 集：课程导学

## 6.1 这一集解决什么问题

新手第一次看 Autoware，通常会遇到这些困难：

- 包很多，不知道从哪里开始。
- 规划和控制边界不清楚。
- route、path、trajectory 混在一起。
- 公式很多，不知道哪些是核心。
- 代码里有大量工程处理，不知道哪些是主干、哪些是保护逻辑。

导学集的任务不是讲具体算法，而是给观众一张地图。

## 6.2 开场可以这样说

```text
很多人学习 Autoware 规划控制时，第一反应是去翻源码。
但源码里的模块、消息、参数、状态机非常多，如果没有主线，很快就会迷路。

这套课不会从某一个 C++ 文件开始，而是从一个更自然的问题开始：
给自动驾驶车一个目标点，它最后是怎么变成方向盘和速度控制命令的？
```

## 6.3 本集要讲的三件事

1. Autoware 规划控制层的整体数据流。
2. Route、Path、Trajectory、Control Command 的区别。
3. 后续每一集会分别解决链路上的哪个问题。

## 6.4 必画图

画一条从左到右的数据流：

```text
目标点 -> Route -> Path -> Trajectory -> Control Command
```

然后在每个箭头下面写：

- Route: 经过哪些车道。
- Path: 在车道里具体走哪条几何线。
- Trajectory: 每个点带速度和时间含义。
- Control Command: 方向盘、油门、刹车等底盘可执行命令。

## 6.5 这一集不要讲太多

导学集控制在 15 分钟以内。它的作用是降低学习焦虑，不是提前讲完所有细节。

---

# 7. 第 1 集：规划控制总览

## 7.1 核心问题

```text
自动驾驶为什么要分路线规划、路径规划、速度规划和控制？
```

## 7.2 新手最需要建立的概念

路线、路径、轨迹、控制命令不是同一个东西。

| 名称 | 回答的问题 | 典型输出 |
|---|---|---|
| Route | 应该经过哪些车道 | lanelet 序列、route sections |
| Path | 在可行驶区域内走哪条线 | 几何路径点 |
| Trajectory | 每个位置应该以多快速度通过 | 带速度的轨迹点 |
| Control | 当前应该打多少方向、给多少加速度 | steering、acceleration |

建议用一个生活类比：

```text
Route 像导航告诉你走哪几条路。
Path 像你在当前道路里具体压哪条线。
Trajectory 像你不只知道走哪条线，还知道每一段开多快。
Control 像你此刻手和脚具体怎么操作。
```

## 7.3 本集脚本结构

### 0 到 5 分钟：从目标点开始

先给一个简单场景：

```text
车辆在停车场出口，目标点在前方某条道路上。
系统不能直接从当前位置画一条直线开过去，因为直线可能穿过隔离带、逆行车道或障碍物。
```

引出路线规划。

### 5 到 12 分钟：从 Route 到 Path

Route 只告诉车“应该走哪些车道”，但不会告诉车在车道内具体怎么绕障、怎么靠边、怎么换道。因此需要后续局部规划。

### 12 到 20 分钟：从 Path 到 Trajectory

一条几何线还不能直接开。车辆还需要速度。尤其是：

- 弯道要降速。
- 停车点前要提前减速。
- 限速变化不能瞬间生效。
- 加速度和 jerk 不能太大。

引出 Velocity Smoother。

### 20 到 30 分钟：从 Trajectory 到 Control

控制器根据当前车辆状态和目标轨迹，输出当前时刻的控制命令。这里可以先简单对比：

- Pure Pursuit 是几何控制。
- MPC 是优化控制。

不要在这一集展开公式，只要告诉观众后面会讲。

## 7.4 本集小结

本集最后用一句话收束：

```text
规划层负责把“我要去哪”变成“未来一段路怎么走、怎么快慢变化”，控制层负责把这条轨迹变成车辆当前可执行的动作。
```

---

# 8. 第 2 集：Lanelet 地图基础

## 8.1 核心问题

```text
为什么自动驾驶路线规划不是在普通图片地图上找路，而是在 Lanelet 图上找路？
```

## 8.2 要讲清楚的概念

Lanelet 可以理解为一个有方向、有边界、有属性的车道片段。

一个 lanelet 至少包含：

- 左边界。
- 右边界。
- 中心线。
- 方向。
- 交通规则属性。
- 与其他 lanelet 的拓扑关系。

你可以用这个抽象公式：

$$
\ell_i = \left(B_i^L, B_i^R, C_i, A_i\right)
$$

其中：

- $B_i^L$ 是左边界。
- $B_i^R$ 是右边界。
- $C_i$ 是中心线。
- $A_i$ 是属性集合。

## 8.3 这一集的讲法

先不要讲复杂代码，先画一条道路：

```text
左边界       中心线       右边界
  |-----------|-----------|
```

然后解释：

```text
路线规划真正搜索的不是一个个 OSM node，而是一个个 lanelet。
```

OSM node 只是几何点。路线图里的节点通常是 lanelet。

## 8.4 必讲误区

### 误区 1：OSM node 就是路线搜索里的图节点

不是。OSM node 是地图文件里的点，路线搜索图里的节点通常是车道片段。

### 误区 2：两个车道挨着就一定能换道

不是。能不能换道取决于交通规则、边界线型、方向和 lanelet 关系。

### 误区 3：中心线就是车辆最终轨迹

不是。中心线只是参考。真正轨迹还要经过行为规划、路径平滑、避障、速度规划和控制。

## 8.5 这一集可以引用的已有文档

- `concept/lanelet.md`
- `concept/mission_planner_route_planning_blog.md`
- `concept/osm文件讲解.md`
- `concept/车道的方向性.md`

---

# 9. 第 3 集：Mission Planner 原理

## 9.1 核心问题

```text
给定车辆当前位姿和目标位姿，Autoware 如何生成一条车道级路线？
```

## 9.2 输入输出

输入：

- 当前车辆位姿。
- 目标位姿。
- 可选途经点。
- Lanelet 地图。
- 交通规则和不可通行车道信息。

输出：

- 一条或多段 lanelet 路线。
- route sections。
- 可行驶走廊。
- 路线状态。

## 9.3 讲解主线

建议按这条链讲：

```text
目标点
  -> 坐标系统一
  -> 找起点候选 lanelet
  -> 找目标候选 lanelet
  -> 在 lanelet 图上搜索最短路
  -> 处理途经点
  -> 扩展相邻可行驶 lanelet
  -> 输出 route sections
```

## 9.4 关键公式和直觉

### 车道图

$$
G=(V,E)
$$

其中：

- $V$ 是所有可通行 lanelet。
- $E$ 是 lanelet 之间允许通行或换道的关系。

### 路线搜索

路线规划就是在图上找：

$$
\ell_{\mathrm{start}} \rightarrow \ell_1 \rightarrow \cdots \rightarrow \ell_{\mathrm{goal}}
$$

总代价通常可以理解为：

$$
J = \sum_i \operatorname{cost}(\ell_i, \ell_{i+1})
$$

实际工程中，代价不一定只是距离，还可能包括换道、不可通行车道、交通规则等。

## 9.5 这一集要重点讲的工程细节

### 起点不能只看距离

车辆当前点离某个 lanelet 很近，不代表它就在这个 lanelet 上。还要看朝向。如果车辆方向和 lanelet 行驶方向相反，把它当成起点就会很危险。

### 目标点也不能只看位置

目标位姿要考虑：

- 点是否在可通行区域内。
- 朝向是否合理。
- 车辆轮廓是否能放进去。
- 高度是否需要贴合地图。

### 输出不是一条线

Mission Planner 输出的不是几何路径，而是车道级 route。后续模块会在这些车道范围内生成具体路径。

## 9.6 本集推荐画图

1. 一个由 lanelet 组成的有向图。
2. 起点和终点投影到 lanelet。
3. 图搜索得到一串 lanelet。
4. 从主路线扩展到相邻可行驶走廊。

## 9.7 常见误区

- 认为 Mission Planner 会避开动态障碍物。
- 认为 route 就是控制器要跟踪的轨迹。
- 认为目标点只要位置合法即可，姿态不重要。
- 认为重规划就是直接换一条新路线。

---

# 10. 第 4 集：Route Section 与可行驶走廊

## 10.1 核心问题

```text
为什么 Mission Planner 的输出不是一串孤立 lanelet，而是 route sections 和可行驶区域？
```

## 10.2 新手直觉

如果只输出“主车道序列”，下游规划器会不知道：

- 相邻车道是否可以借用。
- 换道时可行驶边界在哪里。
- 当前 route 的局部车道集合是什么。
- 不同 lanelet 在同一段道路上如何组织。

所以 Autoware 需要把路线组织成 route sections。

## 10.3 讲解主线

```text
主路线 lanelet
  -> 找左右相邻 lanelet
  -> 判断是否属于可行驶候选
  -> 组成 route section
  -> 下游在 route section 中生成路径
```

## 10.4 这集的重点不是公式

这一集更像工程结构课。要反复强调：

```text
Mission Planner 不是为了给控制器一条线，而是为了给下游规划器一个合法的道路搜索空间。
```

## 10.5 推荐小结

```text
Route 告诉系统应该经过哪里，route section 告诉系统在每一段路上有哪些车道可以作为局部规划空间。
```

---

# 11. 第 5 集：行驶中重规划

## 11.1 核心问题

```text
车辆已经在开了，为什么不能随便把旧路线替换成新路线？
```

## 11.2 直觉场景

假设车辆正在左车道行驶，系统突然收到一个新目标点。如果新路线立刻要求车辆切到右侧匝道，但车辆距离分岔口已经太近，就算路线在地图上合法，车辆也可能来不及安全执行。

## 11.3 要讲清楚的概念

重规划不是单纯的图搜索，还需要判断：

- 新旧路线是否有共同路段。
- 当前车辆到分歧点的距离是否足够。
- 以当前速度和减速度能力，是否可以安全调整。
- 新路线是否会导致突变行为。

## 11.4 安全距离直觉

可以先用最简单的停车距离讲：

$$
d = \frac{v^2}{2|a|}
$$

这不是 Autoware 所有重规划逻辑的完整表达，但它能帮助新手理解：

```text
速度越高，需要越长距离来安全改变计划。
```

## 11.5 这一集的价值

很多新手以为规划就是“算出一条路”。这一集要让他们明白：

```text
在线自动驾驶系统里的规划，还要考虑当前车辆能不能安全过渡到新计划。
```

---

# 12. 第 6 集：Start Planner 与 Pull Out 问题

## 12.1 核心问题

```text
如果车辆一开始不在正常车道中心线上，系统怎么让它回到 route 上？
```

## 12.2 典型场景

- 车停在路边停车位。
- 车停在停车场。
- 车身朝向和车道方向不一致。
- 车前方有障碍物，需要先倒一下再出去。

这类问题不能简单地沿车道中心线规划，因为车辆当前状态可能不在车道拓扑上。

## 12.3 为什么需要自由空间规划

Mission Planner 是在 lanelet 图上找路线，它假设车辆大体在道路拓扑中。

Pull Out 问题需要处理：

- 车辆当前实际位置。
- 车辆朝向。
- 占据栅格或障碍物。
- 车辆矩形轮廓。
- 最小转弯半径。
- 前进和倒车。

所以它更像是局部运动规划问题。

## 12.4 本集和下一集怎么分

第 6 集建议只讲“为什么需要 Pull Out”和整体流程，不要展开 Hybrid A* 细节。第 7 集再专门讲 Hybrid A*。

## 12.5 本集流程图

```text
当前车辆状态
  -> 判断是否需要 pull out
  -> 生成候选回归目标点
  -> 选择算法: shift / geometric / freespace
  -> 生成 pull out path
  -> 拼接到 route 上
```

## 12.6 本集常见误区

- 认为从路边起步就是简单并线。
- 认为车辆可以原地任意转向。
- 认为只要路径点不碰障碍物就安全。
- 认为自由空间规划可以代替所有车道级路线规划。

---

# 13. 第 7 集：Hybrid A* 搜索原理

## 13.1 核心问题

```text
为什么普通 A* 不够，Hybrid A* 如何让搜索结果符合车辆运动学？
```

## 13.2 普通 A* 的限制

普通栅格 A* 的状态通常是：

$$
(x,y)
$$

但车辆状态至少需要：

$$
(x,y,\psi)
$$

其中 $\psi$ 是车辆朝向。

同一个位置，如果车辆朝向不同，能不能继续开、会不会碰撞、能不能到目标，结果都可能完全不同。

## 13.3 Hybrid A* 的核心思想

Hybrid A* 把搜索状态扩展成：

$$
\mathbf{x} = (x,y,\psi,\mathrm{gear})
$$

每次扩展节点时，不是简单走到相邻格子，而是用车辆运动模型生成一小段可行驶轨迹。

## 13.4 运动原语

每个动作可以理解为：

```text
选择一个转角
选择前进或倒车
按照自行车模型向前积分一小段距离
得到下一个状态
```

自行车模型可以写成：

$$
\dot{x}=v\cos\psi
$$

$$
\dot{y}=v\sin\psi
$$

$$
\dot{\psi}=\frac{v}{L}\tan\delta
$$

其中：

- $v$ 是速度。
- $L$ 是轴距。
- $\delta$ 是前轮转角。

## 13.5 碰撞检查

车辆不是点。碰撞检查要用车辆矩形轮廓，或者用多个圆、多个采样点近似车身。

要讲清楚：

```text
Hybrid A* 搜出来的每个节点都必须代表一小段车辆真正能开出来的运动。
```

## 13.6 代价函数

Hybrid A* 不是只追求最短路径，还要追求“好开”。

常见代价包括：

- 路径长度代价。
- 转角代价。
- 转角变化代价。
- 前进/倒车切换代价。
- 离障碍物太近的代价。
- 目标附近横向对齐代价。

可以写成：

$$
f(n)=g(n)+h(n)
$$

其中：

- $g(n)$ 是从起点走到当前节点的实际代价。
- $h(n)$ 是从当前节点到目标的估计代价。

## 13.7 推荐画图

1. 普通 A* 在格子上走八邻域。
2. Hybrid A* 用弧线扩展节点。
3. 同一个格子中不同朝向对应不同状态。
4. 车辆矩形沿轨迹运动时的碰撞检查。

## 13.8 常见误区

- Hybrid A* 不是普通 A* 加一点平滑。
- 搜索状态必须包含朝向。
- 最短路径不一定是最好开的路径。
- 倒车不是失败，而是自由空间规划里的合法动作。

---

# 14. 第 8 集：Elastic Band 路径平滑

## 14.1 核心问题

```text
为什么上游给出的路径不能直接交给控制器？
```

## 14.2 直觉场景

上游路径可能来自：

- 车道中心线。
- 路径拼接。
- 避障绕行。
- 离散采样。
- 历史路径复用。

这些路径可能有轻微折角。折角对人眼看起来不明显，但会影响：

- 曲率计算。
- Frenet 投影。
- 横向误差。
- 控制器转角输出。

## 14.3 最小数学模型

一条离散路径：

$$
\mathbf{r}_0,\mathbf{r}_1,\dots,\mathbf{r}_{N-1}
$$

平滑的核心是让二阶差分变小：

$$
J_{\mathrm{smooth}}
=
\sum_{k=1}^{N-2}
\left\|
\mathbf{p}_{k+1}-2\mathbf{p}_k+\mathbf{p}_{k-1}
\right\|^2
$$

二阶差分的直觉是：

```text
前一段方向和后一段方向差多少。
```

差得越多，局部折角越大。

## 14.4 Autoware 的关键简化

Autoware Elastic Band 不是直接优化每个点的二维坐标，而是优化每个点的横向偏移：

$$
\mathbf{p}_i = \mathbf{r}_i + l_i \mathbf{n}_i
$$

其中：

- $\mathbf{r}_i$ 是原始点。
- $\mathbf{n}_i$ 是法向量。
- $l_i$ 是横向偏移。

优化变量从：

$$
(x_0,y_0,\dots,x_{N-1},y_{N-1})
$$

变成：

$$
(l_0,l_1,\dots,l_{N-1})
$$

这样做的好处：

- 变量更少。
- 点不会沿纵向乱滑。
- 路径顺序更稳定。
- 更容易加横向边界约束。

## 14.5 为什么是 QP

目标函数是二次型，约束通常是线性的横向边界：

$$
l_i^{\min} \le l_i \le l_i^{\max}
$$

所以可以形成标准 QP：

$$
\min_{\mathbf{l}} \frac{1}{2}\mathbf{l}^T H \mathbf{l} + \mathbf{g}^T \mathbf{l}
$$

$$
\text{s.t.}\quad \mathbf{l}_{\min} \le \mathbf{l} \le \mathbf{l}_{\max}
$$

## 14.6 本集推荐画图

1. 原始折线路径。
2. 每个点沿法向移动。
3. 二阶差分表示局部折角。
4. 横向移动边界限制每个点只能在短线段内移动。

## 14.7 参数直觉

- 平滑权重越大，路径越光顺，但可能更偏离原路径。
- 横向误差权重越大，路径越贴近原路径，但平滑效果减弱。
- clearance 越大，允许移动范围越保守。
- 固定点越多，路径越稳定，但可优化自由度越少。

## 14.8 常见误区

- Elastic Band 不是完整避障规划。
- 它不负责决定走哪条车道。
- 它不是传统弹簧斥力迭代法，而是 QP 平滑。
- 平滑不是越强越好，过度平滑可能破坏路径意图。

---

# 15. 第 9 集和第 10 集：MPT 轨迹优化

MPT 内容很长，建议拆成两集。

## 15.1 第 9 集核心问题

```text
为什么路径平滑之后，还需要轨迹优化？
```

## 15.2 第 9 集讲解主线

```text
原始参考路径
  -> 截取局部优化范围
  -> 构造参考线坐标
  -> 建立车辆模型
  -> 离散化
  -> 堆叠预测模型
  -> 设计轨迹跟踪和平滑代价
```

## 15.3 MPT 和 Elastic Band 的区别

| 模块 | 主要目标 | 典型变量 | 是否强依赖车辆模型 |
|---|---|---|---|
| Elastic Band | 几何路径平滑 | 横向偏移 $l_i$ | 较弱 |
| MPT | 生成更可跟踪、更满足边界的轨迹 | 状态、输入、横向位置等 | 更强 |

Elastic Band 更像在修几何线。MPT 更像在考虑车辆运动和约束后优化一段未来轨迹。

## 15.4 第 9 集关键公式

车辆误差状态可以抽象为：

$$
x_{k+1}=A_k x_k+B_k u_k+w_k
$$

把整个预测时域堆起来：

$$
X=A_{\mathrm{ex}}x_0+B_{\mathrm{ex}}U+W_{\mathrm{ex}}
$$

这个式子非常重要。它表达的是：

```text
未来整段轨迹都可以写成当前状态和未来控制输入的函数。
```

## 15.5 第 10 集核心问题

```text
MPT 如何把道路边界、车辆轮廓和连续性要求统一放进 QP？
```

## 15.6 第 10 集重点

- 为什么需要优化中心。
- 为什么车辆可以用多个圆近似。
- 边界约束如何从道路左右距离变成优化变量约束。
- 软约束为什么有必要。
- 固定点如何保证相邻帧轨迹连续。
- warm start 如何让在线优化更稳定。

## 15.7 MPT 的讲解难点

MPT 很容易讲散。建议你始终围绕一句话：

```text
MPT 是在局部可行驶区域内，寻找一条既接近参考、又平滑、又满足边界和车辆约束的优化轨迹。
```

每讲一个公式，都要解释它在这句话中对应哪个词：

- 接近参考。
- 平滑。
- 满足边界。
- 车辆约束。
- 在线稳定。

---

# 16. 第 11 集和第 12 集：Velocity Smoother

Velocity Smoother 建议拆成两集。第一集讲速度约束怎么来，第二集讲 jerk 受限解析减速。

## 16.1 第 11 集核心问题

```text
同一条几何路径，车辆应该以什么速度通过每个点？
```

## 16.2 输入输出

输入：

- 已经生成的几何轨迹。
- 轨迹点上的参考速度或速度上限。
- 外部限速。
- 停车点。
- 当前车辆速度和加速度。
- 车辆运动约束。

输出：

- 每个轨迹点的目标速度。
- 每个轨迹点的目标加速度。
- 更适合控制器使用的轨迹。

## 16.3 第 11 集主线

```text
输入轨迹
  -> 局部截取
  -> 外部限速处理
  -> 停车接近速度
  -> 曲率限速
  -> 转向角速度限速
  -> 重采样
  -> 选择平滑器
  -> 后处理
```

## 16.4 关键直觉

速度规划不是简单地做：

```text
v_i = min(v_i, speed_limit)
```

因为车辆速度不能瞬间变化。真正需要考虑的是：

$$
0 \le v \le v_{\max}
$$

$$
a_{\min} \le a \le a_{\max}
$$

$$
j_{\min} \le j \le j_{\max}
$$

## 16.5 曲率限速

横向加速度：

$$
a_y = v^2 \kappa
$$

如果给定最大横向加速度：

$$
a_y \le a_{y,\max}
$$

则速度上限为：

$$
v \le \sqrt{\frac{a_{y,\max}}{|\kappa|}}
$$

这非常适合讲给新手，因为直觉很强：

```text
弯越急，曲率越大，速度就必须越低。
```

## 16.6 第 12 集核心问题

```text
如果前方有停车点或限速点，车辆如何在加速度和 jerk 约束下提前减速？
```

## 16.7 jerk 的物理意义

速度：

$$
v(t)=\frac{dx}{dt}
$$

加速度：

$$
a(t)=\frac{dv}{dt}
$$

jerk：

$$
j(t)=\frac{da}{dt}
$$

jerk 影响的是加速度变化得是否突然。乘坐舒适性很大程度上和 jerk 有关。

## 16.8 常 jerk 区间公式

在一段常 jerk 区间内：

$$
a(t)=a_0+jt
$$

$$
v(t)=v_0+a_0t+\frac{1}{2}jt^2
$$

$$
x(t)=x_0+v_0t+\frac{1}{2}a_0t^2+\frac{1}{6}jt^3
$$

这一集的所有解析减速曲线都来自这三个积分公式。

## 16.9 推荐画图

1. 速度曲线 $v(t)$。
2. 加速度曲线 $a(t)$。
3. jerk 曲线 $j(t)$。
4. 梯形加速度曲线和三角形加速度曲线对比。

## 16.10 参数直觉

- 最大速度越高，效率越高，但制动距离更长。
- 最大加速度越大，响应更快，但舒适性可能下降。
- 最大减速度越大，刹车更强，但可能不舒适。
- jerk 限制越严格，速度变化越柔和，但需要更长距离。
- 重采样间距太大，速度曲线粗糙；太小，计算量增加。

## 16.11 常见误区

- 认为速度平滑就是简单低通滤波。
- 认为速度上限降低后，车辆可以立刻达到新速度。
- 认为 jerk 只是数学概念，和乘坐体验无关。
- 认为速度规划只管纵向，和曲率无关。

---

# 17. 第 13 集：Pure Pursuit 横向控制

## 17.1 核心问题

```text
给定一条目标轨迹，车辆如何用最简单的几何方法算出转向角？
```

## 17.2 开场直觉

人开车不会盯着车头下面的点，而是看前方一段距离。Pure Pursuit 也是这样，它不是追最近点，而是追前方的前瞻点。

## 17.3 最小模型

在车辆坐标系下：

- 车辆在原点。
- 车头朝 $x$ 轴。
- 目标点为 $(x,y)$。

Pure Pursuit 假设车辆沿一段圆弧追到目标点。圆心在 $y$ 轴上，设为 $(0,R)$。

目标点在圆上：

$$
x^2+(y-R)^2=R^2
$$

展开得到：

$$
R=\frac{x^2+y^2}{2y}
$$

曲率：

$$
\kappa = \frac{1}{R}=\frac{2y}{x^2+y^2}
$$

再由自行车模型：

$$
\kappa=\frac{\tan\delta}{L}
$$

得到：

$$
\delta=\arctan(L\kappa)
$$

## 17.4 这一集的讲法

Pure Pursuit 最适合用几何图讲。不要先讲代码，先画：

1. 车辆坐标系。
2. 前瞻点。
3. 连接车辆和前瞻点的圆。
4. 圆心为什么在 $y$ 轴上。
5. 曲率公式怎么来。

## 17.5 工程增强

基础 Pure Pursuit 之后，再讲 Autoware 中常见工程处理：

- 最近点搜索要看距离和朝向。
- 前瞻距离会随速度、曲率、横向误差变化。
- 离散轨迹需要插值找前瞻点。
- 轨迹需要重采样和平滑。
- 前进和倒车要分开处理。
- 求解失败需要 fallback。
- 控制器还会生成预测轨迹用于调试和评估。

## 17.6 参数直觉

前瞻距离是最核心参数。

如果前瞻距离小：

- 跟踪更积极。
- 低速精度较好。
- 高速容易振荡。

如果前瞻距离大：

- 行驶更平稳。
- 高速更稳定。
- 弯道可能切弯或响应慢。

## 17.7 常见误区

- Pure Pursuit 不是追最近点。
- 前瞻距离不是越小越准。
- 曲率不是方向盘角，方向盘角还要经过车辆模型转换。
- 轨迹质量差时，控制器再好也会抖。

---

# 18. 第 14 集和第 15 集：横向 MPC

MPC 是这套课里最难的部分。建议分成两集，甚至三集。

## 18.1 第 14 集核心问题

```text
为什么横向控制要从车辆几何、Frenet 误差和状态空间模型开始？
```

## 18.2 第 14 集主线

```text
阿克曼几何
  -> 自行车模型
  -> 世界坐标运动学
  -> 参考轨迹
  -> Frenet 横向误差和航向误差
  -> 小误差近似
  -> 线性误差模型
```

## 18.3 阿克曼几何

最基础关系：

$$
\tan\delta=\frac{L}{R}
$$

曲率：

$$
\kappa=\frac{1}{R}
$$

所以：

$$
\kappa=\frac{\tan\delta}{L}
$$

## 18.4 自行车模型

世界坐标下：

$$
\dot{x}=v\cos\psi
$$

$$
\dot{y}=v\sin\psi
$$

$$
\dot{\psi}=\frac{v}{L}\tan\delta
$$

这组公式是横向控制的起点。

## 18.5 为什么要 Frenet 误差

控制器真正关心的不是全局坐标 $x,y$ 是否等于某个点，而是：

- 车辆离参考轨迹横向偏了多少。
- 车辆朝向和参考轨迹朝向差多少。
- 当前转角和期望转角差多少。

所以引入：

$$
e
$$

表示横向误差，引入：

$$
\theta_e = \psi - \psi_r
$$

表示航向误差。

## 18.6 第 15 集核心问题

```text
MPC 如何把未来一段时间的控制问题变成 QP 求解器可以吃的矩阵？
```

## 18.7 第 15 集主线

```text
连续误差模型
  -> 线性化
  -> 离散化
  -> 单步状态方程
  -> 预测时域展开
  -> 代价函数
  -> 输入约束
  -> QP 标准形式
  -> 只执行第一个控制量
```

## 18.8 单步离散模型

$$
x_{k+1}=A_d x_k+B_d u_k+W_d
$$

其中：

- $x_k$ 是误差状态。
- $u_k$ 是控制输入。
- $A_d$ 描述状态自然演化。
- $B_d$ 描述输入如何影响状态。
- $W_d$ 是参考曲率、线性化点等带来的仿射项。

## 18.9 预测时域展开

把未来 $N$ 步堆起来：

$$
X=A_{\mathrm{ex}}x_0+B_{\mathrm{ex}}U+W_{\mathrm{ex}}
$$

这是 MPC 最关键的一步。它的意思是：

```text
只要给定未来一串输入 U，未来一串状态 X 就能被预测出来。
```

## 18.10 代价函数

MPC 通常同时追求：

- 横向误差小。
- 航向误差小。
- 控制输入不要太大。
- 控制输入变化不要太剧烈。
- 尽量接近前馈转角。

可以抽象写成：

$$
J
=
\sum_k x_k^T Q x_k
+
\sum_k u_k^T R u_k
+
\sum_k (u_k-u_{k-1})^T R_d (u_k-u_{k-1})
$$

代入预测方程后，变成：

$$
\min_U \frac{1}{2}U^T H U + g^T U
$$

再加上转角和转角变化率约束，就得到 QP。

## 18.11 为什么只执行第一个控制量

MPC 每次求出未来一串控制：

$$
U^* = [u_0^*,u_1^*,\dots,u_{N-1}^*]
$$

但系统只执行第一个：

$$
u_0^*
$$

下一帧获取新的车辆状态后，重新求解。这样可以不断吸收真实车辆误差和外界变化。

## 18.12 常见误区

- MPC 不是直接把参考轨迹点喂给 QP。
- QP 的变量通常不是整条轨迹本身，而是控制序列或某种优化变量。
- 线性化不是数学装饰，而是为了把问题变成可实时求解的形式。
- 前馈转角不是多余项，它帮助弯道跟踪。
- 只执行第一个输入是 MPC 的核心闭环思想。

---

# 19. 第 16 集：总复盘

## 19.1 核心问题

```text
一条 Autoware 轨迹到底是怎样一步步变成车辆控制命令的？
```

## 19.2 复盘顺序

用一个完整案例串起来：

```text
车辆当前位置和目标点
  -> 在 Lanelet 地图中找到起点和终点 lanelet
  -> Mission Planner 生成 route
  -> route sections 提供可行驶空间
  -> 如果车辆不在正常车道中，Start Planner / Pull Out 生成回归路径
  -> 路径经过 Elastic Band 或其他方法平滑
  -> MPT 等模块优化局部轨迹
  -> Velocity Smoother 生成速度曲线
  -> Pure Pursuit 或 MPC 计算转向
  -> 纵向控制计算加速度或制动
  -> 底盘执行
```

## 19.3 最后一集要回答的问题

- 哪些模块属于规划？
- 哪些模块属于控制？
- 哪些模块更偏几何？
- 哪些模块更偏优化？
- 哪些模块对地图依赖强？
- 哪些模块对车辆模型依赖强？
- 如果车抖动，应该先看轨迹还是控制器？
- 如果车刹车突兀，应该先看速度规划还是纵向控制？
- 如果路线不对，应该先看地图、目标点投影还是行为规划？

## 19.4 推荐结尾

```text
Autoware 的规划控制不是一个单独算法，而是一条链。
学习这条链时，不要只问某个函数做了什么，还要问它的输入从哪里来、输出给谁用、解决的物理问题是什么。
```

---

# 20. PPT 制作建议

## 20.1 每集 PPT 推荐结构

每集大约 25 到 45 页，按下面结构：

| 页码范围 | 内容 |
|---|---|
| 1 | 标题页 |
| 2 | 本集要解决的问题 |
| 3 | 模块在总链路中的位置 |
| 4 到 8 | 直觉场景和问题来源 |
| 9 到 18 | 核心数学模型 |
| 19 到 28 | 工程流程图 |
| 29 到 34 | 参数直觉 |
| 35 到 40 | 代码位置或伪代码 |
| 41 到 44 | 常见误区 |
| 45 | 总结和下一集预告 |

如果一集公式很多，例如 MPC 或 MPT，可以拆成两集，不要硬塞。

## 20.2 每页 PPT 的原则

一页只讲一个意思。

推荐格式：

```text
标题: 这一页的结论
图: 一个简单流程或几何图
文字: 3 到 5 行解释
公式: 只放必要公式
```

不要一页放整段文章。文章适合读，PPT 适合讲。

## 20.3 公式页怎么讲

公式页不要直接念公式。建议按三步：

1. 先说这个公式想描述什么物理现象。
2. 再解释每个符号。
3. 最后说这个公式进入工程后会变成什么变量或矩阵。

例如讲：

$$
\kappa=\frac{2y}{x^2+y^2}
$$

不要只说“这是 Pure Pursuit 曲率公式”，而要说：

```text
它描述的是：目标点越偏左，车越应该向左转；目标点越远，同样的横向偏移对应的转弯越缓。
```

---

# 21. 录制流程建议

## 21.1 一集视频的生产流程

建议你每一集按这个流程做：

```text
1. 先写本集一句话目标
2. 从已有文章中摘出本集相关章节
3. 整理成 8 到 10 个讲解小节
4. 每小节写 3 到 5 句口播提纲
5. 画核心图
6. 制作 PPT
7. 试讲一遍
8. 删除太细或太散的内容
9. 正式录制
10. 回看并记录下一集要补充的问题
```

## 21.2 不建议一开始就追求完美

第一套课程最重要的是完整跑通。你可以先录精简版 8 集，每集 30 到 45 分钟。录完之后，你会更清楚观众会卡在哪里，再回来扩展完整版。

## 21.3 录制时建议同时打开的材料

- PPT。
- 对应 `concept/` 文章。
- Autoware 源码或关键代码片段。
- 一张总数据流图。
- 参数文件或简化参数表。

视频中不一定要频繁切源码，但你自己讲的时候要知道代码在哪里。

---

# 22. 代码讲解方式建议

即使你的课程以原理为重点，也建议每集最后保留 5 到 10 分钟代码定位。代码讲解不要逐行展开，而是定位到：

- 这个模块入口在哪里。
- 输入消息是什么。
- 输出消息是什么。
- 核心函数是哪几个。
- 哪些函数对应刚才讲的数学步骤。
- 参数从哪里来。

推荐用下面的说法：

```text
刚才我们讲的是原理主线。现在看代码时，只需要找三类东西：
第一，数据从哪里进来；
第二，核心模型或优化问题在哪里构造；
第三，结果在哪里发布给下游。
```

这样观众不会被大量辅助函数带跑。

---

# 23. 每类算法的讲解重点

## 23.1 图搜索类

包括：

- Mission Planner。
- Hybrid A*。

讲解重点：

- 状态是什么。
- 边或动作是什么。
- 代价是什么。
- 启发函数是什么。
- 终止条件是什么。
- 搜索结果如何变回路径或 route。

通用模板：

```text
搜索空间 + 状态转移 + 代价函数 + 终止条件 = 搜索算法
```

## 23.2 QP 优化类

包括：

- Elastic Band。
- MPT。
- MPC。
- 部分速度优化器。

讲解重点：

- 优化变量是什么。
- 目标函数惩罚什么。
- 约束来自哪里。
- 为什么目标是二次型。
- 为什么约束是线性的或可线性化的。
- 求解结果如何转换回轨迹或控制命令。

通用模板：

```text
变量决定自由度，目标决定偏好，约束决定安全边界。
```

## 23.3 几何控制类

包括：

- Pure Pursuit。
- 阿克曼几何。
- 自行车模型。

讲解重点：

- 坐标系怎么定义。
- 几何关系怎么画。
- 曲率和转角怎么转换。
- 参数如何影响稳定性。

通用模板：

```text
先画几何，再推公式，最后解释物理意义。
```

## 23.4 速度规划类

包括：

- Velocity Smoother。
- jerk 受限减速。
- 曲率限速。

讲解重点：

- 速度上限从哪里来。
- 车辆为什么不能瞬间满足上限。
- 加速度和 jerk 约束如何改变速度曲线。
- 前向滤波和后向滤波分别解决什么。

通用模板：

```text
先得到“不能超过多少”，再判断“能不能平滑做到”。
```

---

# 24. 新手最容易卡住的地方

## 24.1 不知道模块边界

解决办法：

每集都重新画一次数据流，并用一句话说明本模块输入输出。

## 24.2 公式看得懂，但不知道为什么要用

解决办法：

每个公式前先讲场景。例如：

- 为什么要二阶差分？因为要惩罚折角。
- 为什么要 Frenet？因为控制器关心相对参考线的误差。
- 为什么要 jerk？因为乘客对加速度突变敏感。
- 为什么要 QP？因为目标是二次型，约束可以写成线性形式。

## 24.3 代码看不懂

解决办法：

先只看主流程函数。不要一开始钻模板、工具函数、消息转换和异常处理。

## 24.4 不知道参数怎么调

解决办法：

每集专门做一个“参数直觉”表格。表格只回答：

```text
调大怎样？
调小怎样？
现象是什么？
```

## 24.5 规划和控制混在一起

解决办法：

反复强调：

- 规划输出未来希望车辆怎么走。
- 控制输出当前车辆怎么操作。

---

# 25. 每集结尾的固定总结模板

你可以每集最后用这个模板：

```text
这一集我们只需要记住三件事：

第一，xxx 模块解决的是 xxx 问题。
第二，它的核心数学思想是 xxx。
第三，在 Autoware 工程里，它额外处理了 xxx，目的是让算法在线运行更稳定。

下一集我们会继续看：xxx。
```

这种固定结构对新手很友好。

---

# 26. 建议先做的 3 个视频

如果你完全不知道从哪里开始，建议先做这 3 个：

## 26.1 第一优先级：规划控制总览

原因：

- 它给后续所有内容建立地图。
- 不需要先讲复杂公式。
- 可以帮助你自己整理课程边界。

## 26.2 第二优先级：Mission Planner

原因：

- route/path/trajectory 的区别会在这里变得具体。
- Lanelet 图搜索是后续规划的基础。
- 你已经有很完整的文章。

## 26.3 第三优先级：Pure Pursuit

原因：

- 几何直觉强。
- 公式推导短。
- 新手容易获得成就感。
- 能自然过渡到自行车模型和 MPC。

不建议第一集就讲 MPC。MPC 很重要，但它依赖太多前置概念。

---

# 27. 后续可以怎么多次问我，让我帮你继续生成

这份文档是总策划。后面你可以分多轮让我继续帮你把每一集做细。建议按下面顺序来问。

## 27.1 第一次追问：生成某一集详细脚本

你可以这样问：

```text
请根据 AUTOWARE规划控制层视频教程制作指南.md，
帮我生成第 1 集“规划控制总览”的详细视频脚本，
要求包含逐页 PPT 大纲、每页讲解口播、需要画的图、结尾总结。
```

## 27.2 第二次追问：生成 PPT 页内容

```text
请把第 1 集脚本进一步整理成 PPT 制作稿，
每一页包括标题、页面文字、讲解备注、配图建议。
```

## 27.3 第三次追问：生成图示说明

```text
请为第 1 集生成所有需要的图示草图，
用 mermaid、ASCII 图或 draw.io 绘图描述都可以，
要求适合我照着做 PPT。
```

## 27.4 第四次追问：结合源码定位

```text
请根据第 1 集内容，帮我在本地 Autoware 代码中定位相关模块入口、
关键类、关键函数和参数文件，并生成一份代码导读文档。
```

## 27.5 第五次追问：生成口播逐字稿

```text
请把第 1 集 PPT 大纲改写成 30 分钟左右的口播逐字稿，
语言自然一点，适合视频录制，不要像论文。
```

## 27.6 第六次追问：做新手检查

```text
请站在新手观众角度审查第 1 集脚本，
指出哪里可能听不懂、哪里需要补图、哪里公式出现太早。
```

## 27.7 第七次追问：制作下一集

```text
请继续生成第 2 集 Lanelet 地图基础的视频脚本，
并且保证它和第 1 集自然衔接。
```

## 27.8 最推荐的工作节奏

不要一次让我生成全部 16 集逐字稿。内容会太多，质量也不容易控制。

推荐节奏是：

```text
一轮只打磨一集。
每集按“脚本 -> PPT -> 图示 -> 源码定位 -> 逐字稿 -> 新手审查”的顺序走。
```

这样生成的教程会更扎实。

---

# 28. 你可以直接复制使用的提问模板

下面这些模板可以直接拿来问我。

## 28.1 生成单集脚本

```text
请基于 AUTOWARE规划控制层视频教程制作指南.md 和 concept/xxx.md，
生成第 N 集《标题》的详细视频教程脚本。
要求：
1. 面向新手。
2. 原理为主，代码为辅。
3. 包含课程目标、输入输出、核心公式、讲解顺序、配图建议、常见误区。
4. 给出 30 到 45 分钟的时间分配。
5. 最后给出 5 句话总结。
```

## 28.2 生成 PPT 大纲

```text
请把第 N 集脚本整理成 PPT 大纲。
每一页需要包含：
1. 页标题。
2. 页面展示文字。
3. 讲解备注。
4. 配图或动画建议。
5. 预计讲解时间。
```

## 28.3 生成逐字稿

```text
请把第 N 集 PPT 大纲扩展成口播逐字稿。
要求语言自然，像老师在讲课，不要像书面论文。
每 3 到 5 分钟插入一次“这里我们停一下总结”的过渡。
```

## 28.4 生成源码导读

```text
请根据第 N 集主题，在本地代码中查找相关实现，
生成源码导读文档。
要求：
1. 列出关键目录和文件。
2. 说明入口函数。
3. 说明输入输出消息。
4. 把代码流程和视频里的原理步骤对应起来。
5. 不要改代码。
```

## 28.5 检查讲解是否适合新手

```text
请审查第 N 集脚本是否适合新手。
重点检查：
1. 是否缺少前置概念。
2. 公式是否出现太突然。
3. 图示是否足够。
4. 是否有容易误解的术语。
5. 是否需要拆成两集。
```

---

# 29. 最终建议

你现在最应该做的不是继续无限补材料，而是先把课程做成闭环。

建议路线：

```text
先完成精简版 8 集
  -> 每集都有 PPT 和口播提纲
  -> 先录制一版
  -> 根据反馈补充公式、源码和案例
  -> 再扩展成完整版 16 集
```

第一版不需要完美，但必须清楚。对新手来说，最有价值的不是你讲了多少细节，而是你能不能让他知道：

```text
这个模块为什么存在；
它接收什么；
它输出什么；
核心原理是什么；
它在 Autoware 整条链路里处于哪里。
```

只要你坚持这条讲法，这套教程就会非常有体系。

