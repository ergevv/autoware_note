虽然CAN总线不“测量”加速度，但它承载了由传感器测得的加速度数据。具体流程如下：
    加速度传感器检测到车辆的加速度（单位通常是 m/s² 或 g）；
    所属的ECU（如ESP模块）将该数据打包成CAN帧；
    通过CAN总线广播到整车网络；
    其他需要该信息的ECU（如仪表盘、变速箱控制单元、车载诊断设备）可以接收并使用这些数据。
    因此，通过监听CAN总线，我们可以间接“读取”车辆的加速度信息。